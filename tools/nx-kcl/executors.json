{
  "executors": {
    "publish": {
      "implementation": "./src/release/publish-executor",
      "schema": "./src/release/publish-schema.json",
      "description": "Publish a KCL package to an OCI registry via `kcl mod push`."
    },
    "remove": {
      "implementation": "./src/remove/remove-executor",
      "schema": "./src/remove/remove-schema.json",
      "description": "Remove a dependency from a KCL package and reconcile kcl.mod.lock."
    },
    "render": {
      "implementation": "./src/render/render-executor",
      "schema": "./src/render/render-schema.json",
      "description": "Render a Composition locally against its example XR via `crossplane render`, using the working tree instead of the published OCI image."
    }
  }
}
