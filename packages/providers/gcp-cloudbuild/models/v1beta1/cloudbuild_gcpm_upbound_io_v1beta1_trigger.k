"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema Trigger:
    r"""
    Trigger is the Schema for the Triggers API. Configuration for an automated build in response to source repository changes.

    Attributes
    ----------
    apiVersion : str, default is "cloudbuild.gcp.m.upbound.io/v1beta1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "Trigger", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : CloudbuildGcpmUpboundIoV1beta1TriggerSpec, default is Undefined, required
        spec
    status : CloudbuildGcpmUpboundIoV1beta1TriggerStatus, default is Undefined, optional
        status
    """


    apiVersion: "cloudbuild.gcp.m.upbound.io/v1beta1" = "cloudbuild.gcp.m.upbound.io/v1beta1"

    kind: "Trigger" = "Trigger"

    metadata?: v1.ObjectMeta

    spec: CloudbuildGcpmUpboundIoV1beta1TriggerSpec

    status?: CloudbuildGcpmUpboundIoV1beta1TriggerStatus


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpec:
    r"""
    TriggerSpec defines the desired state of Trigger

    Attributes
    ----------
    forProvider : CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProvider, default is Undefined, required
        for provider
    initProvider : CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProvider, default is Undefined, optional
        init provider
    managementPolicies : [str], default is ["*"], optional
        THIS IS A BETA FIELD. It is on by default but can be opted out
        through a Crossplane feature flag.
        ManagementPolicies specify the array of actions Crossplane is allowed to
        take on the managed and external resources.
        See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
        and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
    providerConfigRef : CloudbuildGcpmUpboundIoV1beta1TriggerSpecProviderConfigRef, default is Undefined, optional
        provider config ref
    writeConnectionSecretToRef : CloudbuildGcpmUpboundIoV1beta1TriggerSpecWriteConnectionSecretToRef, default is Undefined, optional
        write connection secret to ref
    """


    forProvider: CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProvider

    initProvider?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProvider

    managementPolicies?: [str] = ["*"]

    providerConfigRef?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecProviderConfigRef

    writeConnectionSecretToRef?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecWriteConnectionSecretToRef


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProvider:
    r"""
    cloudbuild gcpm upbound io v1beta1 trigger spec for provider

    Attributes
    ----------
    approvalConfig : CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderApprovalConfig, default is Undefined, optional
        approval config
    bitbucketServerTriggerConfig : CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBitbucketServerTriggerConfig, default is Undefined, optional
        bitbucket server trigger config
    build : CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuild, default is Undefined, optional
        build
    description : str, default is Undefined, optional
        Human-readable description of the trigger.
    disabled : bool, default is Undefined, optional
        Whether the trigger is disabled or not. If true, the trigger will never result in a build.
    filename : str, default is Undefined, optional
        Path, from the source root, to a file whose contents is used for the template.
        Either a filename or build template must be provided. Set this only when using trigger_template or github.
        When using Pub/Sub, Webhook or Manual set the file name using git_file_source instead.
    $filter : str, default is Undefined, optional
        A Common Expression Language string. Used only with Pub/Sub and Webhook.
    gitFileSource : CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderGitFileSource, default is Undefined, optional
        git file source
    github : CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderGithub, default is Undefined, optional
        github
    ignoredFiles : [str], default is Undefined, optional
        ignoredFiles and includedFiles are file glob matches using https://golang.org/pkg/path/filepath/#Match
        extended with support for **.
        If ignoredFiles and changed files are both empty, then they are not
        used to determine whether or not to trigger a build.
        If ignoredFiles is not empty, then we ignore any files that match any
        of the ignored_file globs. If the change has no files that are outside
        of the ignoredFiles globs, then we do not trigger a build.
    includeBuildLogs : str, default is Undefined, optional
        Build logs will be sent back to GitHub as part of the checkrun
        result.  Values can be INCLUDE_BUILD_LOGS_UNSPECIFIED or
        INCLUDE_BUILD_LOGS_WITH_STATUS
        Possible values are: INCLUDE_BUILD_LOGS_UNSPECIFIED, INCLUDE_BUILD_LOGS_WITH_STATUS.
    includedFiles : [str], default is Undefined, optional
        ignoredFiles and includedFiles are file glob matches using https://golang.org/pkg/path/filepath/#Match
        extended with support for **.
        If any of the files altered in the commit pass the ignoredFiles filter
        and includedFiles is empty, then as far as this filter is concerned, we
        should trigger the build.
        If any of the files altered in the commit pass the ignoredFiles filter
        and includedFiles is not empty, then we make sure that at least one of
        those files matches a includedFiles glob. If not, then we do not trigger
        a build.
    location : str, default is Undefined, optional
        The Cloud Build location for the trigger.
        If not specified, "global" is used.
    name : str, default is Undefined, optional
        Name of the trigger. Must be unique within the project.
    project : str, default is Undefined, optional
        The ID of the project in which the resource belongs.
        If it is not provided, the provider project is used.
    pubsubConfig : CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderPubsubConfig, default is Undefined, optional
        pubsub config
    repositoryEventConfig : CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderRepositoryEventConfig, default is Undefined, optional
        repository event config
    serviceAccount : str, default is Undefined, optional
        The service account used for all user-controlled operations including
        triggers.patch, triggers.run, builds.create, and builds.cancel.
        If no service account is set, then the standard Cloud Build service account
        ([PROJECT_NUM]@system.gserviceaccount.com) will be used instead.
        Format: projects/{PROJECT_ID}/serviceAccounts/{ACCOUNT_ID_OR_EMAIL}
    serviceAccountRef : CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderServiceAccountRef, default is Undefined, optional
        service account ref
    serviceAccountSelector : CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderServiceAccountSelector, default is Undefined, optional
        service account selector
    sourceToBuild : CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderSourceToBuild, default is Undefined, optional
        source to build
    substitutions : {str:str}, default is Undefined, optional
        Substitutions data for Build resource.
    tags : [str], default is Undefined, optional
        Tags for annotation of a BuildTrigger
    triggerTemplate : CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderTriggerTemplate, default is Undefined, optional
        trigger template
    webhookConfig : CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderWebhookConfig, default is Undefined, optional
        webhook config
    """


    approvalConfig?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderApprovalConfig

    bitbucketServerTriggerConfig?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBitbucketServerTriggerConfig

    build?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuild

    description?: str

    disabled?: bool

    filename?: str

    $filter?: str

    gitFileSource?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderGitFileSource

    github?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderGithub

    ignoredFiles?: [str]

    includeBuildLogs?: str

    includedFiles?: [str]

    location?: str

    name?: str

    project?: str

    pubsubConfig?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderPubsubConfig

    repositoryEventConfig?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderRepositoryEventConfig

    serviceAccount?: str

    serviceAccountRef?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderServiceAccountRef

    serviceAccountSelector?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderServiceAccountSelector

    sourceToBuild?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderSourceToBuild

    substitutions?: {str:str}

    tags?: [str]

    triggerTemplate?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderTriggerTemplate

    webhookConfig?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderWebhookConfig


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderApprovalConfig:
    r"""
    Configuration for manual approval to start a build invocation of this BuildTrigger.
    Builds created by this trigger will require approval before they execute.
    Any user with a Cloud Build Approver role for the project can approve a build.
    Structure is documented below.

    Attributes
    ----------
    approvalRequired : bool, default is Undefined, optional
        Whether or not approval is needed. If this is set on a build, it will become pending when run,
        and will need to be explicitly approved to start.
    """


    approvalRequired?: bool


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBitbucketServerTriggerConfig:
    r"""
    BitbucketServerTriggerConfig describes the configuration of a trigger that creates a build whenever a Bitbucket Server event is received.
    Structure is documented below.

    Attributes
    ----------
    bitbucketServerConfigResource : str, default is Undefined, optional
        The Bitbucket server config resource that this trigger config maps to.
    projectKey : str, default is Undefined, optional
        Key of the project that the repo is in. For example: The key for https://mybitbucket.server/projects/TEST/repos/test-repo is "TEST".
    pullRequest : CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBitbucketServerTriggerConfigPullRequest, default is Undefined, optional
        pull request
    push : CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBitbucketServerTriggerConfigPush, default is Undefined, optional
        push
    repoSlug : str, default is Undefined, optional
        Slug of the repository. A repository slug is a URL-friendly version of a repository name, automatically generated by Bitbucket for use in the URL.
        For example, if the repository name is 'test repo', in the URL it would become 'test-repo' as in https://mybitbucket.server/projects/TEST/repos/test-repo.
    """


    bitbucketServerConfigResource?: str

    projectKey?: str

    pullRequest?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBitbucketServerTriggerConfigPullRequest

    push?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBitbucketServerTriggerConfigPush

    repoSlug?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBitbucketServerTriggerConfigPullRequest:
    r"""
    Filter to match changes in pull requests.
    Structure is documented below.

    Attributes
    ----------
    branch : str, default is Undefined, optional
        Regex of branches to match.
        The syntax of the regular expressions accepted is the syntax accepted by
        RE2 and described at https://github.com/google/re2/wiki/Syntax
    commentControl : str, default is Undefined, optional
        Configure builds to run whether a repository owner or collaborator need to comment /gcbrun.
        Possible values are: COMMENTS_DISABLED, COMMENTS_ENABLED, COMMENTS_ENABLED_FOR_EXTERNAL_CONTRIBUTORS_ONLY.
    invertRegex : bool, default is Undefined, optional
        Only trigger a build if the revision regex does NOT match the revision regex.
    """


    branch?: str

    commentControl?: str

    invertRegex?: bool


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBitbucketServerTriggerConfigPush:
    r"""
    Filter to match changes in refs like branches, tags.
    Structure is documented below.

    Attributes
    ----------
    branch : str, default is Undefined, optional
        Regex of branches to match.
        The syntax of the regular expressions accepted is the syntax accepted by
        RE2 and described at https://github.com/google/re2/wiki/Syntax
    invertRegex : bool, default is Undefined, optional
        Only trigger a build if the revision regex does NOT match the revision regex.
    tag : str, default is Undefined, optional
        Regex of tags to match.
        The syntax of the regular expressions accepted is the syntax accepted by
        RE2 and described at https://github.com/google/re2/wiki/Syntax
    """


    branch?: str

    invertRegex?: bool

    tag?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuild:
    r"""
    Contents of the build template. Either a filename or build template must be provided.
    Structure is documented below.

    Attributes
    ----------
    artifacts : CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuildArtifacts, default is Undefined, optional
        artifacts
    availableSecrets : CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuildAvailableSecrets, default is Undefined, optional
        available secrets
    images : [str], default is Undefined, optional
        A list of images to be pushed upon the successful completion of all build steps.
        The images are pushed using the builder service account's credentials.
        The digests of the pushed images will be stored in the Build resource's results field.
        If any of the images fail to be pushed, the build status is marked FAILURE.
    logsBucket : str, default is Undefined, optional
        Google Cloud Storage bucket where logs should be written.
        Logs file names will be of the format ${logsBucket}/log-${build_id}.txt.
    options : CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuildOptions, default is Undefined, optional
        options
    queueTtl : str, default is Undefined, optional
        TTL in queue for this build. If provided and the build is enqueued longer than this value,
        the build will expire and the build status will be EXPIRED.
        The TTL starts ticking from createTime.
        A duration in seconds with up to nine fractional digits, terminated by 's'. Example: "3.5s".
    secret : [CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuildSecretItems0], default is Undefined, optional
        Secrets to decrypt using Cloud Key Management Service.
        Structure is documented below.
    source : CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuildSource, default is Undefined, optional
        source
    step : [CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuildStepItems0], default is Undefined, optional
        The operations to be performed on the workspace.
        Structure is documented below.
    substitutions : {str:str}, default is Undefined, optional
        Substitutions data for Build resource.
    tags : [str], default is Undefined, optional
        Tags for annotation of a Build. These are not docker tags.
    timeout : str, default is Undefined, optional
        Amount of time that this build should be allowed to run, to second granularity.
        If this amount of time elapses, work on the build will cease and the build status will be TIMEOUT.
        This timeout must be equal to or greater than the sum of the timeouts for build steps within the build.
        The expected format is the number of seconds followed by s.
        Default time is ten minutes (600s).
    """


    artifacts?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuildArtifacts

    availableSecrets?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuildAvailableSecrets

    images?: [str]

    logsBucket?: str

    options?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuildOptions

    queueTtl?: str

    secret?: [CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuildSecretItems0]

    source?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuildSource

    step?: [CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuildStepItems0]

    substitutions?: {str:str}

    tags?: [str]

    timeout?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuildArtifacts:
    r"""
    Artifacts produced by the build that should be uploaded upon successful completion of all build steps.
    Structure is documented below.

    Attributes
    ----------
    images : [str], default is Undefined, optional
        A list of images to be pushed upon the successful completion of all build steps.
        The images are pushed using the builder service account's credentials.
        The digests of the pushed images will be stored in the Build resource's results field.
        If any of the images fail to be pushed, the build status is marked FAILURE.
    mavenArtifacts : [CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuildArtifactsMavenArtifactsItems0], default is Undefined, optional
        A Maven artifact to upload to Artifact Registry upon successful completion of all build steps.
        The location and generation of the uploaded objects will be stored in the Build resource's results field.
        If any objects fail to be pushed, the build is marked FAILURE.
        Structure is documented below.
    npmPackages : [CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuildArtifactsNpmPackagesItems0], default is Undefined, optional
        Npm package to upload to Artifact Registry upon successful completion of all build steps.
        The location and generation of the uploaded objects will be stored in the Build resource's results field.
        If any objects fail to be pushed, the build is marked FAILURE.
        Structure is documented below.
    objects : CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuildArtifactsObjects, default is Undefined, optional
        objects
    pythonPackages : [CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuildArtifactsPythonPackagesItems0], default is Undefined, optional
        Python package to upload to Artifact Registry upon successful completion of all build steps. A package can encapsulate multiple objects to be uploaded to a single repository.
        The location and generation of the uploaded objects will be stored in the Build resource's results field.
        If any objects fail to be pushed, the build is marked FAILURE.
        Structure is documented below.
    """


    images?: [str]

    mavenArtifacts?: [CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuildArtifactsMavenArtifactsItems0]

    npmPackages?: [CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuildArtifactsNpmPackagesItems0]

    objects?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuildArtifactsObjects

    pythonPackages?: [CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuildArtifactsPythonPackagesItems0]


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuildArtifactsMavenArtifactsItems0:
    r"""
    cloudbuild gcpm upbound io v1beta1 trigger spec for provider build artifacts maven artifacts items0

    Attributes
    ----------
    artifactId : str, default is Undefined, optional
        Maven artifactId value used when uploading the artifact to Artifact Registry.
    groupId : str, default is Undefined, optional
        Maven groupId value used when uploading the artifact to Artifact Registry.
    path : str, default is Undefined, optional
        Path at which to mount the volume.
        Paths must be absolute and cannot conflict with other volume paths on
        the same build step or with certain reserved volume paths.
    repository : str, default is Undefined, optional
        The qualified resource name of the Repo API repository.
        Either uri or repository can be specified and is required.
    version : str, default is Undefined, optional
        Maven version value used when uploading the artifact to Artifact Registry.
    """


    artifactId?: str

    groupId?: str

    path?: str

    repository?: str

    version?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuildArtifactsNpmPackagesItems0:
    r"""
    cloudbuild gcpm upbound io v1beta1 trigger spec for provider build artifacts npm packages items0

    Attributes
    ----------
    packagePath : str, default is Undefined, optional
        Path to the package.json. e.g. workspace/path/to/package
    repository : str, default is Undefined, optional
        The qualified resource name of the Repo API repository.
        Either uri or repository can be specified and is required.
    """


    packagePath?: str

    repository?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuildArtifactsObjects:
    r"""
    A list of objects to be uploaded to Cloud Storage upon successful completion of all build steps.
    Files in the workspace matching specified paths globs will be uploaded to the
    Cloud Storage location using the builder service account's credentials.
    The location and generation of the uploaded objects will be stored in the Build resource's results field.
    If any objects fail to be pushed, the build is marked FAILURE.
    Structure is documented below.

    Attributes
    ----------
    location : str, default is Undefined, optional
        Cloud Storage bucket and optional object path, in the form "gs://bucket/path/to/somewhere/".
        Files in the workspace matching any path pattern will be uploaded to Cloud Storage with
        this location as a prefix.
    paths : [str], default is Undefined, optional
        Path globs used to match files in the build's workspace. For Python/ Twine, this is usually dist/*, and sometimes additionally an .asc file.
    """


    location?: str

    paths?: [str]


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuildArtifactsPythonPackagesItems0:
    r"""
    cloudbuild gcpm upbound io v1beta1 trigger spec for provider build artifacts python packages items0

    Attributes
    ----------
    paths : [str], default is Undefined, optional
        Path globs used to match files in the build's workspace. For Python/ Twine, this is usually dist/*, and sometimes additionally an .asc file.
    repository : str, default is Undefined, optional
        The qualified resource name of the Repo API repository.
        Either uri or repository can be specified and is required.
    """


    paths?: [str]

    repository?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuildAvailableSecrets:
    r"""
    Secrets and secret environment variables.
    Structure is documented below.

    Attributes
    ----------
    secretManager : [CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuildAvailableSecretsSecretManagerItems0], default is Undefined, optional
        Pairs a secret environment variable with a SecretVersion in Secret Manager.
        Structure is documented below.
    """


    secretManager?: [CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuildAvailableSecretsSecretManagerItems0]


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuildAvailableSecretsSecretManagerItems0:
    r"""
    cloudbuild gcpm upbound io v1beta1 trigger spec for provider build available secrets secret manager items0

    Attributes
    ----------
    env : str, default is Undefined, optional
        A list of environment variable definitions to be used when
        running a step.
        The elements are of the form "KEY=VALUE" for the environment variable
        "KEY" being given the value "VALUE".
    versionName : str, default is Undefined, optional
        Resource name of the SecretVersion. In format: projects//secrets//versions/*
    """


    env?: str

    versionName?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuildOptions:
    r"""
    Special options for this build.
    Structure is documented below.

    Attributes
    ----------
    diskSizeGb : float, default is Undefined, optional
        Requested disk size for the VM that runs the build. Note that this is NOT "disk free";
        some of the space will be used by the operating system and build utilities.
        Also note that this is the minimum disk size that will be allocated for the build --
        the build may run with a larger disk than requested. At present, the maximum disk size
        is 1000GB; builds that request more than the maximum are rejected with an error.
    dynamicSubstitutions : bool, default is Undefined, optional
        Option to specify whether or not to apply bash style string operations to the substitutions.
        NOTE this is always enabled for triggered builds and cannot be overridden in the build configuration file.
    env : [str], default is Undefined, optional
        A list of environment variable definitions to be used when
        running a step.
        The elements are of the form "KEY=VALUE" for the environment variable
        "KEY" being given the value "VALUE".
    logStreamingOption : str, default is Undefined, optional
        Option to define build log streaming behavior to Google Cloud Storage.
        Possible values are: STREAM_DEFAULT, STREAM_ON, STREAM_OFF.
    logging : str, default is Undefined, optional
        Option to specify the logging mode, which determines if and where build logs are stored.
        Possible values are: LOGGING_UNSPECIFIED, LEGACY, GCS_ONLY, STACKDRIVER_ONLY, CLOUD_LOGGING_ONLY, NONE.
    machineType : str, default is Undefined, optional
        Compute Engine machine type on which to run the build.
    requestedVerifyOption : str, default is Undefined, optional
        Requested verifiability options.
        Possible values are: NOT_VERIFIED, VERIFIED.
    secretEnv : [str], default is Undefined, optional
        A list of environment variables which are encrypted using
        a Cloud Key
        Management Service crypto key. These values must be specified in
        the build's Secret.
    sourceProvenanceHash : [str], default is Undefined, optional
        Requested hash for SourceProvenance.
        Each value may be one of: NONE, SHA256, MD5.
    substitutionOption : str, default is Undefined, optional
        Option to specify behavior when there is an error in the substitution checks.
        NOTE this is always set to ALLOW_LOOSE for triggered builds and cannot be overridden
        in the build configuration file.
        Possible values are: MUST_MATCH, ALLOW_LOOSE.
    volumes : [CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuildOptionsVolumesItems0], default is Undefined, optional
        List of volumes to mount into the build step.
        Each volume is created as an empty volume prior to execution of the
        build step. Upon completion of the build, volumes and their contents
        are discarded.
        Using a named volume in only one step is not valid as it is
        indicative of a build request with an incorrect configuration.
        Structure is documented below.
    workerPool : str, default is Undefined, optional
        Option to specify a WorkerPool for the build. Format projects/{project}/workerPools/{workerPool}
        This field is experimental.
    """


    diskSizeGb?: float

    dynamicSubstitutions?: bool

    env?: [str]

    logStreamingOption?: str

    logging?: str

    machineType?: str

    requestedVerifyOption?: str

    secretEnv?: [str]

    sourceProvenanceHash?: [str]

    substitutionOption?: str

    volumes?: [CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuildOptionsVolumesItems0]

    workerPool?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuildOptionsVolumesItems0:
    r"""
    cloudbuild gcpm upbound io v1beta1 trigger spec for provider build options volumes items0

    Attributes
    ----------
    name : str, default is Undefined, optional
        Name of the volume to mount.
        Volume names must be unique per build step and must be valid names for
        Docker volumes. Each named volume must be used by at least two build steps.
    path : str, default is Undefined, optional
        Path at which to mount the volume.
        Paths must be absolute and cannot conflict with other volume paths on
        the same build step or with certain reserved volume paths.
    """


    name?: str

    path?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuildSecretItems0:
    r"""
    cloudbuild gcpm upbound io v1beta1 trigger spec for provider build secret items0

    Attributes
    ----------
    kmsKeyName : str, default is Undefined, optional
        Cloud KMS key name to use to decrypt these envs.
    secretEnv : {str:str}, default is Undefined, optional
        A list of environment variables which are encrypted using
        a Cloud Key
        Management Service crypto key. These values must be specified in
        the build's Secret.
    """


    kmsKeyName?: str

    secretEnv?: {str:str}


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuildSource:
    r"""
    The location of the source files to build.
    One of storageSource or repoSource must be provided.
    Structure is documented below.

    Attributes
    ----------
    repoSource : CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuildSourceRepoSource, default is Undefined, optional
        repo source
    storageSource : CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuildSourceStorageSource, default is Undefined, optional
        storage source
    """


    repoSource?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuildSourceRepoSource

    storageSource?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuildSourceStorageSource


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuildSourceRepoSource:
    r"""
    Location of the source in a Google Cloud Source Repository.
    Structure is documented below.

    Attributes
    ----------
    branchName : str, default is Undefined, optional
        Name of the branch to build. Exactly one a of branch name, tag, or commit SHA must be provided.
        This field is a regular expression.
    commitSha : str, default is Undefined, optional
        Explicit commit SHA to build. Exactly one of a branch name, tag, or commit SHA must be provided.
    dir : str, default is Undefined, optional
        Directory, relative to the source root, in which to run the build.
        This must be a relative path. If a step's dir is specified and
        is an absolute path, this value is ignored for that step's
        execution.
    invertRegex : bool, default is Undefined, optional
        Only trigger a build if the revision regex does NOT match the revision regex.
    projectId : str, default is Undefined, optional
        ID of the project that owns the Cloud Source Repository. If
        omitted, the project ID requesting the build is assumed.
    repoName : str, default is Undefined, optional
        Name of the Cloud Source Repository. If omitted, the name "default" is assumed.
    substitutions : {str:str}, default is Undefined, optional
        Substitutions data for Build resource.
    tagName : str, default is Undefined, optional
        Name of the tag to build. Exactly one of a branch name, tag, or commit SHA must be provided.
        This field is a regular expression.
    """


    branchName?: str

    commitSha?: str

    dir?: str

    invertRegex?: bool

    projectId?: str

    repoName?: str

    substitutions?: {str:str}

    tagName?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuildSourceStorageSource:
    r"""
    Location of the source in an archive file in Google Cloud Storage.
    Structure is documented below.

    Attributes
    ----------
    bucket : str, default is Undefined, optional
        Google Cloud Storage bucket containing the source.
    generation : str, default is Undefined, optional
        Google Cloud Storage generation for the object.
        If the generation is omitted, the latest generation will be used
    object : str, default is Undefined, optional
        Google Cloud Storage object containing the source.
        This object must be a gzipped archive file (.tar.gz) containing source to build.
    """


    bucket?: str

    generation?: str

    object?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuildStepItems0:
    r"""
    cloudbuild gcpm upbound io v1beta1 trigger spec for provider build step items0

    Attributes
    ----------
    allowExitCodes : [float], default is Undefined, optional
        Allow this build step to fail without failing the entire build if and
        only if the exit code is one of the specified codes.
        If allowFailure is also specified, this field will take precedence.
    allowFailure : bool, default is Undefined, optional
        Allow this build step to fail without failing the entire build.
        If false, the entire build will fail if this step fails. Otherwise, the
        build will succeed, but this step will still have a failure status.
        Error information will be reported in the failureDetail field.
        allowExitCodes takes precedence over this field.
    args : [str], default is Undefined, optional
        A list of arguments that will be presented to the step when it is started.
        If the image used to run the step's container has an entrypoint, the args
        are used as arguments to that entrypoint. If the image does not define an
        entrypoint, the first element in args is used as the entrypoint, and the
        remainder will be used as arguments.
    dir : str, default is Undefined, optional
        Directory, relative to the source root, in which to run the build.
        This must be a relative path. If a step's dir is specified and
        is an absolute path, this value is ignored for that step's
        execution.
    entrypoint : str, default is Undefined, optional
        Entrypoint to be used instead of the build step image's
        default entrypoint.
        If unset, the image's default entrypoint is used
    env : [str], default is Undefined, optional
        A list of environment variable definitions to be used when
        running a step.
        The elements are of the form "KEY=VALUE" for the environment variable
        "KEY" being given the value "VALUE".
    id : str, default is Undefined, optional
        Unique identifier for this build step, used in wait_for to
        reference this build step as a dependency.
    name : str, default is Undefined, optional
        Name of the volume to mount.
        Volume names must be unique per build step and must be valid names for
        Docker volumes. Each named volume must be used by at least two build steps.
    script : str, default is Undefined, optional
        A shell script to be executed in the step.
        When script is provided, the user cannot specify the entrypoint or args.
    secretEnv : [str], default is Undefined, optional
        A list of environment variables which are encrypted using
        a Cloud Key
        Management Service crypto key. These values must be specified in
        the build's Secret.
    timeout : str, default is Undefined, optional
        Time limit for executing this build step. If not defined,
        the step has no
        time limit and will be allowed to continue to run until either it
        completes or the build itself times out.
    timing : str, default is Undefined, optional
        Output only. Stores timing information for executing this
        build step.
    volumes : [CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuildStepItems0VolumesItems0], default is Undefined, optional
        List of volumes to mount into the build step.
        Each volume is created as an empty volume prior to execution of the
        build step. Upon completion of the build, volumes and their contents
        are discarded.
        Using a named volume in only one step is not valid as it is
        indicative of a build request with an incorrect configuration.
        Structure is documented below.
    waitFor : [str], default is Undefined, optional
        The ID(s) of the step(s) that this build step depends on.
        This build step will not start until all the build steps in wait_for
        have completed successfully. If wait_for is empty, this build step
        will start when all previous build steps in the Build.Steps list
        have completed successfully.
    """


    allowExitCodes?: [float]

    allowFailure?: bool

    args?: [str]

    dir?: str

    entrypoint?: str

    env?: [str]

    id?: str

    name?: str

    script?: str

    secretEnv?: [str]

    timeout?: str

    timing?: str

    volumes?: [CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuildStepItems0VolumesItems0]

    waitFor?: [str]


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderBuildStepItems0VolumesItems0:
    r"""
    cloudbuild gcpm upbound io v1beta1 trigger spec for provider build step items0 volumes items0

    Attributes
    ----------
    name : str, default is Undefined, optional
        Name of the volume to mount.
        Volume names must be unique per build step and must be valid names for
        Docker volumes. Each named volume must be used by at least two build steps.
    path : str, default is Undefined, optional
        Path at which to mount the volume.
        Paths must be absolute and cannot conflict with other volume paths on
        the same build step or with certain reserved volume paths.
    """


    name?: str

    path?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderGitFileSource:
    r"""
    The file source describing the local or remote Build template.
    Structure is documented below.

    Attributes
    ----------
    bitbucketServerConfig : str, default is Undefined, optional
        The full resource name of the bitbucket server config.
        Format: projects/{project}/locations/{location}/bitbucketServerConfigs/{id}.
    githubEnterpriseConfig : str, default is Undefined, optional
        The full resource name of the github enterprise config.
        Format: projects/{project}/locations/{location}/githubEnterpriseConfigs/{id}. projects/{project}/githubEnterpriseConfigs/{id}.
    path : str, default is Undefined, optional
        The path of the file, with the repo root as the root of the path.
    repoType : str, default is Undefined, optional
        The type of the repo, since it may not be explicit from the repo field (e.g from a URL).
        Values can be UNKNOWN, CLOUD_SOURCE_REPOSITORIES, GITHUB, BITBUCKET_SERVER
        Possible values are: UNKNOWN, CLOUD_SOURCE_REPOSITORIES, GITHUB, BITBUCKET_SERVER.
    repository : str, default is Undefined, optional
        The fully qualified resource name of the Repo API repository. The fully qualified resource name of the Repo API repository.
        If unspecified, the repo from which the trigger invocation originated is assumed to be the repo from which to read the specified path.
    revision : str, default is Undefined, optional
        The branch, tag, arbitrary ref, or SHA version of the repo to use when resolving the
        filename . This field respects the same syntax/resolution as described here: https://git-scm.com/docs/gitrevisions
        If unspecified, the revision from which the trigger invocation originated is assumed to be the revision from which to read the specified path.
    uri : str, default is Undefined, optional
        The URI of the repo . If unspecified, the repo from which the trigger
        invocation originated is assumed to be the repo from which to read the specified path.
    """


    bitbucketServerConfig?: str

    githubEnterpriseConfig?: str

    path?: str

    repoType?: str

    repository?: str

    revision?: str

    uri?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderGithub:
    r"""
    Describes the configuration of a trigger that creates a build whenever a GitHub event is received.
    One of trigger_template, github, pubsub_config or webhook_config must be provided.
    Structure is documented below.

    Attributes
    ----------
    enterpriseConfigResourceName : str, default is Undefined, optional
        The resource name of the github enterprise config that should be applied to this installation.
        For example: "projects/{$projectId}/locations/{$locationId}/githubEnterpriseConfigs/{$configId}"
    name : str, default is Undefined, optional
        Name of the repository. For example: The name for
        https://github.com/googlecloudplatform/cloud-builders is "cloud-builders".
    owner : str, default is Undefined, optional
        Owner of the repository. For example: The owner for
        https://github.com/googlecloudplatform/cloud-builders is "googlecloudplatform".
    pullRequest : CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderGithubPullRequest, default is Undefined, optional
        pull request
    push : CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderGithubPush, default is Undefined, optional
        push
    """


    enterpriseConfigResourceName?: str

    name?: str

    owner?: str

    pullRequest?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderGithubPullRequest

    push?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderGithubPush


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderGithubPullRequest:
    r"""
    filter to match changes in pull requests. Specify only one of pull_request or push.
    Structure is documented below.

    Attributes
    ----------
    branch : str, default is Undefined, optional
        Regex of branches to match.
        The syntax of the regular expressions accepted is the syntax accepted by
        RE2 and described at https://github.com/google/re2/wiki/Syntax
    commentControl : str, default is Undefined, optional
        Configure builds to run whether a repository owner or collaborator need to comment /gcbrun.
        Possible values are: COMMENTS_DISABLED, COMMENTS_ENABLED, COMMENTS_ENABLED_FOR_EXTERNAL_CONTRIBUTORS_ONLY.
    invertRegex : bool, default is Undefined, optional
        Only trigger a build if the revision regex does NOT match the revision regex.
    """


    branch?: str

    commentControl?: str

    invertRegex?: bool


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderGithubPush:
    r"""
    filter to match changes in refs, like branches or tags. Specify only one of pull_request or push.
    Structure is documented below.

    Attributes
    ----------
    branch : str, default is Undefined, optional
        Regex of branches to match.
        The syntax of the regular expressions accepted is the syntax accepted by
        RE2 and described at https://github.com/google/re2/wiki/Syntax
    invertRegex : bool, default is Undefined, optional
        Only trigger a build if the revision regex does NOT match the revision regex.
    tag : str, default is Undefined, optional
        Regex of tags to match.
        The syntax of the regular expressions accepted is the syntax accepted by
        RE2 and described at https://github.com/google/re2/wiki/Syntax
    """


    branch?: str

    invertRegex?: bool

    tag?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderPubsubConfig:
    r"""
    PubsubConfig describes the configuration of a trigger that creates
    a build whenever a Pub/Sub message is published.
    One of trigger_template, github, pubsub_config webhook_config or source_to_build must be provided.
    Structure is documented below.

    Attributes
    ----------
    serviceAccountEmail : str, default is Undefined, optional
        Service account that will make the push request.
    topic : str, default is Undefined, optional
        The name of the topic from which this subscription is receiving messages.
    topicRef : CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderPubsubConfigTopicRef, default is Undefined, optional
        topic ref
    topicSelector : CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderPubsubConfigTopicSelector, default is Undefined, optional
        topic selector
    """


    serviceAccountEmail?: str

    topic?: str

    topicRef?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderPubsubConfigTopicRef

    topicSelector?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderPubsubConfigTopicSelector


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderPubsubConfigTopicRef:
    r"""
    Reference to a Topic in pubsub to populate topic.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    namespace : str, default is Undefined, optional
        Namespace of the referenced object
    policy : CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderPubsubConfigTopicRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    namespace?: str

    policy?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderPubsubConfigTopicRefPolicy


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderPubsubConfigTopicRefPolicy:
    r"""
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderPubsubConfigTopicSelector:
    r"""
    Selector for a Topic in pubsub to populate topic.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    namespace : str, default is Undefined, optional
        Namespace for the selector
    policy : CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderPubsubConfigTopicSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    namespace?: str

    policy?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderPubsubConfigTopicSelectorPolicy


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderPubsubConfigTopicSelectorPolicy:
    r"""
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderRepositoryEventConfig:
    r"""
    The configuration of a trigger that creates a build whenever an event from Repo API is received.
    Structure is documented below.

    Attributes
    ----------
    pullRequest : CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderRepositoryEventConfigPullRequest, default is Undefined, optional
        pull request
    push : CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderRepositoryEventConfigPush, default is Undefined, optional
        push
    repository : str, default is Undefined, optional
        The resource name of the Repo API resource.
    """


    pullRequest?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderRepositoryEventConfigPullRequest

    push?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderRepositoryEventConfigPush

    repository?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderRepositoryEventConfigPullRequest:
    r"""
    Contains filter properties for matching Pull Requests.
    Structure is documented below.

    Attributes
    ----------
    branch : str, default is Undefined, optional
        Regex of branches to match.
        The syntax of the regular expressions accepted is the syntax accepted by
        RE2 and described at https://github.com/google/re2/wiki/Syntax
    commentControl : str, default is Undefined, optional
        Configure builds to run whether a repository owner or collaborator need to comment /gcbrun.
        Possible values are: COMMENTS_DISABLED, COMMENTS_ENABLED, COMMENTS_ENABLED_FOR_EXTERNAL_CONTRIBUTORS_ONLY.
    invertRegex : bool, default is Undefined, optional
        Only trigger a build if the revision regex does NOT match the revision regex.
    """


    branch?: str

    commentControl?: str

    invertRegex?: bool


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderRepositoryEventConfigPush:
    r"""
    Contains filter properties for matching git pushes.
    Structure is documented below.

    Attributes
    ----------
    branch : str, default is Undefined, optional
        Regex of branches to match.
        The syntax of the regular expressions accepted is the syntax accepted by
        RE2 and described at https://github.com/google/re2/wiki/Syntax
    invertRegex : bool, default is Undefined, optional
        Only trigger a build if the revision regex does NOT match the revision regex.
    tag : str, default is Undefined, optional
        Regex of tags to match.
        The syntax of the regular expressions accepted is the syntax accepted by
        RE2 and described at https://github.com/google/re2/wiki/Syntax
    """


    branch?: str

    invertRegex?: bool

    tag?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderServiceAccountRef:
    r"""
    Reference to a ServiceAccount in cloudplatform to populate serviceAccount.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    namespace : str, default is Undefined, optional
        Namespace of the referenced object
    policy : CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderServiceAccountRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    namespace?: str

    policy?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderServiceAccountRefPolicy


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderServiceAccountRefPolicy:
    r"""
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderServiceAccountSelector:
    r"""
    Selector for a ServiceAccount in cloudplatform to populate serviceAccount.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    namespace : str, default is Undefined, optional
        Namespace for the selector
    policy : CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderServiceAccountSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    namespace?: str

    policy?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderServiceAccountSelectorPolicy


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderServiceAccountSelectorPolicy:
    r"""
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderSourceToBuild:
    r"""
    The repo and ref of the repository from which to build.
    This field is used only for those triggers that do not respond to SCM events.
    Triggers that respond to such events build source at whatever commit caused the event.
    This field is currently only used by Webhook, Pub/Sub, Manual, and Cron triggers.
    One of trigger_template, github, pubsub_config webhook_config or source_to_build must be provided.
    Structure is documented below.

    Attributes
    ----------
    bitbucketServerConfig : str, default is Undefined, optional
        The full resource name of the bitbucket server config.
        Format: projects/{project}/locations/{location}/bitbucketServerConfigs/{id}.
    githubEnterpriseConfig : str, default is Undefined, optional
        The full resource name of the github enterprise config.
        Format: projects/{project}/locations/{location}/githubEnterpriseConfigs/{id}. projects/{project}/githubEnterpriseConfigs/{id}.
    ref : str, default is Undefined, optional
        The branch or tag to use. Must start with "refs/" .
    repoType : str, default is Undefined, optional
        The type of the repo, since it may not be explicit from the repo field (e.g from a URL).
        Values can be UNKNOWN, CLOUD_SOURCE_REPOSITORIES, GITHUB, BITBUCKET_SERVER
        Possible values are: UNKNOWN, CLOUD_SOURCE_REPOSITORIES, GITHUB, BITBUCKET_SERVER.
    repository : str, default is Undefined, optional
        The qualified resource name of the Repo API repository.
        Either uri or repository can be specified and is required.
    uri : str, default is Undefined, optional
        The URI of the repo.
    """


    bitbucketServerConfig?: str

    githubEnterpriseConfig?: str

    ref?: str

    repoType?: str

    repository?: str

    uri?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderTriggerTemplate:
    r"""
    Template describing the types of source changes to trigger a build.
    Branch and tag names in trigger templates are interpreted as regular
    expressions. Any branch or tag change that matches that regular
    expression will trigger a build.
    One of trigger_template, github, pubsub_config, webhook_config or source_to_build must be provided.
    Structure is documented below.

    Attributes
    ----------
    branchName : str, default is Undefined, optional
        Name of the branch to build. Exactly one a of branch name, tag, or commit SHA must be provided.
        This field is a regular expression.
    commitSha : str, default is Undefined, optional
        Explicit commit SHA to build. Exactly one of a branch name, tag, or commit SHA must be provided.
    dir : str, default is Undefined, optional
        Directory, relative to the source root, in which to run the build.
        This must be a relative path. If a step's dir is specified and
        is an absolute path, this value is ignored for that step's
        execution.
    invertRegex : bool, default is Undefined, optional
        Only trigger a build if the revision regex does NOT match the revision regex.
    projectId : str, default is Undefined, optional
        ID of the project that owns the Cloud Source Repository. If
        omitted, the project ID requesting the build is assumed.
    repoName : str, default is Undefined, optional
        Name of the Cloud Source Repository. If omitted, the name "default" is assumed.
    tagName : str, default is Undefined, optional
        Name of the tag to build. Exactly one of a branch name, tag, or commit SHA must be provided.
        This field is a regular expression.
    """


    branchName?: str

    commitSha?: str

    dir?: str

    invertRegex?: bool

    projectId?: str

    repoName?: str

    tagName?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderWebhookConfig:
    r"""
    WebhookConfig describes the configuration of a trigger that creates
    a build whenever a webhook is sent to a trigger's webhook URL.
    One of trigger_template, github, pubsub_config webhook_config or source_to_build must be provided.
    Structure is documented below.

    Attributes
    ----------
    secret : str, default is Undefined, optional
        Resource name for the secret required as a URL parameter.
    secretRef : CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderWebhookConfigSecretRef, default is Undefined, optional
        secret ref
    secretSelector : CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderWebhookConfigSecretSelector, default is Undefined, optional
        secret selector
    """


    secret?: str

    secretRef?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderWebhookConfigSecretRef

    secretSelector?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderWebhookConfigSecretSelector


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderWebhookConfigSecretRef:
    r"""
    Reference to a SecretVersion in secretmanager to populate secret.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    namespace : str, default is Undefined, optional
        Namespace of the referenced object
    policy : CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderWebhookConfigSecretRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    namespace?: str

    policy?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderWebhookConfigSecretRefPolicy


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderWebhookConfigSecretRefPolicy:
    r"""
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderWebhookConfigSecretSelector:
    r"""
    Selector for a SecretVersion in secretmanager to populate secret.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    namespace : str, default is Undefined, optional
        Namespace for the selector
    policy : CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderWebhookConfigSecretSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    namespace?: str

    policy?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderWebhookConfigSecretSelectorPolicy


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecForProviderWebhookConfigSecretSelectorPolicy:
    r"""
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProvider:
    r"""
    THIS IS A BETA FIELD. It will be honored
    unless the Management Policies feature flag is disabled.
    InitProvider holds the same fields as ForProvider, with the exception
    of Identifier and other resource reference fields. The fields that are
    in InitProvider are merged into ForProvider when the resource is created.
    The same fields are also added to the terraform ignore_changes hook, to
    avoid updating them after creation. This is useful for fields that are
    required on creation, but we do not desire to update them after creation,
    for example because of an external controller is managing them, like an
    autoscaler.

    Attributes
    ----------
    approvalConfig : CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderApprovalConfig, default is Undefined, optional
        approval config
    bitbucketServerTriggerConfig : CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBitbucketServerTriggerConfig, default is Undefined, optional
        bitbucket server trigger config
    build : CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuild, default is Undefined, optional
        build
    description : str, default is Undefined, optional
        Human-readable description of the trigger.
    disabled : bool, default is Undefined, optional
        Whether the trigger is disabled or not. If true, the trigger will never result in a build.
    filename : str, default is Undefined, optional
        Path, from the source root, to a file whose contents is used for the template.
        Either a filename or build template must be provided. Set this only when using trigger_template or github.
        When using Pub/Sub, Webhook or Manual set the file name using git_file_source instead.
    $filter : str, default is Undefined, optional
        A Common Expression Language string. Used only with Pub/Sub and Webhook.
    gitFileSource : CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderGitFileSource, default is Undefined, optional
        git file source
    github : CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderGithub, default is Undefined, optional
        github
    ignoredFiles : [str], default is Undefined, optional
        ignoredFiles and includedFiles are file glob matches using https://golang.org/pkg/path/filepath/#Match
        extended with support for **.
        If ignoredFiles and changed files are both empty, then they are not
        used to determine whether or not to trigger a build.
        If ignoredFiles is not empty, then we ignore any files that match any
        of the ignored_file globs. If the change has no files that are outside
        of the ignoredFiles globs, then we do not trigger a build.
    includeBuildLogs : str, default is Undefined, optional
        Build logs will be sent back to GitHub as part of the checkrun
        result.  Values can be INCLUDE_BUILD_LOGS_UNSPECIFIED or
        INCLUDE_BUILD_LOGS_WITH_STATUS
        Possible values are: INCLUDE_BUILD_LOGS_UNSPECIFIED, INCLUDE_BUILD_LOGS_WITH_STATUS.
    includedFiles : [str], default is Undefined, optional
        ignoredFiles and includedFiles are file glob matches using https://golang.org/pkg/path/filepath/#Match
        extended with support for **.
        If any of the files altered in the commit pass the ignoredFiles filter
        and includedFiles is empty, then as far as this filter is concerned, we
        should trigger the build.
        If any of the files altered in the commit pass the ignoredFiles filter
        and includedFiles is not empty, then we make sure that at least one of
        those files matches a includedFiles glob. If not, then we do not trigger
        a build.
    location : str, default is Undefined, optional
        The Cloud Build location for the trigger.
        If not specified, "global" is used.
    name : str, default is Undefined, optional
        Name of the trigger. Must be unique within the project.
    project : str, default is Undefined, optional
        The ID of the project in which the resource belongs.
        If it is not provided, the provider project is used.
    pubsubConfig : CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderPubsubConfig, default is Undefined, optional
        pubsub config
    repositoryEventConfig : CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderRepositoryEventConfig, default is Undefined, optional
        repository event config
    serviceAccount : str, default is Undefined, optional
        The service account used for all user-controlled operations including
        triggers.patch, triggers.run, builds.create, and builds.cancel.
        If no service account is set, then the standard Cloud Build service account
        ([PROJECT_NUM]@system.gserviceaccount.com) will be used instead.
        Format: projects/{PROJECT_ID}/serviceAccounts/{ACCOUNT_ID_OR_EMAIL}
    serviceAccountRef : CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderServiceAccountRef, default is Undefined, optional
        service account ref
    serviceAccountSelector : CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderServiceAccountSelector, default is Undefined, optional
        service account selector
    sourceToBuild : CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderSourceToBuild, default is Undefined, optional
        source to build
    substitutions : {str:str}, default is Undefined, optional
        Substitutions data for Build resource.
    tags : [str], default is Undefined, optional
        Tags for annotation of a BuildTrigger
    triggerTemplate : CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderTriggerTemplate, default is Undefined, optional
        trigger template
    webhookConfig : CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderWebhookConfig, default is Undefined, optional
        webhook config
    """


    approvalConfig?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderApprovalConfig

    bitbucketServerTriggerConfig?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBitbucketServerTriggerConfig

    build?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuild

    description?: str

    disabled?: bool

    filename?: str

    $filter?: str

    gitFileSource?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderGitFileSource

    github?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderGithub

    ignoredFiles?: [str]

    includeBuildLogs?: str

    includedFiles?: [str]

    location?: str

    name?: str

    project?: str

    pubsubConfig?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderPubsubConfig

    repositoryEventConfig?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderRepositoryEventConfig

    serviceAccount?: str

    serviceAccountRef?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderServiceAccountRef

    serviceAccountSelector?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderServiceAccountSelector

    sourceToBuild?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderSourceToBuild

    substitutions?: {str:str}

    tags?: [str]

    triggerTemplate?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderTriggerTemplate

    webhookConfig?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderWebhookConfig


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderApprovalConfig:
    r"""
    Configuration for manual approval to start a build invocation of this BuildTrigger.
    Builds created by this trigger will require approval before they execute.
    Any user with a Cloud Build Approver role for the project can approve a build.
    Structure is documented below.

    Attributes
    ----------
    approvalRequired : bool, default is Undefined, optional
        Whether or not approval is needed. If this is set on a build, it will become pending when run,
        and will need to be explicitly approved to start.
    """


    approvalRequired?: bool


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBitbucketServerTriggerConfig:
    r"""
    BitbucketServerTriggerConfig describes the configuration of a trigger that creates a build whenever a Bitbucket Server event is received.
    Structure is documented below.

    Attributes
    ----------
    bitbucketServerConfigResource : str, default is Undefined, optional
        The Bitbucket server config resource that this trigger config maps to.
    projectKey : str, default is Undefined, optional
        Key of the project that the repo is in. For example: The key for https://mybitbucket.server/projects/TEST/repos/test-repo is "TEST".
    pullRequest : CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBitbucketServerTriggerConfigPullRequest, default is Undefined, optional
        pull request
    push : CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBitbucketServerTriggerConfigPush, default is Undefined, optional
        push
    repoSlug : str, default is Undefined, optional
        Slug of the repository. A repository slug is a URL-friendly version of a repository name, automatically generated by Bitbucket for use in the URL.
        For example, if the repository name is 'test repo', in the URL it would become 'test-repo' as in https://mybitbucket.server/projects/TEST/repos/test-repo.
    """


    bitbucketServerConfigResource?: str

    projectKey?: str

    pullRequest?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBitbucketServerTriggerConfigPullRequest

    push?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBitbucketServerTriggerConfigPush

    repoSlug?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBitbucketServerTriggerConfigPullRequest:
    r"""
    Filter to match changes in pull requests.
    Structure is documented below.

    Attributes
    ----------
    branch : str, default is Undefined, optional
        Regex of branches to match.
        The syntax of the regular expressions accepted is the syntax accepted by
        RE2 and described at https://github.com/google/re2/wiki/Syntax
    commentControl : str, default is Undefined, optional
        Configure builds to run whether a repository owner or collaborator need to comment /gcbrun.
        Possible values are: COMMENTS_DISABLED, COMMENTS_ENABLED, COMMENTS_ENABLED_FOR_EXTERNAL_CONTRIBUTORS_ONLY.
    invertRegex : bool, default is Undefined, optional
        Only trigger a build if the revision regex does NOT match the revision regex.
    """


    branch?: str

    commentControl?: str

    invertRegex?: bool


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBitbucketServerTriggerConfigPush:
    r"""
    Filter to match changes in refs like branches, tags.
    Structure is documented below.

    Attributes
    ----------
    branch : str, default is Undefined, optional
        Regex of branches to match.
        The syntax of the regular expressions accepted is the syntax accepted by
        RE2 and described at https://github.com/google/re2/wiki/Syntax
    invertRegex : bool, default is Undefined, optional
        Only trigger a build if the revision regex does NOT match the revision regex.
    tag : str, default is Undefined, optional
        Regex of tags to match.
        The syntax of the regular expressions accepted is the syntax accepted by
        RE2 and described at https://github.com/google/re2/wiki/Syntax
    """


    branch?: str

    invertRegex?: bool

    tag?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuild:
    r"""
    Contents of the build template. Either a filename or build template must be provided.
    Structure is documented below.

    Attributes
    ----------
    artifacts : CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuildArtifacts, default is Undefined, optional
        artifacts
    availableSecrets : CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuildAvailableSecrets, default is Undefined, optional
        available secrets
    images : [str], default is Undefined, optional
        A list of images to be pushed upon the successful completion of all build steps.
        The images are pushed using the builder service account's credentials.
        The digests of the pushed images will be stored in the Build resource's results field.
        If any of the images fail to be pushed, the build status is marked FAILURE.
    logsBucket : str, default is Undefined, optional
        Google Cloud Storage bucket where logs should be written.
        Logs file names will be of the format ${logsBucket}/log-${build_id}.txt.
    options : CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuildOptions, default is Undefined, optional
        options
    queueTtl : str, default is Undefined, optional
        TTL in queue for this build. If provided and the build is enqueued longer than this value,
        the build will expire and the build status will be EXPIRED.
        The TTL starts ticking from createTime.
        A duration in seconds with up to nine fractional digits, terminated by 's'. Example: "3.5s".
    secret : [CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuildSecretItems0], default is Undefined, optional
        Secrets to decrypt using Cloud Key Management Service.
        Structure is documented below.
    source : CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuildSource, default is Undefined, optional
        source
    step : [CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuildStepItems0], default is Undefined, optional
        The operations to be performed on the workspace.
        Structure is documented below.
    substitutions : {str:str}, default is Undefined, optional
        Substitutions data for Build resource.
    tags : [str], default is Undefined, optional
        Tags for annotation of a Build. These are not docker tags.
    timeout : str, default is Undefined, optional
        Amount of time that this build should be allowed to run, to second granularity.
        If this amount of time elapses, work on the build will cease and the build status will be TIMEOUT.
        This timeout must be equal to or greater than the sum of the timeouts for build steps within the build.
        The expected format is the number of seconds followed by s.
        Default time is ten minutes (600s).
    """


    artifacts?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuildArtifacts

    availableSecrets?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuildAvailableSecrets

    images?: [str]

    logsBucket?: str

    options?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuildOptions

    queueTtl?: str

    secret?: [CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuildSecretItems0]

    source?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuildSource

    step?: [CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuildStepItems0]

    substitutions?: {str:str}

    tags?: [str]

    timeout?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuildArtifacts:
    r"""
    Artifacts produced by the build that should be uploaded upon successful completion of all build steps.
    Structure is documented below.

    Attributes
    ----------
    images : [str], default is Undefined, optional
        A list of images to be pushed upon the successful completion of all build steps.
        The images are pushed using the builder service account's credentials.
        The digests of the pushed images will be stored in the Build resource's results field.
        If any of the images fail to be pushed, the build status is marked FAILURE.
    mavenArtifacts : [CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuildArtifactsMavenArtifactsItems0], default is Undefined, optional
        A Maven artifact to upload to Artifact Registry upon successful completion of all build steps.
        The location and generation of the uploaded objects will be stored in the Build resource's results field.
        If any objects fail to be pushed, the build is marked FAILURE.
        Structure is documented below.
    npmPackages : [CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuildArtifactsNpmPackagesItems0], default is Undefined, optional
        Npm package to upload to Artifact Registry upon successful completion of all build steps.
        The location and generation of the uploaded objects will be stored in the Build resource's results field.
        If any objects fail to be pushed, the build is marked FAILURE.
        Structure is documented below.
    objects : CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuildArtifactsObjects, default is Undefined, optional
        objects
    pythonPackages : [CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuildArtifactsPythonPackagesItems0], default is Undefined, optional
        Python package to upload to Artifact Registry upon successful completion of all build steps. A package can encapsulate multiple objects to be uploaded to a single repository.
        The location and generation of the uploaded objects will be stored in the Build resource's results field.
        If any objects fail to be pushed, the build is marked FAILURE.
        Structure is documented below.
    """


    images?: [str]

    mavenArtifacts?: [CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuildArtifactsMavenArtifactsItems0]

    npmPackages?: [CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuildArtifactsNpmPackagesItems0]

    objects?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuildArtifactsObjects

    pythonPackages?: [CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuildArtifactsPythonPackagesItems0]


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuildArtifactsMavenArtifactsItems0:
    r"""
    cloudbuild gcpm upbound io v1beta1 trigger spec init provider build artifacts maven artifacts items0

    Attributes
    ----------
    artifactId : str, default is Undefined, optional
        Maven artifactId value used when uploading the artifact to Artifact Registry.
    groupId : str, default is Undefined, optional
        Maven groupId value used when uploading the artifact to Artifact Registry.
    path : str, default is Undefined, optional
        Path at which to mount the volume.
        Paths must be absolute and cannot conflict with other volume paths on
        the same build step or with certain reserved volume paths.
    repository : str, default is Undefined, optional
        The qualified resource name of the Repo API repository.
        Either uri or repository can be specified and is required.
    version : str, default is Undefined, optional
        Maven version value used when uploading the artifact to Artifact Registry.
    """


    artifactId?: str

    groupId?: str

    path?: str

    repository?: str

    version?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuildArtifactsNpmPackagesItems0:
    r"""
    cloudbuild gcpm upbound io v1beta1 trigger spec init provider build artifacts npm packages items0

    Attributes
    ----------
    packagePath : str, default is Undefined, optional
        Path to the package.json. e.g. workspace/path/to/package
    repository : str, default is Undefined, optional
        The qualified resource name of the Repo API repository.
        Either uri or repository can be specified and is required.
    """


    packagePath?: str

    repository?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuildArtifactsObjects:
    r"""
    A list of objects to be uploaded to Cloud Storage upon successful completion of all build steps.
    Files in the workspace matching specified paths globs will be uploaded to the
    Cloud Storage location using the builder service account's credentials.
    The location and generation of the uploaded objects will be stored in the Build resource's results field.
    If any objects fail to be pushed, the build is marked FAILURE.
    Structure is documented below.

    Attributes
    ----------
    location : str, default is Undefined, optional
        Cloud Storage bucket and optional object path, in the form "gs://bucket/path/to/somewhere/".
        Files in the workspace matching any path pattern will be uploaded to Cloud Storage with
        this location as a prefix.
    paths : [str], default is Undefined, optional
        Path globs used to match files in the build's workspace. For Python/ Twine, this is usually dist/*, and sometimes additionally an .asc file.
    """


    location?: str

    paths?: [str]


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuildArtifactsPythonPackagesItems0:
    r"""
    cloudbuild gcpm upbound io v1beta1 trigger spec init provider build artifacts python packages items0

    Attributes
    ----------
    paths : [str], default is Undefined, optional
        Path globs used to match files in the build's workspace. For Python/ Twine, this is usually dist/*, and sometimes additionally an .asc file.
    repository : str, default is Undefined, optional
        The qualified resource name of the Repo API repository.
        Either uri or repository can be specified and is required.
    """


    paths?: [str]

    repository?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuildAvailableSecrets:
    r"""
    Secrets and secret environment variables.
    Structure is documented below.

    Attributes
    ----------
    secretManager : [CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuildAvailableSecretsSecretManagerItems0], default is Undefined, optional
        Pairs a secret environment variable with a SecretVersion in Secret Manager.
        Structure is documented below.
    """


    secretManager?: [CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuildAvailableSecretsSecretManagerItems0]


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuildAvailableSecretsSecretManagerItems0:
    r"""
    cloudbuild gcpm upbound io v1beta1 trigger spec init provider build available secrets secret manager items0

    Attributes
    ----------
    env : str, default is Undefined, optional
        A list of environment variable definitions to be used when
        running a step.
        The elements are of the form "KEY=VALUE" for the environment variable
        "KEY" being given the value "VALUE".
    versionName : str, default is Undefined, optional
        Resource name of the SecretVersion. In format: projects//secrets//versions/*
    """


    env?: str

    versionName?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuildOptions:
    r"""
    Special options for this build.
    Structure is documented below.

    Attributes
    ----------
    diskSizeGb : float, default is Undefined, optional
        Requested disk size for the VM that runs the build. Note that this is NOT "disk free";
        some of the space will be used by the operating system and build utilities.
        Also note that this is the minimum disk size that will be allocated for the build --
        the build may run with a larger disk than requested. At present, the maximum disk size
        is 1000GB; builds that request more than the maximum are rejected with an error.
    dynamicSubstitutions : bool, default is Undefined, optional
        Option to specify whether or not to apply bash style string operations to the substitutions.
        NOTE this is always enabled for triggered builds and cannot be overridden in the build configuration file.
    env : [str], default is Undefined, optional
        A list of environment variable definitions to be used when
        running a step.
        The elements are of the form "KEY=VALUE" for the environment variable
        "KEY" being given the value "VALUE".
    logStreamingOption : str, default is Undefined, optional
        Option to define build log streaming behavior to Google Cloud Storage.
        Possible values are: STREAM_DEFAULT, STREAM_ON, STREAM_OFF.
    logging : str, default is Undefined, optional
        Option to specify the logging mode, which determines if and where build logs are stored.
        Possible values are: LOGGING_UNSPECIFIED, LEGACY, GCS_ONLY, STACKDRIVER_ONLY, CLOUD_LOGGING_ONLY, NONE.
    machineType : str, default is Undefined, optional
        Compute Engine machine type on which to run the build.
    requestedVerifyOption : str, default is Undefined, optional
        Requested verifiability options.
        Possible values are: NOT_VERIFIED, VERIFIED.
    secretEnv : [str], default is Undefined, optional
        A list of environment variables which are encrypted using
        a Cloud Key
        Management Service crypto key. These values must be specified in
        the build's Secret.
    sourceProvenanceHash : [str], default is Undefined, optional
        Requested hash for SourceProvenance.
        Each value may be one of: NONE, SHA256, MD5.
    substitutionOption : str, default is Undefined, optional
        Option to specify behavior when there is an error in the substitution checks.
        NOTE this is always set to ALLOW_LOOSE for triggered builds and cannot be overridden
        in the build configuration file.
        Possible values are: MUST_MATCH, ALLOW_LOOSE.
    volumes : [CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuildOptionsVolumesItems0], default is Undefined, optional
        List of volumes to mount into the build step.
        Each volume is created as an empty volume prior to execution of the
        build step. Upon completion of the build, volumes and their contents
        are discarded.
        Using a named volume in only one step is not valid as it is
        indicative of a build request with an incorrect configuration.
        Structure is documented below.
    workerPool : str, default is Undefined, optional
        Option to specify a WorkerPool for the build. Format projects/{project}/workerPools/{workerPool}
        This field is experimental.
    """


    diskSizeGb?: float

    dynamicSubstitutions?: bool

    env?: [str]

    logStreamingOption?: str

    logging?: str

    machineType?: str

    requestedVerifyOption?: str

    secretEnv?: [str]

    sourceProvenanceHash?: [str]

    substitutionOption?: str

    volumes?: [CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuildOptionsVolumesItems0]

    workerPool?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuildOptionsVolumesItems0:
    r"""
    cloudbuild gcpm upbound io v1beta1 trigger spec init provider build options volumes items0

    Attributes
    ----------
    name : str, default is Undefined, optional
        Name of the volume to mount.
        Volume names must be unique per build step and must be valid names for
        Docker volumes. Each named volume must be used by at least two build steps.
    path : str, default is Undefined, optional
        Path at which to mount the volume.
        Paths must be absolute and cannot conflict with other volume paths on
        the same build step or with certain reserved volume paths.
    """


    name?: str

    path?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuildSecretItems0:
    r"""
    cloudbuild gcpm upbound io v1beta1 trigger spec init provider build secret items0

    Attributes
    ----------
    kmsKeyName : str, default is Undefined, optional
        Cloud KMS key name to use to decrypt these envs.
    secretEnv : {str:str}, default is Undefined, optional
        A list of environment variables which are encrypted using
        a Cloud Key
        Management Service crypto key. These values must be specified in
        the build's Secret.
    """


    kmsKeyName?: str

    secretEnv?: {str:str}


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuildSource:
    r"""
    The location of the source files to build.
    One of storageSource or repoSource must be provided.
    Structure is documented below.

    Attributes
    ----------
    repoSource : CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuildSourceRepoSource, default is Undefined, optional
        repo source
    storageSource : CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuildSourceStorageSource, default is Undefined, optional
        storage source
    """


    repoSource?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuildSourceRepoSource

    storageSource?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuildSourceStorageSource


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuildSourceRepoSource:
    r"""
    Location of the source in a Google Cloud Source Repository.
    Structure is documented below.

    Attributes
    ----------
    branchName : str, default is Undefined, optional
        Name of the branch to build. Exactly one a of branch name, tag, or commit SHA must be provided.
        This field is a regular expression.
    commitSha : str, default is Undefined, optional
        Explicit commit SHA to build. Exactly one of a branch name, tag, or commit SHA must be provided.
    dir : str, default is Undefined, optional
        Directory, relative to the source root, in which to run the build.
        This must be a relative path. If a step's dir is specified and
        is an absolute path, this value is ignored for that step's
        execution.
    invertRegex : bool, default is Undefined, optional
        Only trigger a build if the revision regex does NOT match the revision regex.
    projectId : str, default is Undefined, optional
        ID of the project that owns the Cloud Source Repository. If
        omitted, the project ID requesting the build is assumed.
    repoName : str, default is Undefined, optional
        Name of the Cloud Source Repository. If omitted, the name "default" is assumed.
    substitutions : {str:str}, default is Undefined, optional
        Substitutions data for Build resource.
    tagName : str, default is Undefined, optional
        Name of the tag to build. Exactly one of a branch name, tag, or commit SHA must be provided.
        This field is a regular expression.
    """


    branchName?: str

    commitSha?: str

    dir?: str

    invertRegex?: bool

    projectId?: str

    repoName?: str

    substitutions?: {str:str}

    tagName?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuildSourceStorageSource:
    r"""
    Location of the source in an archive file in Google Cloud Storage.
    Structure is documented below.

    Attributes
    ----------
    bucket : str, default is Undefined, optional
        Google Cloud Storage bucket containing the source.
    generation : str, default is Undefined, optional
        Google Cloud Storage generation for the object.
        If the generation is omitted, the latest generation will be used
    object : str, default is Undefined, optional
        Google Cloud Storage object containing the source.
        This object must be a gzipped archive file (.tar.gz) containing source to build.
    """


    bucket?: str

    generation?: str

    object?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuildStepItems0:
    r"""
    cloudbuild gcpm upbound io v1beta1 trigger spec init provider build step items0

    Attributes
    ----------
    allowExitCodes : [float], default is Undefined, optional
        Allow this build step to fail without failing the entire build if and
        only if the exit code is one of the specified codes.
        If allowFailure is also specified, this field will take precedence.
    allowFailure : bool, default is Undefined, optional
        Allow this build step to fail without failing the entire build.
        If false, the entire build will fail if this step fails. Otherwise, the
        build will succeed, but this step will still have a failure status.
        Error information will be reported in the failureDetail field.
        allowExitCodes takes precedence over this field.
    args : [str], default is Undefined, optional
        A list of arguments that will be presented to the step when it is started.
        If the image used to run the step's container has an entrypoint, the args
        are used as arguments to that entrypoint. If the image does not define an
        entrypoint, the first element in args is used as the entrypoint, and the
        remainder will be used as arguments.
    dir : str, default is Undefined, optional
        Directory, relative to the source root, in which to run the build.
        This must be a relative path. If a step's dir is specified and
        is an absolute path, this value is ignored for that step's
        execution.
    entrypoint : str, default is Undefined, optional
        Entrypoint to be used instead of the build step image's
        default entrypoint.
        If unset, the image's default entrypoint is used
    env : [str], default is Undefined, optional
        A list of environment variable definitions to be used when
        running a step.
        The elements are of the form "KEY=VALUE" for the environment variable
        "KEY" being given the value "VALUE".
    id : str, default is Undefined, optional
        Unique identifier for this build step, used in wait_for to
        reference this build step as a dependency.
    name : str, default is Undefined, optional
        Name of the volume to mount.
        Volume names must be unique per build step and must be valid names for
        Docker volumes. Each named volume must be used by at least two build steps.
    script : str, default is Undefined, optional
        A shell script to be executed in the step.
        When script is provided, the user cannot specify the entrypoint or args.
    secretEnv : [str], default is Undefined, optional
        A list of environment variables which are encrypted using
        a Cloud Key
        Management Service crypto key. These values must be specified in
        the build's Secret.
    timeout : str, default is Undefined, optional
        Time limit for executing this build step. If not defined,
        the step has no
        time limit and will be allowed to continue to run until either it
        completes or the build itself times out.
    timing : str, default is Undefined, optional
        Output only. Stores timing information for executing this
        build step.
    volumes : [CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuildStepItems0VolumesItems0], default is Undefined, optional
        List of volumes to mount into the build step.
        Each volume is created as an empty volume prior to execution of the
        build step. Upon completion of the build, volumes and their contents
        are discarded.
        Using a named volume in only one step is not valid as it is
        indicative of a build request with an incorrect configuration.
        Structure is documented below.
    waitFor : [str], default is Undefined, optional
        The ID(s) of the step(s) that this build step depends on.
        This build step will not start until all the build steps in wait_for
        have completed successfully. If wait_for is empty, this build step
        will start when all previous build steps in the Build.Steps list
        have completed successfully.
    """


    allowExitCodes?: [float]

    allowFailure?: bool

    args?: [str]

    dir?: str

    entrypoint?: str

    env?: [str]

    id?: str

    name?: str

    script?: str

    secretEnv?: [str]

    timeout?: str

    timing?: str

    volumes?: [CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuildStepItems0VolumesItems0]

    waitFor?: [str]


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderBuildStepItems0VolumesItems0:
    r"""
    cloudbuild gcpm upbound io v1beta1 trigger spec init provider build step items0 volumes items0

    Attributes
    ----------
    name : str, default is Undefined, optional
        Name of the volume to mount.
        Volume names must be unique per build step and must be valid names for
        Docker volumes. Each named volume must be used by at least two build steps.
    path : str, default is Undefined, optional
        Path at which to mount the volume.
        Paths must be absolute and cannot conflict with other volume paths on
        the same build step or with certain reserved volume paths.
    """


    name?: str

    path?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderGitFileSource:
    r"""
    The file source describing the local or remote Build template.
    Structure is documented below.

    Attributes
    ----------
    bitbucketServerConfig : str, default is Undefined, optional
        The full resource name of the bitbucket server config.
        Format: projects/{project}/locations/{location}/bitbucketServerConfigs/{id}.
    githubEnterpriseConfig : str, default is Undefined, optional
        The full resource name of the github enterprise config.
        Format: projects/{project}/locations/{location}/githubEnterpriseConfigs/{id}. projects/{project}/githubEnterpriseConfigs/{id}.
    path : str, default is Undefined, optional
        The path of the file, with the repo root as the root of the path.
    repoType : str, default is Undefined, optional
        The type of the repo, since it may not be explicit from the repo field (e.g from a URL).
        Values can be UNKNOWN, CLOUD_SOURCE_REPOSITORIES, GITHUB, BITBUCKET_SERVER
        Possible values are: UNKNOWN, CLOUD_SOURCE_REPOSITORIES, GITHUB, BITBUCKET_SERVER.
    repository : str, default is Undefined, optional
        The fully qualified resource name of the Repo API repository. The fully qualified resource name of the Repo API repository.
        If unspecified, the repo from which the trigger invocation originated is assumed to be the repo from which to read the specified path.
    revision : str, default is Undefined, optional
        The branch, tag, arbitrary ref, or SHA version of the repo to use when resolving the
        filename . This field respects the same syntax/resolution as described here: https://git-scm.com/docs/gitrevisions
        If unspecified, the revision from which the trigger invocation originated is assumed to be the revision from which to read the specified path.
    uri : str, default is Undefined, optional
        The URI of the repo . If unspecified, the repo from which the trigger
        invocation originated is assumed to be the repo from which to read the specified path.
    """


    bitbucketServerConfig?: str

    githubEnterpriseConfig?: str

    path?: str

    repoType?: str

    repository?: str

    revision?: str

    uri?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderGithub:
    r"""
    Describes the configuration of a trigger that creates a build whenever a GitHub event is received.
    One of trigger_template, github, pubsub_config or webhook_config must be provided.
    Structure is documented below.

    Attributes
    ----------
    enterpriseConfigResourceName : str, default is Undefined, optional
        The resource name of the github enterprise config that should be applied to this installation.
        For example: "projects/{$projectId}/locations/{$locationId}/githubEnterpriseConfigs/{$configId}"
    name : str, default is Undefined, optional
        Name of the repository. For example: The name for
        https://github.com/googlecloudplatform/cloud-builders is "cloud-builders".
    owner : str, default is Undefined, optional
        Owner of the repository. For example: The owner for
        https://github.com/googlecloudplatform/cloud-builders is "googlecloudplatform".
    pullRequest : CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderGithubPullRequest, default is Undefined, optional
        pull request
    push : CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderGithubPush, default is Undefined, optional
        push
    """


    enterpriseConfigResourceName?: str

    name?: str

    owner?: str

    pullRequest?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderGithubPullRequest

    push?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderGithubPush


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderGithubPullRequest:
    r"""
    filter to match changes in pull requests. Specify only one of pull_request or push.
    Structure is documented below.

    Attributes
    ----------
    branch : str, default is Undefined, optional
        Regex of branches to match.
        The syntax of the regular expressions accepted is the syntax accepted by
        RE2 and described at https://github.com/google/re2/wiki/Syntax
    commentControl : str, default is Undefined, optional
        Configure builds to run whether a repository owner or collaborator need to comment /gcbrun.
        Possible values are: COMMENTS_DISABLED, COMMENTS_ENABLED, COMMENTS_ENABLED_FOR_EXTERNAL_CONTRIBUTORS_ONLY.
    invertRegex : bool, default is Undefined, optional
        Only trigger a build if the revision regex does NOT match the revision regex.
    """


    branch?: str

    commentControl?: str

    invertRegex?: bool


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderGithubPush:
    r"""
    filter to match changes in refs, like branches or tags. Specify only one of pull_request or push.
    Structure is documented below.

    Attributes
    ----------
    branch : str, default is Undefined, optional
        Regex of branches to match.
        The syntax of the regular expressions accepted is the syntax accepted by
        RE2 and described at https://github.com/google/re2/wiki/Syntax
    invertRegex : bool, default is Undefined, optional
        Only trigger a build if the revision regex does NOT match the revision regex.
    tag : str, default is Undefined, optional
        Regex of tags to match.
        The syntax of the regular expressions accepted is the syntax accepted by
        RE2 and described at https://github.com/google/re2/wiki/Syntax
    """


    branch?: str

    invertRegex?: bool

    tag?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderPubsubConfig:
    r"""
    PubsubConfig describes the configuration of a trigger that creates
    a build whenever a Pub/Sub message is published.
    One of trigger_template, github, pubsub_config webhook_config or source_to_build must be provided.
    Structure is documented below.

    Attributes
    ----------
    serviceAccountEmail : str, default is Undefined, optional
        Service account that will make the push request.
    topic : str, default is Undefined, optional
        The name of the topic from which this subscription is receiving messages.
    topicRef : CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderPubsubConfigTopicRef, default is Undefined, optional
        topic ref
    topicSelector : CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderPubsubConfigTopicSelector, default is Undefined, optional
        topic selector
    """


    serviceAccountEmail?: str

    topic?: str

    topicRef?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderPubsubConfigTopicRef

    topicSelector?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderPubsubConfigTopicSelector


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderPubsubConfigTopicRef:
    r"""
    Reference to a Topic in pubsub to populate topic.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    namespace : str, default is Undefined, optional
        Namespace of the referenced object
    policy : CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderPubsubConfigTopicRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    namespace?: str

    policy?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderPubsubConfigTopicRefPolicy


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderPubsubConfigTopicRefPolicy:
    r"""
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderPubsubConfigTopicSelector:
    r"""
    Selector for a Topic in pubsub to populate topic.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    namespace : str, default is Undefined, optional
        Namespace for the selector
    policy : CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderPubsubConfigTopicSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    namespace?: str

    policy?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderPubsubConfigTopicSelectorPolicy


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderPubsubConfigTopicSelectorPolicy:
    r"""
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderRepositoryEventConfig:
    r"""
    The configuration of a trigger that creates a build whenever an event from Repo API is received.
    Structure is documented below.

    Attributes
    ----------
    pullRequest : CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderRepositoryEventConfigPullRequest, default is Undefined, optional
        pull request
    push : CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderRepositoryEventConfigPush, default is Undefined, optional
        push
    repository : str, default is Undefined, optional
        The resource name of the Repo API resource.
    """


    pullRequest?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderRepositoryEventConfigPullRequest

    push?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderRepositoryEventConfigPush

    repository?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderRepositoryEventConfigPullRequest:
    r"""
    Contains filter properties for matching Pull Requests.
    Structure is documented below.

    Attributes
    ----------
    branch : str, default is Undefined, optional
        Regex of branches to match.
        The syntax of the regular expressions accepted is the syntax accepted by
        RE2 and described at https://github.com/google/re2/wiki/Syntax
    commentControl : str, default is Undefined, optional
        Configure builds to run whether a repository owner or collaborator need to comment /gcbrun.
        Possible values are: COMMENTS_DISABLED, COMMENTS_ENABLED, COMMENTS_ENABLED_FOR_EXTERNAL_CONTRIBUTORS_ONLY.
    invertRegex : bool, default is Undefined, optional
        Only trigger a build if the revision regex does NOT match the revision regex.
    """


    branch?: str

    commentControl?: str

    invertRegex?: bool


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderRepositoryEventConfigPush:
    r"""
    Contains filter properties for matching git pushes.
    Structure is documented below.

    Attributes
    ----------
    branch : str, default is Undefined, optional
        Regex of branches to match.
        The syntax of the regular expressions accepted is the syntax accepted by
        RE2 and described at https://github.com/google/re2/wiki/Syntax
    invertRegex : bool, default is Undefined, optional
        Only trigger a build if the revision regex does NOT match the revision regex.
    tag : str, default is Undefined, optional
        Regex of tags to match.
        The syntax of the regular expressions accepted is the syntax accepted by
        RE2 and described at https://github.com/google/re2/wiki/Syntax
    """


    branch?: str

    invertRegex?: bool

    tag?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderServiceAccountRef:
    r"""
    Reference to a ServiceAccount in cloudplatform to populate serviceAccount.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    namespace : str, default is Undefined, optional
        Namespace of the referenced object
    policy : CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderServiceAccountRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    namespace?: str

    policy?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderServiceAccountRefPolicy


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderServiceAccountRefPolicy:
    r"""
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderServiceAccountSelector:
    r"""
    Selector for a ServiceAccount in cloudplatform to populate serviceAccount.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    namespace : str, default is Undefined, optional
        Namespace for the selector
    policy : CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderServiceAccountSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    namespace?: str

    policy?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderServiceAccountSelectorPolicy


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderServiceAccountSelectorPolicy:
    r"""
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderSourceToBuild:
    r"""
    The repo and ref of the repository from which to build.
    This field is used only for those triggers that do not respond to SCM events.
    Triggers that respond to such events build source at whatever commit caused the event.
    This field is currently only used by Webhook, Pub/Sub, Manual, and Cron triggers.
    One of trigger_template, github, pubsub_config webhook_config or source_to_build must be provided.
    Structure is documented below.

    Attributes
    ----------
    bitbucketServerConfig : str, default is Undefined, optional
        The full resource name of the bitbucket server config.
        Format: projects/{project}/locations/{location}/bitbucketServerConfigs/{id}.
    githubEnterpriseConfig : str, default is Undefined, optional
        The full resource name of the github enterprise config.
        Format: projects/{project}/locations/{location}/githubEnterpriseConfigs/{id}. projects/{project}/githubEnterpriseConfigs/{id}.
    ref : str, default is Undefined, optional
        The branch or tag to use. Must start with "refs/" .
    repoType : str, default is Undefined, optional
        The type of the repo, since it may not be explicit from the repo field (e.g from a URL).
        Values can be UNKNOWN, CLOUD_SOURCE_REPOSITORIES, GITHUB, BITBUCKET_SERVER
        Possible values are: UNKNOWN, CLOUD_SOURCE_REPOSITORIES, GITHUB, BITBUCKET_SERVER.
    repository : str, default is Undefined, optional
        The qualified resource name of the Repo API repository.
        Either uri or repository can be specified and is required.
    uri : str, default is Undefined, optional
        The URI of the repo.
    """


    bitbucketServerConfig?: str

    githubEnterpriseConfig?: str

    ref?: str

    repoType?: str

    repository?: str

    uri?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderTriggerTemplate:
    r"""
    Template describing the types of source changes to trigger a build.
    Branch and tag names in trigger templates are interpreted as regular
    expressions. Any branch or tag change that matches that regular
    expression will trigger a build.
    One of trigger_template, github, pubsub_config, webhook_config or source_to_build must be provided.
    Structure is documented below.

    Attributes
    ----------
    branchName : str, default is Undefined, optional
        Name of the branch to build. Exactly one a of branch name, tag, or commit SHA must be provided.
        This field is a regular expression.
    commitSha : str, default is Undefined, optional
        Explicit commit SHA to build. Exactly one of a branch name, tag, or commit SHA must be provided.
    dir : str, default is Undefined, optional
        Directory, relative to the source root, in which to run the build.
        This must be a relative path. If a step's dir is specified and
        is an absolute path, this value is ignored for that step's
        execution.
    invertRegex : bool, default is Undefined, optional
        Only trigger a build if the revision regex does NOT match the revision regex.
    projectId : str, default is Undefined, optional
        ID of the project that owns the Cloud Source Repository. If
        omitted, the project ID requesting the build is assumed.
    repoName : str, default is Undefined, optional
        Name of the Cloud Source Repository. If omitted, the name "default" is assumed.
    tagName : str, default is Undefined, optional
        Name of the tag to build. Exactly one of a branch name, tag, or commit SHA must be provided.
        This field is a regular expression.
    """


    branchName?: str

    commitSha?: str

    dir?: str

    invertRegex?: bool

    projectId?: str

    repoName?: str

    tagName?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderWebhookConfig:
    r"""
    WebhookConfig describes the configuration of a trigger that creates
    a build whenever a webhook is sent to a trigger's webhook URL.
    One of trigger_template, github, pubsub_config webhook_config or source_to_build must be provided.
    Structure is documented below.

    Attributes
    ----------
    secret : str, default is Undefined, optional
        Resource name for the secret required as a URL parameter.
    secretRef : CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderWebhookConfigSecretRef, default is Undefined, optional
        secret ref
    secretSelector : CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderWebhookConfigSecretSelector, default is Undefined, optional
        secret selector
    """


    secret?: str

    secretRef?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderWebhookConfigSecretRef

    secretSelector?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderWebhookConfigSecretSelector


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderWebhookConfigSecretRef:
    r"""
    Reference to a SecretVersion in secretmanager to populate secret.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the referenced object.
    namespace : str, default is Undefined, optional
        Namespace of the referenced object
    policy : CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderWebhookConfigSecretRefPolicy, default is Undefined, optional
        policy
    """


    name: str

    namespace?: str

    policy?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderWebhookConfigSecretRefPolicy


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderWebhookConfigSecretRefPolicy:
    r"""
    Policies for referencing.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderWebhookConfigSecretSelector:
    r"""
    Selector for a SecretVersion in secretmanager to populate secret.

    Attributes
    ----------
    matchControllerRef : bool, default is Undefined, optional
        MatchControllerRef ensures an object with the same controller reference
        as the selecting object is selected.
    matchLabels : {str:str}, default is Undefined, optional
        MatchLabels ensures an object with matching labels is selected.
    namespace : str, default is Undefined, optional
        Namespace for the selector
    policy : CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderWebhookConfigSecretSelectorPolicy, default is Undefined, optional
        policy
    """


    matchControllerRef?: bool

    matchLabels?: {str:str}

    namespace?: str

    policy?: CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderWebhookConfigSecretSelectorPolicy


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecInitProviderWebhookConfigSecretSelectorPolicy:
    r"""
    Policies for selection.

    Attributes
    ----------
    resolution : str, default is "Required", optional
        Resolution specifies whether resolution of this reference is required.
        The default is 'Required', which means the reconcile will fail if the
        reference cannot be resolved. 'Optional' means this reference will be
        a no-op if it cannot be resolved.
    resolve : str, default is Undefined, optional
        Resolve specifies when this reference should be resolved. The default
        is 'IfNotPresent', which will attempt to resolve the reference only when
        the corresponding field is not present. Use 'Always' to resolve the
        reference on every reconcile.
    """


    resolution?: "Required" | "Optional" = "Required"

    resolve?: "Always" | "IfNotPresent"


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecProviderConfigRef:
    r"""
    ProviderConfigReference specifies how the provider that will be used to
    create, observe, update, and delete this managed resource should be
    configured.

    Attributes
    ----------
    kind : str, default is Undefined, required
        Kind of the referenced object.
    name : str, default is Undefined, required
        Name of the referenced object.
    """


    kind: str

    name: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerSpecWriteConnectionSecretToRef:
    r"""
    WriteConnectionSecretToReference specifies the namespace and name of a
    Secret to which any connection details for this managed resource should
    be written. Connection details frequently include the endpoint, username,
    and password required to connect to the managed resource.

    Attributes
    ----------
    name : str, default is Undefined, required
        Name of the secret.
    """


    name: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerStatus:
    r"""
    TriggerStatus defines the observed state of Trigger.

    Attributes
    ----------
    atProvider : CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProvider, default is Undefined, optional
        at provider
    conditions : [CloudbuildGcpmUpboundIoV1beta1TriggerStatusConditionsItems0], default is Undefined, optional
        Conditions of the resource.
    observedGeneration : int, default is Undefined, optional
        ObservedGeneration is the latest metadata.generation
        which resulted in either a ready state, or stalled due to error
        it can not recover from without human intervention.
    """


    atProvider?: CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProvider

    conditions?: [CloudbuildGcpmUpboundIoV1beta1TriggerStatusConditionsItems0]

    observedGeneration?: int


schema CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProvider:
    r"""
    cloudbuild gcpm upbound io v1beta1 trigger status at provider

    Attributes
    ----------
    approvalConfig : CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderApprovalConfig, default is Undefined, optional
        approval config
    bitbucketServerTriggerConfig : CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBitbucketServerTriggerConfig, default is Undefined, optional
        bitbucket server trigger config
    build : CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuild, default is Undefined, optional
        build
    createTime : str, default is Undefined, optional
        Time when the trigger was created.
    description : str, default is Undefined, optional
        Human-readable description of the trigger.
    disabled : bool, default is Undefined, optional
        Whether the trigger is disabled or not. If true, the trigger will never result in a build.
    filename : str, default is Undefined, optional
        Path, from the source root, to a file whose contents is used for the template.
        Either a filename or build template must be provided. Set this only when using trigger_template or github.
        When using Pub/Sub, Webhook or Manual set the file name using git_file_source instead.
    $filter : str, default is Undefined, optional
        A Common Expression Language string. Used only with Pub/Sub and Webhook.
    gitFileSource : CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderGitFileSource, default is Undefined, optional
        git file source
    github : CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderGithub, default is Undefined, optional
        github
    id : str, default is Undefined, optional
        an identifier for the resource with format projects/{{project}}/locations/{{location}}/triggers/{{trigger_id}}
    ignoredFiles : [str], default is Undefined, optional
        ignoredFiles and includedFiles are file glob matches using https://golang.org/pkg/path/filepath/#Match
        extended with support for **.
        If ignoredFiles and changed files are both empty, then they are not
        used to determine whether or not to trigger a build.
        If ignoredFiles is not empty, then we ignore any files that match any
        of the ignored_file globs. If the change has no files that are outside
        of the ignoredFiles globs, then we do not trigger a build.
    includeBuildLogs : str, default is Undefined, optional
        Build logs will be sent back to GitHub as part of the checkrun
        result.  Values can be INCLUDE_BUILD_LOGS_UNSPECIFIED or
        INCLUDE_BUILD_LOGS_WITH_STATUS
        Possible values are: INCLUDE_BUILD_LOGS_UNSPECIFIED, INCLUDE_BUILD_LOGS_WITH_STATUS.
    includedFiles : [str], default is Undefined, optional
        ignoredFiles and includedFiles are file glob matches using https://golang.org/pkg/path/filepath/#Match
        extended with support for **.
        If any of the files altered in the commit pass the ignoredFiles filter
        and includedFiles is empty, then as far as this filter is concerned, we
        should trigger the build.
        If any of the files altered in the commit pass the ignoredFiles filter
        and includedFiles is not empty, then we make sure that at least one of
        those files matches a includedFiles glob. If not, then we do not trigger
        a build.
    location : str, default is Undefined, optional
        The Cloud Build location for the trigger.
        If not specified, "global" is used.
    name : str, default is Undefined, optional
        Name of the trigger. Must be unique within the project.
    project : str, default is Undefined, optional
        The ID of the project in which the resource belongs.
        If it is not provided, the provider project is used.
    pubsubConfig : CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderPubsubConfig, default is Undefined, optional
        pubsub config
    repositoryEventConfig : CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderRepositoryEventConfig, default is Undefined, optional
        repository event config
    serviceAccount : str, default is Undefined, optional
        The service account used for all user-controlled operations including
        triggers.patch, triggers.run, builds.create, and builds.cancel.
        If no service account is set, then the standard Cloud Build service account
        ([PROJECT_NUM]@system.gserviceaccount.com) will be used instead.
        Format: projects/{PROJECT_ID}/serviceAccounts/{ACCOUNT_ID_OR_EMAIL}
    sourceToBuild : CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderSourceToBuild, default is Undefined, optional
        source to build
    substitutions : {str:str}, default is Undefined, optional
        Substitutions data for Build resource.
    tags : [str], default is Undefined, optional
        Tags for annotation of a BuildTrigger
    triggerId : str, default is Undefined, optional
        The unique identifier for the trigger.
    triggerTemplate : CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderTriggerTemplate, default is Undefined, optional
        trigger template
    webhookConfig : CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderWebhookConfig, default is Undefined, optional
        webhook config
    """


    approvalConfig?: CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderApprovalConfig

    bitbucketServerTriggerConfig?: CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBitbucketServerTriggerConfig

    build?: CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuild

    createTime?: str

    description?: str

    disabled?: bool

    filename?: str

    $filter?: str

    gitFileSource?: CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderGitFileSource

    github?: CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderGithub

    id?: str

    ignoredFiles?: [str]

    includeBuildLogs?: str

    includedFiles?: [str]

    location?: str

    name?: str

    project?: str

    pubsubConfig?: CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderPubsubConfig

    repositoryEventConfig?: CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderRepositoryEventConfig

    serviceAccount?: str

    sourceToBuild?: CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderSourceToBuild

    substitutions?: {str:str}

    tags?: [str]

    triggerId?: str

    triggerTemplate?: CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderTriggerTemplate

    webhookConfig?: CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderWebhookConfig


schema CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderApprovalConfig:
    r"""
    Configuration for manual approval to start a build invocation of this BuildTrigger.
    Builds created by this trigger will require approval before they execute.
    Any user with a Cloud Build Approver role for the project can approve a build.
    Structure is documented below.

    Attributes
    ----------
    approvalRequired : bool, default is Undefined, optional
        Whether or not approval is needed. If this is set on a build, it will become pending when run,
        and will need to be explicitly approved to start.
    """


    approvalRequired?: bool


schema CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBitbucketServerTriggerConfig:
    r"""
    BitbucketServerTriggerConfig describes the configuration of a trigger that creates a build whenever a Bitbucket Server event is received.
    Structure is documented below.

    Attributes
    ----------
    bitbucketServerConfigResource : str, default is Undefined, optional
        The Bitbucket server config resource that this trigger config maps to.
    projectKey : str, default is Undefined, optional
        Key of the project that the repo is in. For example: The key for https://mybitbucket.server/projects/TEST/repos/test-repo is "TEST".
    pullRequest : CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBitbucketServerTriggerConfigPullRequest, default is Undefined, optional
        pull request
    push : CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBitbucketServerTriggerConfigPush, default is Undefined, optional
        push
    repoSlug : str, default is Undefined, optional
        Slug of the repository. A repository slug is a URL-friendly version of a repository name, automatically generated by Bitbucket for use in the URL.
        For example, if the repository name is 'test repo', in the URL it would become 'test-repo' as in https://mybitbucket.server/projects/TEST/repos/test-repo.
    """


    bitbucketServerConfigResource?: str

    projectKey?: str

    pullRequest?: CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBitbucketServerTriggerConfigPullRequest

    push?: CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBitbucketServerTriggerConfigPush

    repoSlug?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBitbucketServerTriggerConfigPullRequest:
    r"""
    Filter to match changes in pull requests.
    Structure is documented below.

    Attributes
    ----------
    branch : str, default is Undefined, optional
        Regex of branches to match.
        The syntax of the regular expressions accepted is the syntax accepted by
        RE2 and described at https://github.com/google/re2/wiki/Syntax
    commentControl : str, default is Undefined, optional
        Configure builds to run whether a repository owner or collaborator need to comment /gcbrun.
        Possible values are: COMMENTS_DISABLED, COMMENTS_ENABLED, COMMENTS_ENABLED_FOR_EXTERNAL_CONTRIBUTORS_ONLY.
    invertRegex : bool, default is Undefined, optional
        Only trigger a build if the revision regex does NOT match the revision regex.
    """


    branch?: str

    commentControl?: str

    invertRegex?: bool


schema CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBitbucketServerTriggerConfigPush:
    r"""
    Filter to match changes in refs like branches, tags.
    Structure is documented below.

    Attributes
    ----------
    branch : str, default is Undefined, optional
        Regex of branches to match.
        The syntax of the regular expressions accepted is the syntax accepted by
        RE2 and described at https://github.com/google/re2/wiki/Syntax
    invertRegex : bool, default is Undefined, optional
        Only trigger a build if the revision regex does NOT match the revision regex.
    tag : str, default is Undefined, optional
        Regex of tags to match.
        The syntax of the regular expressions accepted is the syntax accepted by
        RE2 and described at https://github.com/google/re2/wiki/Syntax
    """


    branch?: str

    invertRegex?: bool

    tag?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuild:
    r"""
    Contents of the build template. Either a filename or build template must be provided.
    Structure is documented below.

    Attributes
    ----------
    artifacts : CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildArtifacts, default is Undefined, optional
        artifacts
    availableSecrets : CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildAvailableSecrets, default is Undefined, optional
        available secrets
    images : [str], default is Undefined, optional
        A list of images to be pushed upon the successful completion of all build steps.
        The images are pushed using the builder service account's credentials.
        The digests of the pushed images will be stored in the Build resource's results field.
        If any of the images fail to be pushed, the build status is marked FAILURE.
    logsBucket : str, default is Undefined, optional
        Google Cloud Storage bucket where logs should be written.
        Logs file names will be of the format ${logsBucket}/log-${build_id}.txt.
    options : CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildOptions, default is Undefined, optional
        options
    queueTtl : str, default is Undefined, optional
        TTL in queue for this build. If provided and the build is enqueued longer than this value,
        the build will expire and the build status will be EXPIRED.
        The TTL starts ticking from createTime.
        A duration in seconds with up to nine fractional digits, terminated by 's'. Example: "3.5s".
    secret : [CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildSecretItems0], default is Undefined, optional
        Secrets to decrypt using Cloud Key Management Service.
        Structure is documented below.
    source : CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildSource, default is Undefined, optional
        source
    step : [CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildStepItems0], default is Undefined, optional
        The operations to be performed on the workspace.
        Structure is documented below.
    substitutions : {str:str}, default is Undefined, optional
        Substitutions data for Build resource.
    tags : [str], default is Undefined, optional
        Tags for annotation of a Build. These are not docker tags.
    timeout : str, default is Undefined, optional
        Amount of time that this build should be allowed to run, to second granularity.
        If this amount of time elapses, work on the build will cease and the build status will be TIMEOUT.
        This timeout must be equal to or greater than the sum of the timeouts for build steps within the build.
        The expected format is the number of seconds followed by s.
        Default time is ten minutes (600s).
    """


    artifacts?: CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildArtifacts

    availableSecrets?: CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildAvailableSecrets

    images?: [str]

    logsBucket?: str

    options?: CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildOptions

    queueTtl?: str

    secret?: [CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildSecretItems0]

    source?: CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildSource

    step?: [CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildStepItems0]

    substitutions?: {str:str}

    tags?: [str]

    timeout?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildArtifacts:
    r"""
    Artifacts produced by the build that should be uploaded upon successful completion of all build steps.
    Structure is documented below.

    Attributes
    ----------
    images : [str], default is Undefined, optional
        A list of images to be pushed upon the successful completion of all build steps.
        The images are pushed using the builder service account's credentials.
        The digests of the pushed images will be stored in the Build resource's results field.
        If any of the images fail to be pushed, the build status is marked FAILURE.
    mavenArtifacts : [CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildArtifactsMavenArtifactsItems0], default is Undefined, optional
        A Maven artifact to upload to Artifact Registry upon successful completion of all build steps.
        The location and generation of the uploaded objects will be stored in the Build resource's results field.
        If any objects fail to be pushed, the build is marked FAILURE.
        Structure is documented below.
    npmPackages : [CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildArtifactsNpmPackagesItems0], default is Undefined, optional
        Npm package to upload to Artifact Registry upon successful completion of all build steps.
        The location and generation of the uploaded objects will be stored in the Build resource's results field.
        If any objects fail to be pushed, the build is marked FAILURE.
        Structure is documented below.
    objects : CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildArtifactsObjects, default is Undefined, optional
        objects
    pythonPackages : [CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildArtifactsPythonPackagesItems0], default is Undefined, optional
        Python package to upload to Artifact Registry upon successful completion of all build steps. A package can encapsulate multiple objects to be uploaded to a single repository.
        The location and generation of the uploaded objects will be stored in the Build resource's results field.
        If any objects fail to be pushed, the build is marked FAILURE.
        Structure is documented below.
    """


    images?: [str]

    mavenArtifacts?: [CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildArtifactsMavenArtifactsItems0]

    npmPackages?: [CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildArtifactsNpmPackagesItems0]

    objects?: CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildArtifactsObjects

    pythonPackages?: [CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildArtifactsPythonPackagesItems0]


schema CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildArtifactsMavenArtifactsItems0:
    r"""
    cloudbuild gcpm upbound io v1beta1 trigger status at provider build artifacts maven artifacts items0

    Attributes
    ----------
    artifactId : str, default is Undefined, optional
        Maven artifactId value used when uploading the artifact to Artifact Registry.
    groupId : str, default is Undefined, optional
        Maven groupId value used when uploading the artifact to Artifact Registry.
    path : str, default is Undefined, optional
        Path at which to mount the volume.
        Paths must be absolute and cannot conflict with other volume paths on
        the same build step or with certain reserved volume paths.
    repository : str, default is Undefined, optional
        The qualified resource name of the Repo API repository.
        Either uri or repository can be specified and is required.
    version : str, default is Undefined, optional
        Maven version value used when uploading the artifact to Artifact Registry.
    """


    artifactId?: str

    groupId?: str

    path?: str

    repository?: str

    version?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildArtifactsNpmPackagesItems0:
    r"""
    cloudbuild gcpm upbound io v1beta1 trigger status at provider build artifacts npm packages items0

    Attributes
    ----------
    packagePath : str, default is Undefined, optional
        Path to the package.json. e.g. workspace/path/to/package
    repository : str, default is Undefined, optional
        The qualified resource name of the Repo API repository.
        Either uri or repository can be specified and is required.
    """


    packagePath?: str

    repository?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildArtifactsObjects:
    r"""
    A list of objects to be uploaded to Cloud Storage upon successful completion of all build steps.
    Files in the workspace matching specified paths globs will be uploaded to the
    Cloud Storage location using the builder service account's credentials.
    The location and generation of the uploaded objects will be stored in the Build resource's results field.
    If any objects fail to be pushed, the build is marked FAILURE.
    Structure is documented below.

    Attributes
    ----------
    location : str, default is Undefined, optional
        Cloud Storage bucket and optional object path, in the form "gs://bucket/path/to/somewhere/".
        Files in the workspace matching any path pattern will be uploaded to Cloud Storage with
        this location as a prefix.
    paths : [str], default is Undefined, optional
        Path globs used to match files in the build's workspace. For Python/ Twine, this is usually dist/*, and sometimes additionally an .asc file.
    timing : [CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildArtifactsObjectsTimingItems0], default is Undefined, optional
        Output only. Stores timing information for executing this
        build step.
    """


    location?: str

    paths?: [str]

    timing?: [CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildArtifactsObjectsTimingItems0]


schema CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildArtifactsObjectsTimingItems0:
    r"""
    cloudbuild gcpm upbound io v1beta1 trigger status at provider build artifacts objects timing items0

    Attributes
    ----------
    endTime : str, default is Undefined, optional
        End of time span.
        A timestamp in RFC3339 UTC "Zulu" format, with nanosecond resolution and up to
        nine fractional digits. Examples: "2014-10-02T15:01:23Z" and "2014-10-02T15:01:23.045123456Z".
    startTime : str, default is Undefined, optional
        Start of time span.
        A timestamp in RFC3339 UTC "Zulu" format, with nanosecond resolution and up to
        nine fractional digits. Examples: "2014-10-02T15:01:23Z" and "2014-10-02T15:01:23.045123456Z".
    """


    endTime?: str

    startTime?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildArtifactsPythonPackagesItems0:
    r"""
    cloudbuild gcpm upbound io v1beta1 trigger status at provider build artifacts python packages items0

    Attributes
    ----------
    paths : [str], default is Undefined, optional
        Path globs used to match files in the build's workspace. For Python/ Twine, this is usually dist/*, and sometimes additionally an .asc file.
    repository : str, default is Undefined, optional
        The qualified resource name of the Repo API repository.
        Either uri or repository can be specified and is required.
    """


    paths?: [str]

    repository?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildAvailableSecrets:
    r"""
    Secrets and secret environment variables.
    Structure is documented below.

    Attributes
    ----------
    secretManager : [CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildAvailableSecretsSecretManagerItems0], default is Undefined, optional
        Pairs a secret environment variable with a SecretVersion in Secret Manager.
        Structure is documented below.
    """


    secretManager?: [CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildAvailableSecretsSecretManagerItems0]


schema CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildAvailableSecretsSecretManagerItems0:
    r"""
    cloudbuild gcpm upbound io v1beta1 trigger status at provider build available secrets secret manager items0

    Attributes
    ----------
    env : str, default is Undefined, optional
        A list of environment variable definitions to be used when
        running a step.
        The elements are of the form "KEY=VALUE" for the environment variable
        "KEY" being given the value "VALUE".
    versionName : str, default is Undefined, optional
        Resource name of the SecretVersion. In format: projects//secrets//versions/*
    """


    env?: str

    versionName?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildOptions:
    r"""
    Special options for this build.
    Structure is documented below.

    Attributes
    ----------
    diskSizeGb : float, default is Undefined, optional
        Requested disk size for the VM that runs the build. Note that this is NOT "disk free";
        some of the space will be used by the operating system and build utilities.
        Also note that this is the minimum disk size that will be allocated for the build --
        the build may run with a larger disk than requested. At present, the maximum disk size
        is 1000GB; builds that request more than the maximum are rejected with an error.
    dynamicSubstitutions : bool, default is Undefined, optional
        Option to specify whether or not to apply bash style string operations to the substitutions.
        NOTE this is always enabled for triggered builds and cannot be overridden in the build configuration file.
    env : [str], default is Undefined, optional
        A list of environment variable definitions to be used when
        running a step.
        The elements are of the form "KEY=VALUE" for the environment variable
        "KEY" being given the value "VALUE".
    logStreamingOption : str, default is Undefined, optional
        Option to define build log streaming behavior to Google Cloud Storage.
        Possible values are: STREAM_DEFAULT, STREAM_ON, STREAM_OFF.
    logging : str, default is Undefined, optional
        Option to specify the logging mode, which determines if and where build logs are stored.
        Possible values are: LOGGING_UNSPECIFIED, LEGACY, GCS_ONLY, STACKDRIVER_ONLY, CLOUD_LOGGING_ONLY, NONE.
    machineType : str, default is Undefined, optional
        Compute Engine machine type on which to run the build.
    requestedVerifyOption : str, default is Undefined, optional
        Requested verifiability options.
        Possible values are: NOT_VERIFIED, VERIFIED.
    secretEnv : [str], default is Undefined, optional
        A list of environment variables which are encrypted using
        a Cloud Key
        Management Service crypto key. These values must be specified in
        the build's Secret.
    sourceProvenanceHash : [str], default is Undefined, optional
        Requested hash for SourceProvenance.
        Each value may be one of: NONE, SHA256, MD5.
    substitutionOption : str, default is Undefined, optional
        Option to specify behavior when there is an error in the substitution checks.
        NOTE this is always set to ALLOW_LOOSE for triggered builds and cannot be overridden
        in the build configuration file.
        Possible values are: MUST_MATCH, ALLOW_LOOSE.
    volumes : [CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildOptionsVolumesItems0], default is Undefined, optional
        List of volumes to mount into the build step.
        Each volume is created as an empty volume prior to execution of the
        build step. Upon completion of the build, volumes and their contents
        are discarded.
        Using a named volume in only one step is not valid as it is
        indicative of a build request with an incorrect configuration.
        Structure is documented below.
    workerPool : str, default is Undefined, optional
        Option to specify a WorkerPool for the build. Format projects/{project}/workerPools/{workerPool}
        This field is experimental.
    """


    diskSizeGb?: float

    dynamicSubstitutions?: bool

    env?: [str]

    logStreamingOption?: str

    logging?: str

    machineType?: str

    requestedVerifyOption?: str

    secretEnv?: [str]

    sourceProvenanceHash?: [str]

    substitutionOption?: str

    volumes?: [CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildOptionsVolumesItems0]

    workerPool?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildOptionsVolumesItems0:
    r"""
    cloudbuild gcpm upbound io v1beta1 trigger status at provider build options volumes items0

    Attributes
    ----------
    name : str, default is Undefined, optional
        Name of the volume to mount.
        Volume names must be unique per build step and must be valid names for
        Docker volumes. Each named volume must be used by at least two build steps.
    path : str, default is Undefined, optional
        Path at which to mount the volume.
        Paths must be absolute and cannot conflict with other volume paths on
        the same build step or with certain reserved volume paths.
    """


    name?: str

    path?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildSecretItems0:
    r"""
    cloudbuild gcpm upbound io v1beta1 trigger status at provider build secret items0

    Attributes
    ----------
    kmsKeyName : str, default is Undefined, optional
        Cloud KMS key name to use to decrypt these envs.
    secretEnv : {str:str}, default is Undefined, optional
        A list of environment variables which are encrypted using
        a Cloud Key
        Management Service crypto key. These values must be specified in
        the build's Secret.
    """


    kmsKeyName?: str

    secretEnv?: {str:str}


schema CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildSource:
    r"""
    The location of the source files to build.
    One of storageSource or repoSource must be provided.
    Structure is documented below.

    Attributes
    ----------
    repoSource : CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildSourceRepoSource, default is Undefined, optional
        repo source
    storageSource : CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildSourceStorageSource, default is Undefined, optional
        storage source
    """


    repoSource?: CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildSourceRepoSource

    storageSource?: CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildSourceStorageSource


schema CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildSourceRepoSource:
    r"""
    Location of the source in a Google Cloud Source Repository.
    Structure is documented below.

    Attributes
    ----------
    branchName : str, default is Undefined, optional
        Name of the branch to build. Exactly one a of branch name, tag, or commit SHA must be provided.
        This field is a regular expression.
    commitSha : str, default is Undefined, optional
        Explicit commit SHA to build. Exactly one of a branch name, tag, or commit SHA must be provided.
    dir : str, default is Undefined, optional
        Directory, relative to the source root, in which to run the build.
        This must be a relative path. If a step's dir is specified and
        is an absolute path, this value is ignored for that step's
        execution.
    invertRegex : bool, default is Undefined, optional
        Only trigger a build if the revision regex does NOT match the revision regex.
    projectId : str, default is Undefined, optional
        ID of the project that owns the Cloud Source Repository. If
        omitted, the project ID requesting the build is assumed.
    repoName : str, default is Undefined, optional
        Name of the Cloud Source Repository. If omitted, the name "default" is assumed.
    substitutions : {str:str}, default is Undefined, optional
        Substitutions data for Build resource.
    tagName : str, default is Undefined, optional
        Name of the tag to build. Exactly one of a branch name, tag, or commit SHA must be provided.
        This field is a regular expression.
    """


    branchName?: str

    commitSha?: str

    dir?: str

    invertRegex?: bool

    projectId?: str

    repoName?: str

    substitutions?: {str:str}

    tagName?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildSourceStorageSource:
    r"""
    Location of the source in an archive file in Google Cloud Storage.
    Structure is documented below.

    Attributes
    ----------
    bucket : str, default is Undefined, optional
        Google Cloud Storage bucket containing the source.
    generation : str, default is Undefined, optional
        Google Cloud Storage generation for the object.
        If the generation is omitted, the latest generation will be used
    object : str, default is Undefined, optional
        Google Cloud Storage object containing the source.
        This object must be a gzipped archive file (.tar.gz) containing source to build.
    """


    bucket?: str

    generation?: str

    object?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildStepItems0:
    r"""
    cloudbuild gcpm upbound io v1beta1 trigger status at provider build step items0

    Attributes
    ----------
    allowExitCodes : [float], default is Undefined, optional
        Allow this build step to fail without failing the entire build if and
        only if the exit code is one of the specified codes.
        If allowFailure is also specified, this field will take precedence.
    allowFailure : bool, default is Undefined, optional
        Allow this build step to fail without failing the entire build.
        If false, the entire build will fail if this step fails. Otherwise, the
        build will succeed, but this step will still have a failure status.
        Error information will be reported in the failureDetail field.
        allowExitCodes takes precedence over this field.
    args : [str], default is Undefined, optional
        A list of arguments that will be presented to the step when it is started.
        If the image used to run the step's container has an entrypoint, the args
        are used as arguments to that entrypoint. If the image does not define an
        entrypoint, the first element in args is used as the entrypoint, and the
        remainder will be used as arguments.
    dir : str, default is Undefined, optional
        Directory, relative to the source root, in which to run the build.
        This must be a relative path. If a step's dir is specified and
        is an absolute path, this value is ignored for that step's
        execution.
    entrypoint : str, default is Undefined, optional
        Entrypoint to be used instead of the build step image's
        default entrypoint.
        If unset, the image's default entrypoint is used
    env : [str], default is Undefined, optional
        A list of environment variable definitions to be used when
        running a step.
        The elements are of the form "KEY=VALUE" for the environment variable
        "KEY" being given the value "VALUE".
    id : str, default is Undefined, optional
        Unique identifier for this build step, used in wait_for to
        reference this build step as a dependency.
    name : str, default is Undefined, optional
        Name of the volume to mount.
        Volume names must be unique per build step and must be valid names for
        Docker volumes. Each named volume must be used by at least two build steps.
    script : str, default is Undefined, optional
        A shell script to be executed in the step.
        When script is provided, the user cannot specify the entrypoint or args.
    secretEnv : [str], default is Undefined, optional
        A list of environment variables which are encrypted using
        a Cloud Key
        Management Service crypto key. These values must be specified in
        the build's Secret.
    timeout : str, default is Undefined, optional
        Time limit for executing this build step. If not defined,
        the step has no
        time limit and will be allowed to continue to run until either it
        completes or the build itself times out.
    timing : str, default is Undefined, optional
        Output only. Stores timing information for executing this
        build step.
    volumes : [CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildStepItems0VolumesItems0], default is Undefined, optional
        List of volumes to mount into the build step.
        Each volume is created as an empty volume prior to execution of the
        build step. Upon completion of the build, volumes and their contents
        are discarded.
        Using a named volume in only one step is not valid as it is
        indicative of a build request with an incorrect configuration.
        Structure is documented below.
    waitFor : [str], default is Undefined, optional
        The ID(s) of the step(s) that this build step depends on.
        This build step will not start until all the build steps in wait_for
        have completed successfully. If wait_for is empty, this build step
        will start when all previous build steps in the Build.Steps list
        have completed successfully.
    """


    allowExitCodes?: [float]

    allowFailure?: bool

    args?: [str]

    dir?: str

    entrypoint?: str

    env?: [str]

    id?: str

    name?: str

    script?: str

    secretEnv?: [str]

    timeout?: str

    timing?: str

    volumes?: [CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildStepItems0VolumesItems0]

    waitFor?: [str]


schema CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderBuildStepItems0VolumesItems0:
    r"""
    cloudbuild gcpm upbound io v1beta1 trigger status at provider build step items0 volumes items0

    Attributes
    ----------
    name : str, default is Undefined, optional
        Name of the volume to mount.
        Volume names must be unique per build step and must be valid names for
        Docker volumes. Each named volume must be used by at least two build steps.
    path : str, default is Undefined, optional
        Path at which to mount the volume.
        Paths must be absolute and cannot conflict with other volume paths on
        the same build step or with certain reserved volume paths.
    """


    name?: str

    path?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderGitFileSource:
    r"""
    The file source describing the local or remote Build template.
    Structure is documented below.

    Attributes
    ----------
    bitbucketServerConfig : str, default is Undefined, optional
        The full resource name of the bitbucket server config.
        Format: projects/{project}/locations/{location}/bitbucketServerConfigs/{id}.
    githubEnterpriseConfig : str, default is Undefined, optional
        The full resource name of the github enterprise config.
        Format: projects/{project}/locations/{location}/githubEnterpriseConfigs/{id}. projects/{project}/githubEnterpriseConfigs/{id}.
    path : str, default is Undefined, optional
        The path of the file, with the repo root as the root of the path.
    repoType : str, default is Undefined, optional
        The type of the repo, since it may not be explicit from the repo field (e.g from a URL).
        Values can be UNKNOWN, CLOUD_SOURCE_REPOSITORIES, GITHUB, BITBUCKET_SERVER
        Possible values are: UNKNOWN, CLOUD_SOURCE_REPOSITORIES, GITHUB, BITBUCKET_SERVER.
    repository : str, default is Undefined, optional
        The fully qualified resource name of the Repo API repository. The fully qualified resource name of the Repo API repository.
        If unspecified, the repo from which the trigger invocation originated is assumed to be the repo from which to read the specified path.
    revision : str, default is Undefined, optional
        The branch, tag, arbitrary ref, or SHA version of the repo to use when resolving the
        filename . This field respects the same syntax/resolution as described here: https://git-scm.com/docs/gitrevisions
        If unspecified, the revision from which the trigger invocation originated is assumed to be the revision from which to read the specified path.
    uri : str, default is Undefined, optional
        The URI of the repo . If unspecified, the repo from which the trigger
        invocation originated is assumed to be the repo from which to read the specified path.
    """


    bitbucketServerConfig?: str

    githubEnterpriseConfig?: str

    path?: str

    repoType?: str

    repository?: str

    revision?: str

    uri?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderGithub:
    r"""
    Describes the configuration of a trigger that creates a build whenever a GitHub event is received.
    One of trigger_template, github, pubsub_config or webhook_config must be provided.
    Structure is documented below.

    Attributes
    ----------
    enterpriseConfigResourceName : str, default is Undefined, optional
        The resource name of the github enterprise config that should be applied to this installation.
        For example: "projects/{$projectId}/locations/{$locationId}/githubEnterpriseConfigs/{$configId}"
    name : str, default is Undefined, optional
        Name of the repository. For example: The name for
        https://github.com/googlecloudplatform/cloud-builders is "cloud-builders".
    owner : str, default is Undefined, optional
        Owner of the repository. For example: The owner for
        https://github.com/googlecloudplatform/cloud-builders is "googlecloudplatform".
    pullRequest : CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderGithubPullRequest, default is Undefined, optional
        pull request
    push : CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderGithubPush, default is Undefined, optional
        push
    """


    enterpriseConfigResourceName?: str

    name?: str

    owner?: str

    pullRequest?: CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderGithubPullRequest

    push?: CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderGithubPush


schema CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderGithubPullRequest:
    r"""
    filter to match changes in pull requests. Specify only one of pull_request or push.
    Structure is documented below.

    Attributes
    ----------
    branch : str, default is Undefined, optional
        Regex of branches to match.
        The syntax of the regular expressions accepted is the syntax accepted by
        RE2 and described at https://github.com/google/re2/wiki/Syntax
    commentControl : str, default is Undefined, optional
        Configure builds to run whether a repository owner or collaborator need to comment /gcbrun.
        Possible values are: COMMENTS_DISABLED, COMMENTS_ENABLED, COMMENTS_ENABLED_FOR_EXTERNAL_CONTRIBUTORS_ONLY.
    invertRegex : bool, default is Undefined, optional
        Only trigger a build if the revision regex does NOT match the revision regex.
    """


    branch?: str

    commentControl?: str

    invertRegex?: bool


schema CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderGithubPush:
    r"""
    filter to match changes in refs, like branches or tags. Specify only one of pull_request or push.
    Structure is documented below.

    Attributes
    ----------
    branch : str, default is Undefined, optional
        Regex of branches to match.
        The syntax of the regular expressions accepted is the syntax accepted by
        RE2 and described at https://github.com/google/re2/wiki/Syntax
    invertRegex : bool, default is Undefined, optional
        Only trigger a build if the revision regex does NOT match the revision regex.
    tag : str, default is Undefined, optional
        Regex of tags to match.
        The syntax of the regular expressions accepted is the syntax accepted by
        RE2 and described at https://github.com/google/re2/wiki/Syntax
    """


    branch?: str

    invertRegex?: bool

    tag?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderPubsubConfig:
    r"""
    PubsubConfig describes the configuration of a trigger that creates
    a build whenever a Pub/Sub message is published.
    One of trigger_template, github, pubsub_config webhook_config or source_to_build must be provided.
    Structure is documented below.

    Attributes
    ----------
    serviceAccountEmail : str, default is Undefined, optional
        Service account that will make the push request.
    state : str, default is Undefined, optional
        (Output)
        Potential issues with the underlying Pub/Sub subscription configuration.
        Only populated on get requests.
    subscription : str, default is Undefined, optional
        (Output)
        Output only. Name of the subscription.
    topic : str, default is Undefined, optional
        The name of the topic from which this subscription is receiving messages.
    """


    serviceAccountEmail?: str

    state?: str

    subscription?: str

    topic?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderRepositoryEventConfig:
    r"""
    The configuration of a trigger that creates a build whenever an event from Repo API is received.
    Structure is documented below.

    Attributes
    ----------
    pullRequest : CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderRepositoryEventConfigPullRequest, default is Undefined, optional
        pull request
    push : CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderRepositoryEventConfigPush, default is Undefined, optional
        push
    repository : str, default is Undefined, optional
        The resource name of the Repo API resource.
    """


    pullRequest?: CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderRepositoryEventConfigPullRequest

    push?: CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderRepositoryEventConfigPush

    repository?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderRepositoryEventConfigPullRequest:
    r"""
    Contains filter properties for matching Pull Requests.
    Structure is documented below.

    Attributes
    ----------
    branch : str, default is Undefined, optional
        Regex of branches to match.
        The syntax of the regular expressions accepted is the syntax accepted by
        RE2 and described at https://github.com/google/re2/wiki/Syntax
    commentControl : str, default is Undefined, optional
        Configure builds to run whether a repository owner or collaborator need to comment /gcbrun.
        Possible values are: COMMENTS_DISABLED, COMMENTS_ENABLED, COMMENTS_ENABLED_FOR_EXTERNAL_CONTRIBUTORS_ONLY.
    invertRegex : bool, default is Undefined, optional
        Only trigger a build if the revision regex does NOT match the revision regex.
    """


    branch?: str

    commentControl?: str

    invertRegex?: bool


schema CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderRepositoryEventConfigPush:
    r"""
    Contains filter properties for matching git pushes.
    Structure is documented below.

    Attributes
    ----------
    branch : str, default is Undefined, optional
        Regex of branches to match.
        The syntax of the regular expressions accepted is the syntax accepted by
        RE2 and described at https://github.com/google/re2/wiki/Syntax
    invertRegex : bool, default is Undefined, optional
        Only trigger a build if the revision regex does NOT match the revision regex.
    tag : str, default is Undefined, optional
        Regex of tags to match.
        The syntax of the regular expressions accepted is the syntax accepted by
        RE2 and described at https://github.com/google/re2/wiki/Syntax
    """


    branch?: str

    invertRegex?: bool

    tag?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderSourceToBuild:
    r"""
    The repo and ref of the repository from which to build.
    This field is used only for those triggers that do not respond to SCM events.
    Triggers that respond to such events build source at whatever commit caused the event.
    This field is currently only used by Webhook, Pub/Sub, Manual, and Cron triggers.
    One of trigger_template, github, pubsub_config webhook_config or source_to_build must be provided.
    Structure is documented below.

    Attributes
    ----------
    bitbucketServerConfig : str, default is Undefined, optional
        The full resource name of the bitbucket server config.
        Format: projects/{project}/locations/{location}/bitbucketServerConfigs/{id}.
    githubEnterpriseConfig : str, default is Undefined, optional
        The full resource name of the github enterprise config.
        Format: projects/{project}/locations/{location}/githubEnterpriseConfigs/{id}. projects/{project}/githubEnterpriseConfigs/{id}.
    ref : str, default is Undefined, optional
        The branch or tag to use. Must start with "refs/" .
    repoType : str, default is Undefined, optional
        The type of the repo, since it may not be explicit from the repo field (e.g from a URL).
        Values can be UNKNOWN, CLOUD_SOURCE_REPOSITORIES, GITHUB, BITBUCKET_SERVER
        Possible values are: UNKNOWN, CLOUD_SOURCE_REPOSITORIES, GITHUB, BITBUCKET_SERVER.
    repository : str, default is Undefined, optional
        The qualified resource name of the Repo API repository.
        Either uri or repository can be specified and is required.
    uri : str, default is Undefined, optional
        The URI of the repo.
    """


    bitbucketServerConfig?: str

    githubEnterpriseConfig?: str

    ref?: str

    repoType?: str

    repository?: str

    uri?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderTriggerTemplate:
    r"""
    Template describing the types of source changes to trigger a build.
    Branch and tag names in trigger templates are interpreted as regular
    expressions. Any branch or tag change that matches that regular
    expression will trigger a build.
    One of trigger_template, github, pubsub_config, webhook_config or source_to_build must be provided.
    Structure is documented below.

    Attributes
    ----------
    branchName : str, default is Undefined, optional
        Name of the branch to build. Exactly one a of branch name, tag, or commit SHA must be provided.
        This field is a regular expression.
    commitSha : str, default is Undefined, optional
        Explicit commit SHA to build. Exactly one of a branch name, tag, or commit SHA must be provided.
    dir : str, default is Undefined, optional
        Directory, relative to the source root, in which to run the build.
        This must be a relative path. If a step's dir is specified and
        is an absolute path, this value is ignored for that step's
        execution.
    invertRegex : bool, default is Undefined, optional
        Only trigger a build if the revision regex does NOT match the revision regex.
    projectId : str, default is Undefined, optional
        ID of the project that owns the Cloud Source Repository. If
        omitted, the project ID requesting the build is assumed.
    repoName : str, default is Undefined, optional
        Name of the Cloud Source Repository. If omitted, the name "default" is assumed.
    tagName : str, default is Undefined, optional
        Name of the tag to build. Exactly one of a branch name, tag, or commit SHA must be provided.
        This field is a regular expression.
    """


    branchName?: str

    commitSha?: str

    dir?: str

    invertRegex?: bool

    projectId?: str

    repoName?: str

    tagName?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerStatusAtProviderWebhookConfig:
    r"""
    WebhookConfig describes the configuration of a trigger that creates
    a build whenever a webhook is sent to a trigger's webhook URL.
    One of trigger_template, github, pubsub_config webhook_config or source_to_build must be provided.
    Structure is documented below.

    Attributes
    ----------
    secret : str, default is Undefined, optional
        Resource name for the secret required as a URL parameter.
    state : str, default is Undefined, optional
        (Output)
        Potential issues with the underlying Pub/Sub subscription configuration.
        Only populated on get requests.
    """


    secret?: str

    state?: str


schema CloudbuildGcpmUpboundIoV1beta1TriggerStatusConditionsItems0:
    r"""
    A Condition that may apply to a resource.

    Attributes
    ----------
    lastTransitionTime : str, default is Undefined, required
        LastTransitionTime is the last time this condition transitioned from one
        status to another.
    message : str, default is Undefined, optional
        A Message containing details about this condition's last transition from
        one status to another, if any.
    observedGeneration : int, default is Undefined, optional
        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
        with respect to the current state of the instance.
    reason : str, default is Undefined, required
        A Reason for this condition's last transition from one status to another.
    status : str, default is Undefined, required
        Status of this condition; is it currently True, False, or Unknown?
    $type : str, default is Undefined, required
        Type of this condition. At most one of each condition type may apply to
        a resource at any point in time.
    """


    lastTransitionTime: str

    message?: str

    observedGeneration?: int

    reason: str

    status: str

    $type: str


