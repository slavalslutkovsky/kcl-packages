"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import regex
import k8s.apimachinery.pkg.apis.meta.v1
_regex_match = regex.match


schema NetworkChaos:
    r"""
    NetworkChaos is the Schema for the networkchaos API

    Attributes
    ----------
    apiVersion : str, default is "chaos-mesh.org/v1alpha1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "NetworkChaos", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : ChaosMeshOrgV1alpha1NetworkChaosSpec, default is Undefined, required
        spec
    status : ChaosMeshOrgV1alpha1NetworkChaosStatus, default is Undefined, optional
        status
    """


    apiVersion: "chaos-mesh.org/v1alpha1" = "chaos-mesh.org/v1alpha1"

    kind: "NetworkChaos" = "NetworkChaos"

    metadata?: v1.ObjectMeta

    spec: ChaosMeshOrgV1alpha1NetworkChaosSpec

    status?: ChaosMeshOrgV1alpha1NetworkChaosStatus


schema ChaosMeshOrgV1alpha1NetworkChaosSpec:
    r"""
    Spec defines the behavior of a pod chaos experiment

    Attributes
    ----------
    action : str, default is Undefined, required
        Action defines the specific network chaos action.
        Supported action: partition, netem, delay, loss, duplicate, corrupt
        Default action: delay
    bandwidth : ChaosMeshOrgV1alpha1NetworkChaosSpecBandwidth, default is Undefined, optional
        bandwidth
    corrupt : ChaosMeshOrgV1alpha1NetworkChaosSpecCorrupt, default is Undefined, optional
        corrupt
    delay : ChaosMeshOrgV1alpha1NetworkChaosSpecDelay, default is Undefined, optional
        delay
    device : str, default is Undefined, optional
        Device represents the network device to be affected.
    direction : str, default is "to", optional
        Direction represents the direction, this applies on netem and network partition action
    duplicate : ChaosMeshOrgV1alpha1NetworkChaosSpecDuplicate, default is Undefined, optional
        duplicate
    duration : str, default is Undefined, optional
        Duration represents the duration of the chaos action
    externalTargets : [str], default is Undefined, optional
        ExternalTargets represents network targets outside k8s
    loss : ChaosMeshOrgV1alpha1NetworkChaosSpecLoss, default is Undefined, optional
        loss
    mode : str, default is Undefined, required
        Mode defines the mode to run chaos action.
        Supported mode: one / all / fixed / fixed-percent / random-max-percent
    rate : ChaosMeshOrgV1alpha1NetworkChaosSpecRate, default is Undefined, optional
        rate
    remoteCluster : str, default is Undefined, optional
        RemoteCluster represents the remote cluster where the chaos will be deployed
    selector : ChaosMeshOrgV1alpha1NetworkChaosSpecSelector, default is Undefined, required
        selector
    target : ChaosMeshOrgV1alpha1NetworkChaosSpecTarget, default is Undefined, optional
        target
    targetDevice : str, default is Undefined, optional
        TargetDevice represents the network device to be affected in target scope.
    value : str, default is Undefined, optional
        Value is required when the mode is set to `FixedMode` / `FixedPercentMode` / `RandomMaxPercentMode`.
        If `FixedMode`, provide an integer of pods to do chaos action.
        If `FixedPercentMode`, provide a number from 0-100 to specify the percent of pods the server can do chaos action.
        IF `RandomMaxPercentMode`,  provide a number from 0-100 to specify the max percent of pods to do chaos action
    """


    action: "netem" | "delay" | "loss" | "duplicate" | "corrupt" | "partition" | "bandwidth"

    bandwidth?: ChaosMeshOrgV1alpha1NetworkChaosSpecBandwidth

    corrupt?: ChaosMeshOrgV1alpha1NetworkChaosSpecCorrupt

    delay?: ChaosMeshOrgV1alpha1NetworkChaosSpecDelay

    device?: str

    direction?: "to" | "from" | "both" = "to"

    duplicate?: ChaosMeshOrgV1alpha1NetworkChaosSpecDuplicate

    duration?: str

    externalTargets?: [str]

    loss?: ChaosMeshOrgV1alpha1NetworkChaosSpecLoss

    mode: "one" | "all" | "fixed" | "fixed-percent" | "random-max-percent"

    rate?: ChaosMeshOrgV1alpha1NetworkChaosSpecRate

    remoteCluster?: str

    selector: ChaosMeshOrgV1alpha1NetworkChaosSpecSelector

    target?: ChaosMeshOrgV1alpha1NetworkChaosSpecTarget

    targetDevice?: str

    value?: str


schema ChaosMeshOrgV1alpha1NetworkChaosSpecBandwidth:
    r"""
    Bandwidth represents the detail about bandwidth control action

    Attributes
    ----------
    buffer : int, default is Undefined, required
        Buffer is the maximum amount of bytes that tokens can be available for instantaneously.
    limit : int, default is Undefined, required
        Limit is the number of bytes that can be queued waiting for tokens to become available.
    minburst : int, default is Undefined, optional
        Minburst specifies the size of the peakrate bucket. For perfect
        accuracy, should be set to the MTU of the interface.  If a
        peakrate is needed, but some burstiness is acceptable, this
        size can be raised. A 3000 byte minburst allows around 3mbit/s
        of peakrate, given 1000 byte packets.
    peakrate : int, default is Undefined, optional
        Peakrate is the maximum depletion rate of the bucket.
        The peakrate does not need to be set, it is only necessary
        if perfect millisecond timescale shaping is required.
    rate : str, default is Undefined, required
        Rate is the speed knob. Allows bit, kbit, mbit, gbit, tbit, bps, kbps, mbps, gbps, tbps unit. bps means bytes per second.
    """


    buffer: int

    limit: int

    minburst?: int

    peakrate?: int

    rate: str


    check:
        buffer >= 1
        limit >= 1
        minburst >= 0 if minburst not in [None, Undefined]
        peakrate >= 0 if peakrate not in [None, Undefined]


schema ChaosMeshOrgV1alpha1NetworkChaosSpecCorrupt:
    r"""
    Corrupt represents the detail about corrupt action

    Attributes
    ----------
    correlation : str, default is Undefined, optional
        correlation
    corrupt : str, default is Undefined, required
        corrupt
    """


    correlation?: str

    corrupt: str


schema ChaosMeshOrgV1alpha1NetworkChaosSpecDelay:
    r"""
    Delay represents the detail about delay action

    Attributes
    ----------
    correlation : str, default is Undefined, optional
        correlation
    jitter : str, default is Undefined, optional
        jitter
    latency : str, default is Undefined, required
        latency
    reorder : ChaosMeshOrgV1alpha1NetworkChaosSpecDelayReorder, default is Undefined, optional
        reorder
    """


    correlation?: str

    jitter?: str

    latency: str

    reorder?: ChaosMeshOrgV1alpha1NetworkChaosSpecDelayReorder


    check:
        _regex_match(str(jitter), r"^[0-9]+(\.[0-9]+)?(ns|us|ms|s|m|h)$") if jitter
        _regex_match(str(latency), r"^[0-9]+(\.[0-9]+)?(ns|us|ms|s|m|h)$")


schema ChaosMeshOrgV1alpha1NetworkChaosSpecDelayReorder:
    r"""
    ReorderSpec defines details of packet reorder.

    Attributes
    ----------
    correlation : str, default is Undefined, optional
        correlation
    gap : int, default is Undefined, required
        gap
    reorder : str, default is Undefined, required
        reorder
    """


    correlation?: str

    gap: int

    reorder: str


schema ChaosMeshOrgV1alpha1NetworkChaosSpecDuplicate:
    r"""
    DuplicateSpec represents the detail about loss action

    Attributes
    ----------
    correlation : str, default is Undefined, optional
        correlation
    duplicate : str, default is Undefined, required
        duplicate
    """


    correlation?: str

    duplicate: str


schema ChaosMeshOrgV1alpha1NetworkChaosSpecLoss:
    r"""
    Loss represents the detail about loss action

    Attributes
    ----------
    correlation : str, default is Undefined, optional
        correlation
    loss : str, default is Undefined, required
        loss
    """


    correlation?: str

    loss: str


schema ChaosMeshOrgV1alpha1NetworkChaosSpecRate:
    r"""
    Rate represents the detail about rate control action

    Attributes
    ----------
    rate : str, default is Undefined, required
        Rate is the speed knob. Allows bit, kbit, mbit, gbit, tbit, bps, kbps, mbps, gbps, tbps unit. bps means bytes per second.
    """


    rate: str


schema ChaosMeshOrgV1alpha1NetworkChaosSpecSelector:
    r"""
    Selector is used to select pods that are used to inject chaos action.

    Attributes
    ----------
    annotationSelectors : {str:str}, default is Undefined, optional
        Map of string keys and values that can be used to select objects.
        A selector based on annotations.
    expressionSelectors : [ChaosMeshOrgV1alpha1NetworkChaosSpecSelectorExpressionSelectorsItems0], default is Undefined, optional
        a slice of label selector expressions that can be used to select objects.
        A list of selectors based on set-based label expressions.
    fieldSelectors : {str:str}, default is Undefined, optional
        Map of string keys and values that can be used to select objects.
        A selector based on fields.
    labelSelectors : {str:str}, default is Undefined, optional
        Map of string keys and values that can be used to select objects.
        A selector based on labels.
    namespaces : [str], default is Undefined, optional
        Namespaces is a set of namespace to which objects belong.
    nodeSelectors : {str:str}, default is Undefined, optional
        Map of string keys and values that can be used to select nodes.
        Selector which must match a node's labels,
        and objects must belong to these selected nodes.
    nodes : [str], default is Undefined, optional
        Nodes is a set of node name and objects must belong to these nodes.
    podPhaseSelectors : [str], default is Undefined, optional
        PodPhaseSelectors is a set of condition of a pod at the current time.
        supported value: Pending / Running / Succeeded / Failed / Unknown
    pods : {str:[str]}, default is Undefined, optional
        Pods is a map of string keys and a set values that used to select pods.
        The key defines the namespace which pods belong,
        and the each values is a set of pod names.
    """


    annotationSelectors?: {str:str}

    expressionSelectors?: [ChaosMeshOrgV1alpha1NetworkChaosSpecSelectorExpressionSelectorsItems0]

    fieldSelectors?: {str:str}

    labelSelectors?: {str:str}

    namespaces?: [str]

    nodeSelectors?: {str:str}

    nodes?: [str]

    podPhaseSelectors?: [str]

    pods?: {str:[str]}


schema ChaosMeshOrgV1alpha1NetworkChaosSpecSelectorExpressionSelectorsItems0:
    r"""
    A label selector requirement is a selector that contains values, a key, and an operator that
    relates the key and values.

    Attributes
    ----------
    key : str, default is Undefined, required
        key is the label key that the selector applies to.
    operator : str, default is Undefined, required
        operator represents a key's relationship to a set of values.
        Valid operators are In, NotIn, Exists and DoesNotExist.
    values : [str], default is Undefined, optional
        values is an array of string values. If the operator is In or NotIn,
        the values array must be non-empty. If the operator is Exists or DoesNotExist,
        the values array must be empty. This array is replaced during a strategic
        merge patch.
    """


    key: str

    operator: str

    values?: [str]


schema ChaosMeshOrgV1alpha1NetworkChaosSpecTarget:
    r"""
    Target represents network target, this applies on netem and network partition action

    Attributes
    ----------
    mode : str, default is Undefined, required
        Mode defines the mode to run chaos action.
        Supported mode: one / all / fixed / fixed-percent / random-max-percent
    selector : ChaosMeshOrgV1alpha1NetworkChaosSpecTargetSelector, default is Undefined, required
        selector
    value : str, default is Undefined, optional
        Value is required when the mode is set to `FixedMode` / `FixedPercentMode` / `RandomMaxPercentMode`.
        If `FixedMode`, provide an integer of pods to do chaos action.
        If `FixedPercentMode`, provide a number from 0-100 to specify the percent of pods the server can do chaos action.
        IF `RandomMaxPercentMode`,  provide a number from 0-100 to specify the max percent of pods to do chaos action
    """


    mode: "one" | "all" | "fixed" | "fixed-percent" | "random-max-percent"

    selector: ChaosMeshOrgV1alpha1NetworkChaosSpecTargetSelector

    value?: str


schema ChaosMeshOrgV1alpha1NetworkChaosSpecTargetSelector:
    r"""
    Selector is used to select pods that are used to inject chaos action.

    Attributes
    ----------
    annotationSelectors : {str:str}, default is Undefined, optional
        Map of string keys and values that can be used to select objects.
        A selector based on annotations.
    expressionSelectors : [ChaosMeshOrgV1alpha1NetworkChaosSpecTargetSelectorExpressionSelectorsItems0], default is Undefined, optional
        a slice of label selector expressions that can be used to select objects.
        A list of selectors based on set-based label expressions.
    fieldSelectors : {str:str}, default is Undefined, optional
        Map of string keys and values that can be used to select objects.
        A selector based on fields.
    labelSelectors : {str:str}, default is Undefined, optional
        Map of string keys and values that can be used to select objects.
        A selector based on labels.
    namespaces : [str], default is Undefined, optional
        Namespaces is a set of namespace to which objects belong.
    nodeSelectors : {str:str}, default is Undefined, optional
        Map of string keys and values that can be used to select nodes.
        Selector which must match a node's labels,
        and objects must belong to these selected nodes.
    nodes : [str], default is Undefined, optional
        Nodes is a set of node name and objects must belong to these nodes.
    podPhaseSelectors : [str], default is Undefined, optional
        PodPhaseSelectors is a set of condition of a pod at the current time.
        supported value: Pending / Running / Succeeded / Failed / Unknown
    pods : {str:[str]}, default is Undefined, optional
        Pods is a map of string keys and a set values that used to select pods.
        The key defines the namespace which pods belong,
        and the each values is a set of pod names.
    """


    annotationSelectors?: {str:str}

    expressionSelectors?: [ChaosMeshOrgV1alpha1NetworkChaosSpecTargetSelectorExpressionSelectorsItems0]

    fieldSelectors?: {str:str}

    labelSelectors?: {str:str}

    namespaces?: [str]

    nodeSelectors?: {str:str}

    nodes?: [str]

    podPhaseSelectors?: [str]

    pods?: {str:[str]}


schema ChaosMeshOrgV1alpha1NetworkChaosSpecTargetSelectorExpressionSelectorsItems0:
    r"""
    A label selector requirement is a selector that contains values, a key, and an operator that
    relates the key and values.

    Attributes
    ----------
    key : str, default is Undefined, required
        key is the label key that the selector applies to.
    operator : str, default is Undefined, required
        operator represents a key's relationship to a set of values.
        Valid operators are In, NotIn, Exists and DoesNotExist.
    values : [str], default is Undefined, optional
        values is an array of string values. If the operator is In or NotIn,
        the values array must be non-empty. If the operator is Exists or DoesNotExist,
        the values array must be empty. This array is replaced during a strategic
        merge patch.
    """


    key: str

    operator: str

    values?: [str]


schema ChaosMeshOrgV1alpha1NetworkChaosStatus:
    r"""
    Most recently observed status of the chaos experiment about pods

    Attributes
    ----------
    conditions : [ChaosMeshOrgV1alpha1NetworkChaosStatusConditionsItems0], default is Undefined, optional
        Conditions represents the current global condition of the chaos
    experiment : ChaosMeshOrgV1alpha1NetworkChaosStatusExperiment, default is Undefined, required
        experiment
    instances : {str:int}, default is Undefined, optional
        Instances always specifies podnetworkchaos generation or empty
    """


    conditions?: [ChaosMeshOrgV1alpha1NetworkChaosStatusConditionsItems0]

    experiment: ChaosMeshOrgV1alpha1NetworkChaosStatusExperiment

    instances?: {str:int}


schema ChaosMeshOrgV1alpha1NetworkChaosStatusConditionsItems0:
    r"""
    chaos mesh org v1alpha1 network chaos status conditions items0

    Attributes
    ----------
    reason : str, default is Undefined, optional
        reason
    status : str, default is Undefined, required
        status
    $type : str, default is Undefined, required
        type
    """


    reason?: str

    status: str

    $type: str


schema ChaosMeshOrgV1alpha1NetworkChaosStatusExperiment:
    r"""
    Experiment records the last experiment state.

    Attributes
    ----------
    containerRecords : [ChaosMeshOrgV1alpha1NetworkChaosStatusExperimentContainerRecordsItems0], default is Undefined, optional
        Records are used to track the running status
    desiredPhase : str, default is Undefined, optional
        desired phase
    """


    containerRecords?: [ChaosMeshOrgV1alpha1NetworkChaosStatusExperimentContainerRecordsItems0]

    desiredPhase?: "Run" | "Stop"


schema ChaosMeshOrgV1alpha1NetworkChaosStatusExperimentContainerRecordsItems0:
    r"""
    chaos mesh org v1alpha1 network chaos status experiment container records items0

    Attributes
    ----------
    events : [ChaosMeshOrgV1alpha1NetworkChaosStatusExperimentContainerRecordsItems0EventsItems0], default is Undefined, optional
        Events are the essential details about the injections and recoveries
    id : str, default is Undefined, required
        id
    injectedCount : int, default is Undefined, required
        InjectedCount is a counter to record the sum of successful injections
    phase : str, default is Undefined, required
        phase
    recoveredCount : int, default is Undefined, required
        RecoveredCount is a counter to record the sum of successful recoveries
    selectorKey : str, default is Undefined, required
        selector key
    """


    events?: [ChaosMeshOrgV1alpha1NetworkChaosStatusExperimentContainerRecordsItems0EventsItems0]

    id: str

    injectedCount: int

    phase: str

    recoveredCount: int

    selectorKey: str


schema ChaosMeshOrgV1alpha1NetworkChaosStatusExperimentContainerRecordsItems0EventsItems0:
    r"""
    chaos mesh org v1alpha1 network chaos status experiment container records items0 events items0

    Attributes
    ----------
    message : str, default is Undefined, optional
        Message is the detail message, e.g. the reason why we failed to inject the chaos
    operation : str, default is Undefined, required
        Operation represents the operation we are doing, when we crate this event
    timestamp : str, default is Undefined, required
        Timestamp is time when we create this event
    $type : str, default is Undefined, required
        Type means the stage of this event
    """


    message?: str

    operation: str

    timestamp: str

    $type: str


