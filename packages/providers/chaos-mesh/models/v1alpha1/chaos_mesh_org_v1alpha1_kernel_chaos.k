"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema KernelChaos:
    r"""
    KernelChaos is the Schema for the kernelchaos API

    Attributes
    ----------
    apiVersion : str, default is "chaos-mesh.org/v1alpha1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "KernelChaos", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : ChaosMeshOrgV1alpha1KernelChaosSpec, default is Undefined, required
        spec
    status : ChaosMeshOrgV1alpha1KernelChaosStatus, default is Undefined, optional
        status
    """


    apiVersion: "chaos-mesh.org/v1alpha1" = "chaos-mesh.org/v1alpha1"

    kind: "KernelChaos" = "KernelChaos"

    metadata?: v1.ObjectMeta

    spec: ChaosMeshOrgV1alpha1KernelChaosSpec

    status?: ChaosMeshOrgV1alpha1KernelChaosStatus


schema ChaosMeshOrgV1alpha1KernelChaosSpec:
    r"""
    Spec defines the behavior of a kernel chaos experiment

    Attributes
    ----------
    containerNames : [str], default is Undefined, optional
        ContainerNames indicates list of the name of affected container.
        If not set, the first container will be injected
    duration : str, default is Undefined, optional
        Duration represents the duration of the chaos action
    failKernRequest : ChaosMeshOrgV1alpha1KernelChaosSpecFailKernRequest, default is Undefined, required
        fail kern request
    mode : str, default is Undefined, required
        Mode defines the mode to run chaos action.
        Supported mode: one / all / fixed / fixed-percent / random-max-percent
    remoteCluster : str, default is Undefined, optional
        RemoteCluster represents the remote cluster where the chaos will be deployed
    selector : ChaosMeshOrgV1alpha1KernelChaosSpecSelector, default is Undefined, required
        selector
    value : str, default is Undefined, optional
        Value is required when the mode is set to `FixedMode` / `FixedPercentMode` / `RandomMaxPercentMode`.
        If `FixedMode`, provide an integer of pods to do chaos action.
        If `FixedPercentMode`, provide a number from 0-100 to specify the percent of pods the server can do chaos action.
        IF `RandomMaxPercentMode`,  provide a number from 0-100 to specify the max percent of pods to do chaos action
    """


    containerNames?: [str]

    duration?: str

    failKernRequest: ChaosMeshOrgV1alpha1KernelChaosSpecFailKernRequest

    mode: "one" | "all" | "fixed" | "fixed-percent" | "random-max-percent"

    remoteCluster?: str

    selector: ChaosMeshOrgV1alpha1KernelChaosSpecSelector

    value?: str


schema ChaosMeshOrgV1alpha1KernelChaosSpecFailKernRequest:
    r"""
    FailKernRequest defines the request of kernel injection

    Attributes
    ----------
    callchain : [ChaosMeshOrgV1alpha1KernelChaosSpecFailKernRequestCallchainItems0], default is Undefined, optional
        Callchain indicate a special call chain, such as:
            ext4_mount
              -> mount_subtree
                 -> ...
                    -> should_failslab
        With an optional set of predicates and an optional set of
        parameters, which used with predicates. You can read call chan
        and predicate examples from https://github.com/chaos-mesh/bpfki/tree/develop/examples
        to learn more.
        If no special call chain, just keep Callchain empty, which means it will fail at any call chain
        with slab alloc (eg: kmalloc).
    failtype : int, default is Undefined, required
        FailType indicates what to fail, can be set to '0' / '1' / '2'
        If `0`, indicates slab to fail (should_failslab)
        If `1`, indicates alloc_page to fail (should_fail_alloc_page)
        If `2`, indicates bio to fail (should_fail_bio)
        You can read:
          1. https://www.kernel.org/doc/html/latest/fault-injection/fault-injection.html
          2. http://github.com/iovisor/bcc/blob/master/tools/inject_example.txt
        to learn more
    headers : [str], default is Undefined, optional
        Headers indicates the appropriate kernel headers you need.
        Eg: "linux/mmzone.h", "linux/blkdev.h" and so on
    probability : int, default is Undefined, optional
        Probability indicates the fails with probability.
        If you want 1%, please set this field with 1.
    times : int, default is Undefined, optional
        Times indicates the max times of fails.
    """


    callchain?: [ChaosMeshOrgV1alpha1KernelChaosSpecFailKernRequestCallchainItems0]

    failtype: int

    headers?: [str]

    probability?: int

    times?: int


    check:
        failtype <= 2
        failtype >= 0
        probability <= 100 if probability not in [None, Undefined]
        probability >= 0 if probability not in [None, Undefined]
        times >= 0 if times not in [None, Undefined]


schema ChaosMeshOrgV1alpha1KernelChaosSpecFailKernRequestCallchainItems0:
    r"""
    Frame defines the function signature and predicate in function's body

    Attributes
    ----------
    funcname : str, default is Undefined, optional
        Funcname can be find from kernel source or `/proc/kallsyms`, such as `ext4_mount`
    parameters : str, default is Undefined, optional
        Parameters is used with predicate, for example, if you want to inject slab error
        in `d_alloc_parallel(struct dentry *parent, const struct qstr *name)` with a special
        name `bananas`, you need to set it to `struct dentry *parent, const struct qstr *name`
        otherwise omit it.
    predicate : str, default is Undefined, optional
        Predicate will access the arguments of this Frame, example with Parameters's, you can
        set it to `STRNCMP(name->name, "bananas", 8)` to make inject only with it, or omit it
        to inject for all d_alloc_parallel call chain.
    """


    funcname?: str

    parameters?: str

    predicate?: str


schema ChaosMeshOrgV1alpha1KernelChaosSpecSelector:
    r"""
    Selector is used to select pods that are used to inject chaos action.

    Attributes
    ----------
    annotationSelectors : {str:str}, default is Undefined, optional
        Map of string keys and values that can be used to select objects.
        A selector based on annotations.
    expressionSelectors : [ChaosMeshOrgV1alpha1KernelChaosSpecSelectorExpressionSelectorsItems0], default is Undefined, optional
        a slice of label selector expressions that can be used to select objects.
        A list of selectors based on set-based label expressions.
    fieldSelectors : {str:str}, default is Undefined, optional
        Map of string keys and values that can be used to select objects.
        A selector based on fields.
    labelSelectors : {str:str}, default is Undefined, optional
        Map of string keys and values that can be used to select objects.
        A selector based on labels.
    namespaces : [str], default is Undefined, optional
        Namespaces is a set of namespace to which objects belong.
    nodeSelectors : {str:str}, default is Undefined, optional
        Map of string keys and values that can be used to select nodes.
        Selector which must match a node's labels,
        and objects must belong to these selected nodes.
    nodes : [str], default is Undefined, optional
        Nodes is a set of node name and objects must belong to these nodes.
    podPhaseSelectors : [str], default is Undefined, optional
        PodPhaseSelectors is a set of condition of a pod at the current time.
        supported value: Pending / Running / Succeeded / Failed / Unknown
    pods : {str:[str]}, default is Undefined, optional
        Pods is a map of string keys and a set values that used to select pods.
        The key defines the namespace which pods belong,
        and the each values is a set of pod names.
    """


    annotationSelectors?: {str:str}

    expressionSelectors?: [ChaosMeshOrgV1alpha1KernelChaosSpecSelectorExpressionSelectorsItems0]

    fieldSelectors?: {str:str}

    labelSelectors?: {str:str}

    namespaces?: [str]

    nodeSelectors?: {str:str}

    nodes?: [str]

    podPhaseSelectors?: [str]

    pods?: {str:[str]}


schema ChaosMeshOrgV1alpha1KernelChaosSpecSelectorExpressionSelectorsItems0:
    r"""
    A label selector requirement is a selector that contains values, a key, and an operator that
    relates the key and values.

    Attributes
    ----------
    key : str, default is Undefined, required
        key is the label key that the selector applies to.
    operator : str, default is Undefined, required
        operator represents a key's relationship to a set of values.
        Valid operators are In, NotIn, Exists and DoesNotExist.
    values : [str], default is Undefined, optional
        values is an array of string values. If the operator is In or NotIn,
        the values array must be non-empty. If the operator is Exists or DoesNotExist,
        the values array must be empty. This array is replaced during a strategic
        merge patch.
    """


    key: str

    operator: str

    values?: [str]


schema ChaosMeshOrgV1alpha1KernelChaosStatus:
    r"""
    Most recently observed status of the kernel chaos experiment

    Attributes
    ----------
    conditions : [ChaosMeshOrgV1alpha1KernelChaosStatusConditionsItems0], default is Undefined, optional
        Conditions represents the current global condition of the chaos
    experiment : ChaosMeshOrgV1alpha1KernelChaosStatusExperiment, default is Undefined, required
        experiment
    """


    conditions?: [ChaosMeshOrgV1alpha1KernelChaosStatusConditionsItems0]

    experiment: ChaosMeshOrgV1alpha1KernelChaosStatusExperiment


schema ChaosMeshOrgV1alpha1KernelChaosStatusConditionsItems0:
    r"""
    chaos mesh org v1alpha1 kernel chaos status conditions items0

    Attributes
    ----------
    reason : str, default is Undefined, optional
        reason
    status : str, default is Undefined, required
        status
    $type : str, default is Undefined, required
        type
    """


    reason?: str

    status: str

    $type: str


schema ChaosMeshOrgV1alpha1KernelChaosStatusExperiment:
    r"""
    Experiment records the last experiment state.

    Attributes
    ----------
    containerRecords : [ChaosMeshOrgV1alpha1KernelChaosStatusExperimentContainerRecordsItems0], default is Undefined, optional
        Records are used to track the running status
    desiredPhase : str, default is Undefined, optional
        desired phase
    """


    containerRecords?: [ChaosMeshOrgV1alpha1KernelChaosStatusExperimentContainerRecordsItems0]

    desiredPhase?: "Run" | "Stop"


schema ChaosMeshOrgV1alpha1KernelChaosStatusExperimentContainerRecordsItems0:
    r"""
    chaos mesh org v1alpha1 kernel chaos status experiment container records items0

    Attributes
    ----------
    events : [ChaosMeshOrgV1alpha1KernelChaosStatusExperimentContainerRecordsItems0EventsItems0], default is Undefined, optional
        Events are the essential details about the injections and recoveries
    id : str, default is Undefined, required
        id
    injectedCount : int, default is Undefined, required
        InjectedCount is a counter to record the sum of successful injections
    phase : str, default is Undefined, required
        phase
    recoveredCount : int, default is Undefined, required
        RecoveredCount is a counter to record the sum of successful recoveries
    selectorKey : str, default is Undefined, required
        selector key
    """


    events?: [ChaosMeshOrgV1alpha1KernelChaosStatusExperimentContainerRecordsItems0EventsItems0]

    id: str

    injectedCount: int

    phase: str

    recoveredCount: int

    selectorKey: str


schema ChaosMeshOrgV1alpha1KernelChaosStatusExperimentContainerRecordsItems0EventsItems0:
    r"""
    chaos mesh org v1alpha1 kernel chaos status experiment container records items0 events items0

    Attributes
    ----------
    message : str, default is Undefined, optional
        Message is the detail message, e.g. the reason why we failed to inject the chaos
    operation : str, default is Undefined, required
        Operation represents the operation we are doing, when we crate this event
    timestamp : str, default is Undefined, required
        Timestamp is time when we create this event
    $type : str, default is Undefined, required
        Type means the stage of this event
    """


    message?: str

    operation: str

    timestamp: str

    $type: str


