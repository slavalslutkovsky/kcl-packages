"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema GCPChaos:
    r"""
    GCPChaos is the Schema for the gcpchaos API

    Attributes
    ----------
    apiVersion : str, default is "chaos-mesh.org/v1alpha1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "GCPChaos", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : ChaosMeshOrgV1alpha1GCPChaosSpec, default is Undefined, required
        spec
    status : ChaosMeshOrgV1alpha1GCPChaosStatus, default is Undefined, optional
        status
    """


    apiVersion: "chaos-mesh.org/v1alpha1" = "chaos-mesh.org/v1alpha1"

    kind: "GCPChaos" = "GCPChaos"

    metadata?: v1.ObjectMeta

    spec: ChaosMeshOrgV1alpha1GCPChaosSpec

    status?: ChaosMeshOrgV1alpha1GCPChaosStatus


schema ChaosMeshOrgV1alpha1GCPChaosSpec:
    r"""
    GCPChaosSpec is the content of the specification for a GCPChaos

    Attributes
    ----------
    action : str, default is Undefined, required
        Action defines the specific gcp chaos action.
        Supported action: node-stop / node-reset / disk-loss
        Default action: node-stop
    deviceNames : [str], default is Undefined, optional
        The device name of disks to detach.
        Needed in disk-loss.
    duration : str, default is Undefined, optional
        Duration represents the duration of the chaos action.
    instance : str, default is Undefined, required
        Instance defines the name of the instance
    project : str, default is Undefined, required
        Project defines the ID of gcp project.
    remoteCluster : str, default is Undefined, optional
        RemoteCluster represents the remote cluster where the chaos will be deployed
    secretName : str, default is Undefined, optional
        SecretName defines the name of kubernetes secret. It is used for GCP credentials.
    zone : str, default is Undefined, required
        Zone defines the zone of gcp project.
    """


    action: "node-stop" | "node-reset" | "disk-loss"

    deviceNames?: [str]

    duration?: str

    instance: str

    project: str

    remoteCluster?: str

    secretName?: str

    zone: str


schema ChaosMeshOrgV1alpha1GCPChaosStatus:
    r"""
    GCPChaosStatus represents the status of a GCPChaos

    Attributes
    ----------
    attachedDiskStrings : [str], default is Undefined, optional
        The attached disk info strings.
        Needed in disk-loss.
    conditions : [ChaosMeshOrgV1alpha1GCPChaosStatusConditionsItems0], default is Undefined, optional
        Conditions represents the current global condition of the chaos
    experiment : ChaosMeshOrgV1alpha1GCPChaosStatusExperiment, default is Undefined, required
        experiment
    """


    attachedDiskStrings?: [str]

    conditions?: [ChaosMeshOrgV1alpha1GCPChaosStatusConditionsItems0]

    experiment: ChaosMeshOrgV1alpha1GCPChaosStatusExperiment


schema ChaosMeshOrgV1alpha1GCPChaosStatusConditionsItems0:
    r"""
    chaos mesh org v1alpha1 g c p chaos status conditions items0

    Attributes
    ----------
    reason : str, default is Undefined, optional
        reason
    status : str, default is Undefined, required
        status
    $type : str, default is Undefined, required
        type
    """


    reason?: str

    status: str

    $type: str


schema ChaosMeshOrgV1alpha1GCPChaosStatusExperiment:
    r"""
    Experiment records the last experiment state.

    Attributes
    ----------
    containerRecords : [ChaosMeshOrgV1alpha1GCPChaosStatusExperimentContainerRecordsItems0], default is Undefined, optional
        Records are used to track the running status
    desiredPhase : str, default is Undefined, optional
        desired phase
    """


    containerRecords?: [ChaosMeshOrgV1alpha1GCPChaosStatusExperimentContainerRecordsItems0]

    desiredPhase?: "Run" | "Stop"


schema ChaosMeshOrgV1alpha1GCPChaosStatusExperimentContainerRecordsItems0:
    r"""
    chaos mesh org v1alpha1 g c p chaos status experiment container records items0

    Attributes
    ----------
    events : [ChaosMeshOrgV1alpha1GCPChaosStatusExperimentContainerRecordsItems0EventsItems0], default is Undefined, optional
        Events are the essential details about the injections and recoveries
    id : str, default is Undefined, required
        id
    injectedCount : int, default is Undefined, required
        InjectedCount is a counter to record the sum of successful injections
    phase : str, default is Undefined, required
        phase
    recoveredCount : int, default is Undefined, required
        RecoveredCount is a counter to record the sum of successful recoveries
    selectorKey : str, default is Undefined, required
        selector key
    """


    events?: [ChaosMeshOrgV1alpha1GCPChaosStatusExperimentContainerRecordsItems0EventsItems0]

    id: str

    injectedCount: int

    phase: str

    recoveredCount: int

    selectorKey: str


schema ChaosMeshOrgV1alpha1GCPChaosStatusExperimentContainerRecordsItems0EventsItems0:
    r"""
    chaos mesh org v1alpha1 g c p chaos status experiment container records items0 events items0

    Attributes
    ----------
    message : str, default is Undefined, optional
        Message is the detail message, e.g. the reason why we failed to inject the chaos
    operation : str, default is Undefined, required
        Operation represents the operation we are doing, when we crate this event
    timestamp : str, default is Undefined, required
        Timestamp is time when we create this event
    $type : str, default is Undefined, required
        Type means the stage of this event
    """


    message?: str

    operation: str

    timestamp: str

    $type: str


