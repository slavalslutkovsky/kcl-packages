"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema IOChaos:
    r"""
    IOChaos is the Schema for the iochaos API

    Attributes
    ----------
    apiVersion : str, default is "chaos-mesh.org/v1alpha1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "IOChaos", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : ChaosMeshOrgV1alpha1IOChaosSpec, default is Undefined, optional
        spec
    status : ChaosMeshOrgV1alpha1IOChaosStatus, default is Undefined, optional
        status
    """


    apiVersion: "chaos-mesh.org/v1alpha1" = "chaos-mesh.org/v1alpha1"

    kind: "IOChaos" = "IOChaos"

    metadata?: v1.ObjectMeta

    spec?: ChaosMeshOrgV1alpha1IOChaosSpec

    status?: ChaosMeshOrgV1alpha1IOChaosStatus


schema ChaosMeshOrgV1alpha1IOChaosSpec:
    r"""
    IOChaosSpec defines the desired state of IOChaos

    Attributes
    ----------
    action : str, default is Undefined, required
        Action defines the specific pod chaos action.
        Supported action: latency / fault / attrOverride / mistake
    attr : ChaosMeshOrgV1alpha1IOChaosSpecAttr, default is Undefined, optional
        attr
    containerNames : [str], default is Undefined, optional
        ContainerNames indicates list of the name of affected container.
        If not set, the first container will be injected
    delay : str, default is Undefined, optional
        Delay defines the value of I/O chaos action delay.
        A delay string is a possibly signed sequence of
        decimal numbers, each with optional fraction and a unit suffix,
        such as "300ms".
        Valid time units are "ns", "us" (or "µs"), "ms", "s", "m", "h".
    duration : str, default is Undefined, optional
        Duration represents the duration of the chaos action.
        It is required when the action is `PodFailureAction`.
        A duration string is a possibly signed sequence of
        decimal numbers, each with optional fraction and a unit suffix,
        such as "300ms", "-1.5h" or "2h45m".
        Valid time units are "ns", "us" (or "µs"), "ms", "s", "m", "h".
    errno : int, default is Undefined, optional
        Errno defines the error code that returned by I/O action.
        refer to: https://www-numi.fnal.gov/offline_software/srt_public_context/WebDocs/Errors/unix_system_errors.html
    methods : [str], default is Undefined, optional
        Methods defines the I/O methods for injecting I/O chaos action.
        default: all I/O methods.
    mistake : ChaosMeshOrgV1alpha1IOChaosSpecMistake, default is Undefined, optional
        mistake
    mode : str, default is Undefined, required
        Mode defines the mode to run chaos action.
        Supported mode: one / all / fixed / fixed-percent / random-max-percent
    path : str, default is Undefined, optional
        Path defines the path of files for injecting I/O chaos action.
    percent : int, default is 100, optional
        Percent defines the percentage of injection errors and provides a number from 0-100.
        default: 100.
    remoteCluster : str, default is Undefined, optional
        RemoteCluster represents the remote cluster where the chaos will be deployed
    selector : ChaosMeshOrgV1alpha1IOChaosSpecSelector, default is Undefined, required
        selector
    value : str, default is Undefined, optional
        Value is required when the mode is set to `FixedMode` / `FixedPercentMode` / `RandomMaxPercentMode`.
        If `FixedMode`, provide an integer of pods to do chaos action.
        If `FixedPercentMode`, provide a number from 0-100 to specify the percent of pods the server can do chaos action.
        IF `RandomMaxPercentMode`,  provide a number from 0-100 to specify the max percent of pods to do chaos action
    volumePath : str, default is Undefined, required
        VolumePath represents the mount path of injected volume
    """


    action: "latency" | "fault" | "attrOverride" | "mistake"

    attr?: ChaosMeshOrgV1alpha1IOChaosSpecAttr

    containerNames?: [str]

    delay?: str

    duration?: str

    errno?: int

    methods?: [str]

    mistake?: ChaosMeshOrgV1alpha1IOChaosSpecMistake

    mode: "one" | "all" | "fixed" | "fixed-percent" | "random-max-percent"

    path?: str

    percent?: int = 100

    remoteCluster?: str

    selector: ChaosMeshOrgV1alpha1IOChaosSpecSelector

    value?: str

    volumePath: str


schema ChaosMeshOrgV1alpha1IOChaosSpecAttr:
    r"""
    Attr defines the overrided attribution

    Attributes
    ----------
    atime : ChaosMeshOrgV1alpha1IOChaosSpecAttrAtime, default is Undefined, optional
        atime
    blocks : int, default is Undefined, optional
        blocks
    ctime : ChaosMeshOrgV1alpha1IOChaosSpecAttrCtime, default is Undefined, optional
        ctime
    gid : int, default is Undefined, optional
        gid
    ino : int, default is Undefined, optional
        ino
    kind : str, default is Undefined, optional
        FileType represents type of file
    mtime : ChaosMeshOrgV1alpha1IOChaosSpecAttrMtime, default is Undefined, optional
        mtime
    nlink : int, default is Undefined, optional
        nlink
    perm : int, default is Undefined, optional
        perm
    rdev : int, default is Undefined, optional
        rdev
    size : int, default is Undefined, optional
        size
    uid : int, default is Undefined, optional
        uid
    """


    atime?: ChaosMeshOrgV1alpha1IOChaosSpecAttrAtime

    blocks?: int

    ctime?: ChaosMeshOrgV1alpha1IOChaosSpecAttrCtime

    gid?: int

    ino?: int

    kind?: str

    mtime?: ChaosMeshOrgV1alpha1IOChaosSpecAttrMtime

    nlink?: int

    perm?: int

    rdev?: int

    size?: int

    uid?: int


schema ChaosMeshOrgV1alpha1IOChaosSpecAttrAtime:
    r"""
    Timespec represents a time

    Attributes
    ----------
    nsec : int, default is Undefined, required
        nsec
    sec : int, default is Undefined, required
        sec
    """


    nsec: int

    sec: int


schema ChaosMeshOrgV1alpha1IOChaosSpecAttrCtime:
    r"""
    Timespec represents a time

    Attributes
    ----------
    nsec : int, default is Undefined, required
        nsec
    sec : int, default is Undefined, required
        sec
    """


    nsec: int

    sec: int


schema ChaosMeshOrgV1alpha1IOChaosSpecAttrMtime:
    r"""
    Timespec represents a time

    Attributes
    ----------
    nsec : int, default is Undefined, required
        nsec
    sec : int, default is Undefined, required
        sec
    """


    nsec: int

    sec: int


schema ChaosMeshOrgV1alpha1IOChaosSpecMistake:
    r"""
    Mistake defines what types of incorrectness are injected to IO operations

    Attributes
    ----------
    filling : str, default is Undefined, optional
        Filling determines what is filled in the mistake data.
    maxLength : int, default is Undefined, optional
        Max length of each wrong data segment in bytes
    maxOccurrences : int, default is Undefined, optional
        There will be [1, MaxOccurrences] segments of wrong data.
    """


    filling?: "zero" | "random"

    maxLength?: int

    maxOccurrences?: int


    check:
        maxLength >= 1 if maxLength not in [None, Undefined]
        maxOccurrences >= 1 if maxOccurrences not in [None, Undefined]


schema ChaosMeshOrgV1alpha1IOChaosSpecSelector:
    r"""
    Selector is used to select pods that are used to inject chaos action.

    Attributes
    ----------
    annotationSelectors : {str:str}, default is Undefined, optional
        Map of string keys and values that can be used to select objects.
        A selector based on annotations.
    expressionSelectors : [ChaosMeshOrgV1alpha1IOChaosSpecSelectorExpressionSelectorsItems0], default is Undefined, optional
        a slice of label selector expressions that can be used to select objects.
        A list of selectors based on set-based label expressions.
    fieldSelectors : {str:str}, default is Undefined, optional
        Map of string keys and values that can be used to select objects.
        A selector based on fields.
    labelSelectors : {str:str}, default is Undefined, optional
        Map of string keys and values that can be used to select objects.
        A selector based on labels.
    namespaces : [str], default is Undefined, optional
        Namespaces is a set of namespace to which objects belong.
    nodeSelectors : {str:str}, default is Undefined, optional
        Map of string keys and values that can be used to select nodes.
        Selector which must match a node's labels,
        and objects must belong to these selected nodes.
    nodes : [str], default is Undefined, optional
        Nodes is a set of node name and objects must belong to these nodes.
    podPhaseSelectors : [str], default is Undefined, optional
        PodPhaseSelectors is a set of condition of a pod at the current time.
        supported value: Pending / Running / Succeeded / Failed / Unknown
    pods : {str:[str]}, default is Undefined, optional
        Pods is a map of string keys and a set values that used to select pods.
        The key defines the namespace which pods belong,
        and the each values is a set of pod names.
    """


    annotationSelectors?: {str:str}

    expressionSelectors?: [ChaosMeshOrgV1alpha1IOChaosSpecSelectorExpressionSelectorsItems0]

    fieldSelectors?: {str:str}

    labelSelectors?: {str:str}

    namespaces?: [str]

    nodeSelectors?: {str:str}

    nodes?: [str]

    podPhaseSelectors?: [str]

    pods?: {str:[str]}


schema ChaosMeshOrgV1alpha1IOChaosSpecSelectorExpressionSelectorsItems0:
    r"""
    A label selector requirement is a selector that contains values, a key, and an operator that
    relates the key and values.

    Attributes
    ----------
    key : str, default is Undefined, required
        key is the label key that the selector applies to.
    operator : str, default is Undefined, required
        operator represents a key's relationship to a set of values.
        Valid operators are In, NotIn, Exists and DoesNotExist.
    values : [str], default is Undefined, optional
        values is an array of string values. If the operator is In or NotIn,
        the values array must be non-empty. If the operator is Exists or DoesNotExist,
        the values array must be empty. This array is replaced during a strategic
        merge patch.
    """


    key: str

    operator: str

    values?: [str]


schema ChaosMeshOrgV1alpha1IOChaosStatus:
    r"""
    IOChaosStatus defines the observed state of IOChaos

    Attributes
    ----------
    conditions : [ChaosMeshOrgV1alpha1IOChaosStatusConditionsItems0], default is Undefined, optional
        Conditions represents the current global condition of the chaos
    experiment : ChaosMeshOrgV1alpha1IOChaosStatusExperiment, default is Undefined, required
        experiment
    instances : {str:int}, default is Undefined, optional
        Instances always specifies podiochaos generation or empty
    """


    conditions?: [ChaosMeshOrgV1alpha1IOChaosStatusConditionsItems0]

    experiment: ChaosMeshOrgV1alpha1IOChaosStatusExperiment

    instances?: {str:int}


schema ChaosMeshOrgV1alpha1IOChaosStatusConditionsItems0:
    r"""
    chaos mesh org v1alpha1 i o chaos status conditions items0

    Attributes
    ----------
    reason : str, default is Undefined, optional
        reason
    status : str, default is Undefined, required
        status
    $type : str, default is Undefined, required
        type
    """


    reason?: str

    status: str

    $type: str


schema ChaosMeshOrgV1alpha1IOChaosStatusExperiment:
    r"""
    Experiment records the last experiment state.

    Attributes
    ----------
    containerRecords : [ChaosMeshOrgV1alpha1IOChaosStatusExperimentContainerRecordsItems0], default is Undefined, optional
        Records are used to track the running status
    desiredPhase : str, default is Undefined, optional
        desired phase
    """


    containerRecords?: [ChaosMeshOrgV1alpha1IOChaosStatusExperimentContainerRecordsItems0]

    desiredPhase?: "Run" | "Stop"


schema ChaosMeshOrgV1alpha1IOChaosStatusExperimentContainerRecordsItems0:
    r"""
    chaos mesh org v1alpha1 i o chaos status experiment container records items0

    Attributes
    ----------
    events : [ChaosMeshOrgV1alpha1IOChaosStatusExperimentContainerRecordsItems0EventsItems0], default is Undefined, optional
        Events are the essential details about the injections and recoveries
    id : str, default is Undefined, required
        id
    injectedCount : int, default is Undefined, required
        InjectedCount is a counter to record the sum of successful injections
    phase : str, default is Undefined, required
        phase
    recoveredCount : int, default is Undefined, required
        RecoveredCount is a counter to record the sum of successful recoveries
    selectorKey : str, default is Undefined, required
        selector key
    """


    events?: [ChaosMeshOrgV1alpha1IOChaosStatusExperimentContainerRecordsItems0EventsItems0]

    id: str

    injectedCount: int

    phase: str

    recoveredCount: int

    selectorKey: str


schema ChaosMeshOrgV1alpha1IOChaosStatusExperimentContainerRecordsItems0EventsItems0:
    r"""
    chaos mesh org v1alpha1 i o chaos status experiment container records items0 events items0

    Attributes
    ----------
    message : str, default is Undefined, optional
        Message is the detail message, e.g. the reason why we failed to inject the chaos
    operation : str, default is Undefined, required
        Operation represents the operation we are doing, when we crate this event
    timestamp : str, default is Undefined, required
        Timestamp is time when we create this event
    $type : str, default is Undefined, required
        Type means the stage of this event
    """


    message?: str

    operation: str

    timestamp: str

    $type: str


