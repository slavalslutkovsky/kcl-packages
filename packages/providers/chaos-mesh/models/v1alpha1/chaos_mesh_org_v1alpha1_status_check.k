"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema StatusCheck:
    r"""
    chaos mesh org v1alpha1 status check

    Attributes
    ----------
    apiVersion : str, default is "chaos-mesh.org/v1alpha1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "StatusCheck", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : ChaosMeshOrgV1alpha1StatusCheckSpec, default is Undefined, required
        spec
    status : ChaosMeshOrgV1alpha1StatusCheckStatus, default is Undefined, optional
        status
    """


    apiVersion: "chaos-mesh.org/v1alpha1" = "chaos-mesh.org/v1alpha1"

    kind: "StatusCheck" = "StatusCheck"

    metadata?: v1.ObjectMeta

    spec: ChaosMeshOrgV1alpha1StatusCheckSpec

    status?: ChaosMeshOrgV1alpha1StatusCheckStatus


schema ChaosMeshOrgV1alpha1StatusCheckSpec:
    r"""
    Spec defines the behavior of a status check

    Attributes
    ----------
    duration : str, default is Undefined, optional
        Duration defines the duration of the whole status check if the
        number of failed execution does not exceed the failure threshold.
        Duration is available to both `Synchronous` and `Continuous` mode.
        A duration string is a possibly signed sequence of
        decimal numbers, each with optional fraction and a unit suffix,
        such as "300ms", "-1.5h" or "2h45m".
        Valid time units are "ns", "us" (or "µs"), "ms", "s", "m", "h".
    failureThreshold : int, default is 3, optional
        FailureThreshold defines the minimum consecutive failure
        for the status check to be considered failed.
    http : ChaosMeshOrgV1alpha1StatusCheckSpecHTTP, default is Undefined, optional
        http
    intervalSeconds : int, default is 10, optional
        IntervalSeconds defines how often (in seconds) to perform
        an execution of status check.
    mode : str, default is Undefined, optional
        Mode defines the execution mode of the status check.
        Support type: Synchronous / Continuous
    recordsHistoryLimit : int, default is 100, optional
        RecordsHistoryLimit defines the number of record to retain.
    successThreshold : int, default is 1, optional
        SuccessThreshold defines the minimum consecutive successes
        for the status check to be considered successful.
        SuccessThreshold only works for `Synchronous` mode.
    timeoutSeconds : int, default is 1, optional
        TimeoutSeconds defines the number of seconds after which
        an execution of status check times out.
    $type : str, default is "HTTP", required
        Type defines the specific status check type.
        Support type: HTTP
    """


    duration?: str

    failureThreshold?: int = 3

    http?: ChaosMeshOrgV1alpha1StatusCheckSpecHTTP

    intervalSeconds?: int = 10

    mode?: "Synchronous" | "Continuous"

    recordsHistoryLimit?: int = 100

    successThreshold?: int = 1

    timeoutSeconds?: int = 1

    $type: "HTTP" = "HTTP"


    check:
        failureThreshold >= 1 if failureThreshold not in [None, Undefined]
        intervalSeconds >= 1 if intervalSeconds not in [None, Undefined]
        recordsHistoryLimit <= 1000 if recordsHistoryLimit not in [None, Undefined]
        recordsHistoryLimit >= 1 if recordsHistoryLimit not in [None, Undefined]
        successThreshold >= 1 if successThreshold not in [None, Undefined]
        timeoutSeconds >= 1 if timeoutSeconds not in [None, Undefined]


schema ChaosMeshOrgV1alpha1StatusCheckSpecHTTP:
    r"""
    chaos mesh org v1alpha1 status check spec HTTP

    Attributes
    ----------
    body : str, default is Undefined, optional
        body
    criteria : ChaosMeshOrgV1alpha1StatusCheckSpecHTTPCriteria, default is Undefined, required
        criteria
    headers : {str:[str]}, default is Undefined, optional
        A Header represents the key-value pairs in an HTTP header.

        The keys should be in canonical form, as returned by
        [CanonicalHeaderKey].
    method : str, default is "GET", optional
        method
    url : str, default is Undefined, required
        url
    """


    body?: str

    criteria: ChaosMeshOrgV1alpha1StatusCheckSpecHTTPCriteria

    headers?: {str:[str]}

    method?: "GET" | "POST" = "GET"

    url: str


schema ChaosMeshOrgV1alpha1StatusCheckSpecHTTPCriteria:
    r"""
    Criteria defines how to determine the result of the status check.

    Attributes
    ----------
    statusCode : str, default is Undefined, required
        StatusCode defines the expected http status code for the request.
        A statusCode string could be a single code (e.g. 200), or
        an inclusive range (e.g. 200-400, both `200` and `400` are included).
    """


    statusCode: str


schema ChaosMeshOrgV1alpha1StatusCheckStatus:
    r"""
    Most recently observed status of status check

    Attributes
    ----------
    completionTime : str, default is Undefined, optional
        CompletionTime represents time when the status check was completed.
    conditions : [ChaosMeshOrgV1alpha1StatusCheckStatusConditionsItems0], default is Undefined, optional
        Conditions represents the latest available observations of a StatusCheck's current state.
    count : int, default is Undefined, optional
        Count represents the total number of the status check executed.
    records : [ChaosMeshOrgV1alpha1StatusCheckStatusRecordsItems0], default is Undefined, optional
        Records contains the history of the execution of StatusCheck.
    startTime : str, default is Undefined, optional
        StartTime represents time when the status check started to execute.
    """


    completionTime?: str

    conditions?: [ChaosMeshOrgV1alpha1StatusCheckStatusConditionsItems0]

    count?: int

    records?: [ChaosMeshOrgV1alpha1StatusCheckStatusRecordsItems0]

    startTime?: str


schema ChaosMeshOrgV1alpha1StatusCheckStatusConditionsItems0:
    r"""
    chaos mesh org v1alpha1 status check status conditions items0

    Attributes
    ----------
    lastProbeTime : str, default is Undefined, required
        last probe time
    lastTransitionTime : str, default is Undefined, required
        last transition time
    reason : str, default is Undefined, required
        reason
    status : str, default is Undefined, required
        status
    $type : str, default is Undefined, required
        type
    """


    lastProbeTime: str

    lastTransitionTime: str

    reason: str

    status: str

    $type: str


schema ChaosMeshOrgV1alpha1StatusCheckStatusRecordsItems0:
    r"""
    chaos mesh org v1alpha1 status check status records items0

    Attributes
    ----------
    outcome : str, default is Undefined, required
        outcome
    startTime : str, default is Undefined, required
        start time
    """


    outcome: str

    startTime: str


