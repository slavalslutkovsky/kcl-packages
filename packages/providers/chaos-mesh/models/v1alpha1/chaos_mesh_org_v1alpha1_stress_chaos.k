"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema StressChaos:
    r"""
    StressChaos is the Schema for the stresschaos API

    Attributes
    ----------
    apiVersion : str, default is "chaos-mesh.org/v1alpha1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "StressChaos", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : ChaosMeshOrgV1alpha1StressChaosSpec, default is Undefined, required
        spec
    status : ChaosMeshOrgV1alpha1StressChaosStatus, default is Undefined, optional
        status
    """


    apiVersion: "chaos-mesh.org/v1alpha1" = "chaos-mesh.org/v1alpha1"

    kind: "StressChaos" = "StressChaos"

    metadata?: v1.ObjectMeta

    spec: ChaosMeshOrgV1alpha1StressChaosSpec

    status?: ChaosMeshOrgV1alpha1StressChaosStatus


schema ChaosMeshOrgV1alpha1StressChaosSpec:
    r"""
    Spec defines the behavior of a time chaos experiment

    Attributes
    ----------
    containerNames : [str], default is Undefined, optional
        ContainerNames indicates list of the name of affected container.
        If not set, the first container will be injected
    duration : str, default is Undefined, optional
        Duration represents the duration of the chaos action
    mode : str, default is Undefined, required
        Mode defines the mode to run chaos action.
        Supported mode: one / all / fixed / fixed-percent / random-max-percent
    remoteCluster : str, default is Undefined, optional
        RemoteCluster represents the remote cluster where the chaos will be deployed
    selector : ChaosMeshOrgV1alpha1StressChaosSpecSelector, default is Undefined, required
        selector
    stressngStressors : str, default is Undefined, optional
        StressngStressors defines plenty of stressors just like `Stressors` except that it's an experimental
        feature and more powerful. You can define stressors in `stress-ng` (see also `man stress-ng`) dialect,
        however not all of the supported stressors are well tested. It maybe retired in later releases. You
        should always use `Stressors` to define the stressors and use this only when you want more stressors
        unsupported by `Stressors`. When both `StressngStressors` and `Stressors` are defined, `StressngStressors`
        wins.
    stressors : ChaosMeshOrgV1alpha1StressChaosSpecStressors, default is Undefined, optional
        stressors
    value : str, default is Undefined, optional
        Value is required when the mode is set to `FixedMode` / `FixedPercentMode` / `RandomMaxPercentMode`.
        If `FixedMode`, provide an integer of pods to do chaos action.
        If `FixedPercentMode`, provide a number from 0-100 to specify the percent of pods the server can do chaos action.
        IF `RandomMaxPercentMode`,  provide a number from 0-100 to specify the max percent of pods to do chaos action
    """


    containerNames?: [str]

    duration?: str

    mode: "one" | "all" | "fixed" | "fixed-percent" | "random-max-percent"

    remoteCluster?: str

    selector: ChaosMeshOrgV1alpha1StressChaosSpecSelector

    stressngStressors?: str

    stressors?: ChaosMeshOrgV1alpha1StressChaosSpecStressors

    value?: str


schema ChaosMeshOrgV1alpha1StressChaosSpecSelector:
    r"""
    Selector is used to select pods that are used to inject chaos action.

    Attributes
    ----------
    annotationSelectors : {str:str}, default is Undefined, optional
        Map of string keys and values that can be used to select objects.
        A selector based on annotations.
    expressionSelectors : [ChaosMeshOrgV1alpha1StressChaosSpecSelectorExpressionSelectorsItems0], default is Undefined, optional
        a slice of label selector expressions that can be used to select objects.
        A list of selectors based on set-based label expressions.
    fieldSelectors : {str:str}, default is Undefined, optional
        Map of string keys and values that can be used to select objects.
        A selector based on fields.
    labelSelectors : {str:str}, default is Undefined, optional
        Map of string keys and values that can be used to select objects.
        A selector based on labels.
    namespaces : [str], default is Undefined, optional
        Namespaces is a set of namespace to which objects belong.
    nodeSelectors : {str:str}, default is Undefined, optional
        Map of string keys and values that can be used to select nodes.
        Selector which must match a node's labels,
        and objects must belong to these selected nodes.
    nodes : [str], default is Undefined, optional
        Nodes is a set of node name and objects must belong to these nodes.
    podPhaseSelectors : [str], default is Undefined, optional
        PodPhaseSelectors is a set of condition of a pod at the current time.
        supported value: Pending / Running / Succeeded / Failed / Unknown
    pods : {str:[str]}, default is Undefined, optional
        Pods is a map of string keys and a set values that used to select pods.
        The key defines the namespace which pods belong,
        and the each values is a set of pod names.
    """


    annotationSelectors?: {str:str}

    expressionSelectors?: [ChaosMeshOrgV1alpha1StressChaosSpecSelectorExpressionSelectorsItems0]

    fieldSelectors?: {str:str}

    labelSelectors?: {str:str}

    namespaces?: [str]

    nodeSelectors?: {str:str}

    nodes?: [str]

    podPhaseSelectors?: [str]

    pods?: {str:[str]}


schema ChaosMeshOrgV1alpha1StressChaosSpecSelectorExpressionSelectorsItems0:
    r"""
    A label selector requirement is a selector that contains values, a key, and an operator that
    relates the key and values.

    Attributes
    ----------
    key : str, default is Undefined, required
        key is the label key that the selector applies to.
    operator : str, default is Undefined, required
        operator represents a key's relationship to a set of values.
        Valid operators are In, NotIn, Exists and DoesNotExist.
    values : [str], default is Undefined, optional
        values is an array of string values. If the operator is In or NotIn,
        the values array must be non-empty. If the operator is Exists or DoesNotExist,
        the values array must be empty. This array is replaced during a strategic
        merge patch.
    """


    key: str

    operator: str

    values?: [str]


schema ChaosMeshOrgV1alpha1StressChaosSpecStressors:
    r"""
    Stressors defines plenty of stressors supported to stress system components out.
    You can use one or more of them to make up various kinds of stresses. At least
    one of the stressors should be specified.

    Attributes
    ----------
    cpu : ChaosMeshOrgV1alpha1StressChaosSpecStressorsCPU, default is Undefined, optional
        cpu
    memory : ChaosMeshOrgV1alpha1StressChaosSpecStressorsMemory, default is Undefined, optional
        memory
    """


    cpu?: ChaosMeshOrgV1alpha1StressChaosSpecStressorsCPU

    memory?: ChaosMeshOrgV1alpha1StressChaosSpecStressorsMemory


schema ChaosMeshOrgV1alpha1StressChaosSpecStressorsCPU:
    r"""
    CPUStressor stresses CPU out

    Attributes
    ----------
    load : int, default is Undefined, optional
        Load specifies P percent loading per CPU worker. 0 is effectively a sleep (no load) and 100
        is full loading.
    options : [str], default is Undefined, optional
        extend stress-ng options
    workers : int, default is Undefined, required
        Workers specifies N workers to apply the stressor.
        Maximum 8192 workers can run by stress-ng
    """


    load?: int

    options?: [str]

    workers: int


    check:
        load <= 100 if load not in [None, Undefined]
        load >= 0 if load not in [None, Undefined]
        workers <= 8192


schema ChaosMeshOrgV1alpha1StressChaosSpecStressorsMemory:
    r"""
    MemoryStressor stresses virtual memory out

    Attributes
    ----------
    oomScoreAdj : int, default is Undefined, optional
        OOMScoreAdj sets the oom_score_adj of the stress process. See `man 5 proc` to know more
        about this option.
    options : [str], default is Undefined, optional
        extend stress-ng options
    size : str, default is Undefined, optional
        Size specifies N bytes consumed per vm worker, default is the total available memory.
        One can specify the size as % of total available memory or in units of B, KB/KiB,
        MB/MiB, GB/GiB, TB/TiB.
    workers : int, default is Undefined, required
        Workers specifies N workers to apply the stressor.
        Maximum 8192 workers can run by stress-ng
    """


    oomScoreAdj?: int = 0

    options?: [str]

    size?: str

    workers: int


    check:
        oomScoreAdj <= 1000 if oomScoreAdj not in [None, Undefined]
        oomScoreAdj >= -1000 if oomScoreAdj not in [None, Undefined]
        workers <= 8192


schema ChaosMeshOrgV1alpha1StressChaosStatus:
    r"""
    Most recently observed status of the time chaos experiment

    Attributes
    ----------
    conditions : [ChaosMeshOrgV1alpha1StressChaosStatusConditionsItems0], default is Undefined, optional
        Conditions represents the current global condition of the chaos
    experiment : ChaosMeshOrgV1alpha1StressChaosStatusExperiment, default is Undefined, required
        experiment
    instances : {str:ChaosMeshOrgV1alpha1StressChaosStatusInstancesAnon}, default is Undefined, optional
        Instances always specifies stressing instances
    """


    conditions?: [ChaosMeshOrgV1alpha1StressChaosStatusConditionsItems0]

    experiment: ChaosMeshOrgV1alpha1StressChaosStatusExperiment

    instances?: {str:ChaosMeshOrgV1alpha1StressChaosStatusInstancesAnon}


schema ChaosMeshOrgV1alpha1StressChaosStatusConditionsItems0:
    r"""
    chaos mesh org v1alpha1 stress chaos status conditions items0

    Attributes
    ----------
    reason : str, default is Undefined, optional
        reason
    status : str, default is Undefined, required
        status
    $type : str, default is Undefined, required
        type
    """


    reason?: str

    status: str

    $type: str


schema ChaosMeshOrgV1alpha1StressChaosStatusExperiment:
    r"""
    Experiment records the last experiment state.

    Attributes
    ----------
    containerRecords : [ChaosMeshOrgV1alpha1StressChaosStatusExperimentContainerRecordsItems0], default is Undefined, optional
        Records are used to track the running status
    desiredPhase : str, default is Undefined, optional
        desired phase
    """


    containerRecords?: [ChaosMeshOrgV1alpha1StressChaosStatusExperimentContainerRecordsItems0]

    desiredPhase?: "Run" | "Stop"


schema ChaosMeshOrgV1alpha1StressChaosStatusExperimentContainerRecordsItems0:
    r"""
    chaos mesh org v1alpha1 stress chaos status experiment container records items0

    Attributes
    ----------
    events : [ChaosMeshOrgV1alpha1StressChaosStatusExperimentContainerRecordsItems0EventsItems0], default is Undefined, optional
        Events are the essential details about the injections and recoveries
    id : str, default is Undefined, required
        id
    injectedCount : int, default is Undefined, required
        InjectedCount is a counter to record the sum of successful injections
    phase : str, default is Undefined, required
        phase
    recoveredCount : int, default is Undefined, required
        RecoveredCount is a counter to record the sum of successful recoveries
    selectorKey : str, default is Undefined, required
        selector key
    """


    events?: [ChaosMeshOrgV1alpha1StressChaosStatusExperimentContainerRecordsItems0EventsItems0]

    id: str

    injectedCount: int

    phase: str

    recoveredCount: int

    selectorKey: str


schema ChaosMeshOrgV1alpha1StressChaosStatusExperimentContainerRecordsItems0EventsItems0:
    r"""
    chaos mesh org v1alpha1 stress chaos status experiment container records items0 events items0

    Attributes
    ----------
    message : str, default is Undefined, optional
        Message is the detail message, e.g. the reason why we failed to inject the chaos
    operation : str, default is Undefined, required
        Operation represents the operation we are doing, when we crate this event
    timestamp : str, default is Undefined, required
        Timestamp is time when we create this event
    $type : str, default is Undefined, required
        Type means the stage of this event
    """


    message?: str

    operation: str

    timestamp: str

    $type: str


schema ChaosMeshOrgV1alpha1StressChaosStatusInstancesAnon:
    r"""
    StressInstance is an instance generates stresses

    Attributes
    ----------
    memoryStartTime : str, default is Undefined, optional
        MemoryStartTime specifies when the memStress starts
    memoryUid : str, default is Undefined, optional
        MemoryUID is the memStress identifier
    startTime : str, default is Undefined, optional
        StartTime specifies when the stress-ng starts
    uid : str, default is Undefined, optional
        UID is the stress-ng identifier
    """


    memoryStartTime?: str

    memoryUid?: str

    startTime?: str

    uid?: str


