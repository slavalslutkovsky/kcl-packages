"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema PhysicalMachineChaos:
    r"""
    PhysicalMachineChaos is the Schema for the physical machine chaos API

    Attributes
    ----------
    apiVersion : str, default is "chaos-mesh.org/v1alpha1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "PhysicalMachineChaos", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : ChaosMeshOrgV1alpha1PhysicalMachineChaosSpec, default is Undefined, required
        spec
    status : ChaosMeshOrgV1alpha1PhysicalMachineChaosStatus, default is Undefined, optional
        status
    """


    apiVersion: "chaos-mesh.org/v1alpha1" = "chaos-mesh.org/v1alpha1"

    kind: "PhysicalMachineChaos" = "PhysicalMachineChaos"

    metadata?: v1.ObjectMeta

    spec: ChaosMeshOrgV1alpha1PhysicalMachineChaosSpec

    status?: ChaosMeshOrgV1alpha1PhysicalMachineChaosStatus


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpec:
    r"""
    Spec defines the behavior of a physical machine chaos experiment

    Attributes
    ----------
    action : str, default is Undefined, required
        the subAction, generate automatically
    address : [str], default is Undefined, optional
        DEPRECATED: Use Selector instead.
        Only one of Address and Selector could be specified.
    clock : ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecClock, default is Undefined, optional
        clock
    "disk-fill" : ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecDiskFill, default is Undefined, optional
        disk fill
    "disk-read-payload" : ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecDiskReadPayload, default is Undefined, optional
        disk read payload
    "disk-write-payload" : ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecDiskWritePayload, default is Undefined, optional
        disk write payload
    duration : str, default is Undefined, optional
        Duration represents the duration of the chaos action
    "file-append" : ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecFileAppend, default is Undefined, optional
        file append
    "file-create" : ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecFileCreate, default is Undefined, optional
        file create
    "file-delete" : ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecFileDelete, default is Undefined, optional
        file delete
    "file-modify" : ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecFileModify, default is Undefined, optional
        file modify
    "file-rename" : ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecFileRename, default is Undefined, optional
        file rename
    "file-replace" : ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecFileReplace, default is Undefined, optional
        file replace
    "http-abort" : ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecHTTPAbort, default is Undefined, optional
        http abort
    "http-config" : ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecHTTPConfig, default is Undefined, optional
        http config
    "http-delay" : ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecHTTPDelay, default is Undefined, optional
        http delay
    "http-request" : ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecHTTPRequest, default is Undefined, optional
        http request
    "jvm-exception" : ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecJvmException, default is Undefined, optional
        jvm exception
    "jvm-gc" : ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecJvmGc, default is Undefined, optional
        jvm gc
    "jvm-latency" : ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecJvmLatency, default is Undefined, optional
        jvm latency
    "jvm-mysql" : ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecJvmMysql, default is Undefined, optional
        jvm mysql
    "jvm-return" : ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecJvmReturn, default is Undefined, optional
        jvm return
    "jvm-rule-data" : ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecJvmRuleData, default is Undefined, optional
        jvm rule data
    "jvm-stress" : ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecJvmStress, default is Undefined, optional
        jvm stress
    "kafka-fill" : ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecKafkaFill, default is Undefined, optional
        kafka fill
    "kafka-flood" : ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecKafkaFlood, default is Undefined, optional
        kafka flood
    "kafka-io" : ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecKafkaIo, default is Undefined, optional
        kafka io
    mode : str, default is Undefined, required
        Mode defines the mode to run chaos action.
        Supported mode: one / all / fixed / fixed-percent / random-max-percent
    "network-bandwidth" : ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecNetworkBandwidth, default is Undefined, optional
        network bandwidth
    "network-corrupt" : ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecNetworkCorrupt, default is Undefined, optional
        network corrupt
    "network-delay" : ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecNetworkDelay, default is Undefined, optional
        network delay
    "network-dns" : ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecNetworkDNS, default is Undefined, optional
        network dns
    "network-down" : ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecNetworkDown, default is Undefined, optional
        network down
    "network-duplicate" : ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecNetworkDuplicate, default is Undefined, optional
        network duplicate
    "network-flood" : ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecNetworkFlood, default is Undefined, optional
        network flood
    "network-loss" : ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecNetworkLoss, default is Undefined, optional
        network loss
    "network-partition" : ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecNetworkPartition, default is Undefined, optional
        network partition
    process : ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecProcess, default is Undefined, optional
        process
    "redis-cacheLimit" : ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecRedisCacheLimit, default is Undefined, optional
        redis cache limit
    "redis-expiration" : ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecRedisExpiration, default is Undefined, optional
        redis expiration
    "redis-penetration" : ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecRedisPenetration, default is Undefined, optional
        redis penetration
    "redis-restart" : ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecRedisRestart, default is Undefined, optional
        redis restart
    "redis-stop" : ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecRedisStop, default is Undefined, optional
        redis stop
    remoteCluster : str, default is Undefined, optional
        RemoteCluster represents the remote cluster where the chaos will be deployed
    selector : ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecSelector, default is Undefined, optional
        selector
    "stress-cpu" : ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecStressCPU, default is Undefined, optional
        stress cpu
    "stress-mem" : ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecStressMem, default is Undefined, optional
        stress mem
    uid : str, default is Undefined, optional
        the experiment ID
    user_defined : ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecUserDefined, default is Undefined, optional
        user defined
    value : str, default is Undefined, optional
        Value is required when the mode is set to `FixedMode` / `FixedPercentMode` / `RandomMaxPercentMode`.
        If `FixedMode`, provide an integer of physical machines to do chaos action.
        If `FixedPercentMode`, provide a number from 0-100 to specify the percent of physical machines the server can do chaos action.
        IF `RandomMaxPercentMode`,  provide a number from 0-100 to specify the max percent of pods to do chaos action
    vm : ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecVM, default is Undefined, optional
        vm
    """


    action: "stress-cpu" | "stress-mem" | "disk-read-payload" | "disk-write-payload" | "disk-fill" | "network-corrupt" | "network-duplicate" | "network-loss" | "network-delay" | "network-partition" | "network-dns" | "network-bandwidth" | "network-flood" | "network-down" | "process" | "jvm-exception" | "jvm-gc" | "jvm-latency" | "jvm-return" | "jvm-stress" | "jvm-rule-data" | "jvm-mysql" | "clock" | "redis-expiration" | "redis-penetration" | "redis-cacheLimit" | "redis-restart" | "redis-stop" | "kafka-fill" | "kafka-flood" | "kafka-io" | "file-create" | "file-modify" | "file-delete" | "file-rename" | "file-append" | "file-replace" | "vm" | "user_defined"

    address?: [str]

    clock?: ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecClock

    "disk-fill"?: ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecDiskFill

    "disk-read-payload"?: ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecDiskReadPayload

    "disk-write-payload"?: ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecDiskWritePayload

    duration?: str

    "file-append"?: ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecFileAppend

    "file-create"?: ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecFileCreate

    "file-delete"?: ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecFileDelete

    "file-modify"?: ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecFileModify

    "file-rename"?: ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecFileRename

    "file-replace"?: ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecFileReplace

    "http-abort"?: ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecHTTPAbort

    "http-config"?: ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecHTTPConfig

    "http-delay"?: ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecHTTPDelay

    "http-request"?: ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecHTTPRequest

    "jvm-exception"?: ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecJvmException

    "jvm-gc"?: ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecJvmGc

    "jvm-latency"?: ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecJvmLatency

    "jvm-mysql"?: ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecJvmMysql

    "jvm-return"?: ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecJvmReturn

    "jvm-rule-data"?: ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecJvmRuleData

    "jvm-stress"?: ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecJvmStress

    "kafka-fill"?: ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecKafkaFill

    "kafka-flood"?: ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecKafkaFlood

    "kafka-io"?: ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecKafkaIo

    mode: "one" | "all" | "fixed" | "fixed-percent" | "random-max-percent"

    "network-bandwidth"?: ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecNetworkBandwidth

    "network-corrupt"?: ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecNetworkCorrupt

    "network-delay"?: ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecNetworkDelay

    "network-dns"?: ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecNetworkDNS

    "network-down"?: ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecNetworkDown

    "network-duplicate"?: ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecNetworkDuplicate

    "network-flood"?: ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecNetworkFlood

    "network-loss"?: ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecNetworkLoss

    "network-partition"?: ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecNetworkPartition

    process?: ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecProcess

    "redis-cacheLimit"?: ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecRedisCacheLimit

    "redis-expiration"?: ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecRedisExpiration

    "redis-penetration"?: ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecRedisPenetration

    "redis-restart"?: ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecRedisRestart

    "redis-stop"?: ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecRedisStop

    remoteCluster?: str

    selector?: ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecSelector

    "stress-cpu"?: ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecStressCPU

    "stress-mem"?: ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecStressMem

    uid?: str

    user_defined?: ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecUserDefined

    value?: str

    vm?: ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecVM


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecClock:
    r"""
    chaos mesh org v1alpha1 physical machine chaos spec clock

    Attributes
    ----------
    "clock-ids-slice" : str, default is Undefined, optional
        the identifier of the particular clock on which to act.
        More clock description in linux kernel can be found in man page of clock_getres, clock_gettime, clock_settime.
        Muti clock ids should be split with ","
    pid : int, default is Undefined, optional
        the pid of target program.
    "time-offset" : str, default is Undefined, optional
        specifies the length of time offset.
    """


    "clock-ids-slice"?: str

    pid?: int

    "time-offset"?: str


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecDiskFill:
    r"""
    chaos mesh org v1alpha1 physical machine chaos spec disk fill

    Attributes
    ----------
    "fill-by-fallocate" : bool, default is Undefined, optional
        fill disk by fallocate
    path : str, default is Undefined, optional
        specifies the location to fill data in. if path not provided,
        payload will read/write from/into a temp file, temp file will be deleted after writing
    size : str, default is Undefined, optional
        specifies how many units of data will write into the file path. support unit: c=1, w=2, b=512, kB=1000,
        K=1024, MB=1000*1000, M=1024*1024, GB=1000*1000*1000, G=1024*1024*1024 BYTES. example : 1M | 512kB
    """


    "fill-by-fallocate"?: bool

    path?: str

    size?: str


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecDiskReadPayload:
    r"""
    chaos mesh org v1alpha1 physical machine chaos spec disk read payload

    Attributes
    ----------
    path : str, default is Undefined, optional
        specifies the location to fill data in. if path not provided,
        payload will read/write from/into a temp file, temp file will be deleted after writing
    "payload-process-num" : int, default is Undefined, optional
        specifies the number of process work on writing, default 1, only 1-255 is valid value
    size : str, default is Undefined, optional
        specifies how many units of data will write into the file path. support unit: c=1, w=2, b=512, kB=1000,
        K=1024, MB=1000*1000, M=1024*1024, GB=1000*1000*1000, G=1024*1024*1024 BYTES. example : 1M | 512kB
    """


    path?: str

    "payload-process-num"?: int

    size?: str


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecDiskWritePayload:
    r"""
    chaos mesh org v1alpha1 physical machine chaos spec disk write payload

    Attributes
    ----------
    path : str, default is Undefined, optional
        specifies the location to fill data in. if path not provided,
        payload will read/write from/into a temp file, temp file will be deleted after writing
    "payload-process-num" : int, default is Undefined, optional
        specifies the number of process work on writing, default 1, only 1-255 is valid value
    size : str, default is Undefined, optional
        specifies how many units of data will write into the file path. support unit: c=1, w=2, b=512, kB=1000,
        K=1024, MB=1000*1000, M=1024*1024, GB=1000*1000*1000, G=1024*1024*1024 BYTES. example : 1M | 512kB
    """


    path?: str

    "payload-process-num"?: int

    size?: str


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecFileAppend:
    r"""
    chaos mesh org v1alpha1 physical machine chaos spec file append

    Attributes
    ----------
    count : int, default is Undefined, optional
        Count is the number of times to append the data.
    data : str, default is Undefined, optional
        Data is the data for append.
    "file-name" : str, default is Undefined, optional
        FileName is the name of the file to be created, modified, deleted, renamed, or appended.
    """


    count?: int

    data?: str

    "file-name"?: str


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecFileCreate:
    r"""
    chaos mesh org v1alpha1 physical machine chaos spec file create

    Attributes
    ----------
    "dir-name" : str, default is Undefined, optional
        DirName is the directory name to create or delete.
    "file-name" : str, default is Undefined, optional
        FileName is the name of the file to be created, modified, deleted, renamed, or appended.
    """


    "dir-name"?: str

    "file-name"?: str


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecFileDelete:
    r"""
    chaos mesh org v1alpha1 physical machine chaos spec file delete

    Attributes
    ----------
    "dir-name" : str, default is Undefined, optional
        DirName is the directory name to create or delete.
    "file-name" : str, default is Undefined, optional
        FileName is the name of the file to be created, modified, deleted, renamed, or appended.
    """


    "dir-name"?: str

    "file-name"?: str


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecFileModify:
    r"""
    chaos mesh org v1alpha1 physical machine chaos spec file modify

    Attributes
    ----------
    "file-name" : str, default is Undefined, optional
        FileName is the name of the file to be created, modified, deleted, renamed, or appended.
    privilege : int, default is Undefined, optional
        Privilege is the file privilege to be set.
    """


    "file-name"?: str

    privilege?: int


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecFileRename:
    r"""
    chaos mesh org v1alpha1 physical machine chaos spec file rename

    Attributes
    ----------
    "dest-file" : str, default is Undefined, optional
        DestFile is the name to be renamed.
    "source-file" : str, default is Undefined, optional
        SourceFile is the name need to be renamed.
    """


    "dest-file"?: str

    "source-file"?: str


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecFileReplace:
    r"""
    chaos mesh org v1alpha1 physical machine chaos spec file replace

    Attributes
    ----------
    "dest-string" : str, default is Undefined, optional
        DestStr is the destination string of the file.
    "file-name" : str, default is Undefined, optional
        FileName is the name of the file to be created, modified, deleted, renamed, or appended.
    line : int, default is Undefined, optional
        Line is the line number of the file to be replaced.
    "origin-string" : str, default is Undefined, optional
        OriginStr is the origin string of the file.
    """


    "dest-string"?: str

    "file-name"?: str

    line?: int

    "origin-string"?: str


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecHTTPAbort:
    r"""
    chaos mesh org v1alpha1 physical machine chaos spec HTTP abort

    Attributes
    ----------
    code : str, default is Undefined, optional
        Code is a rule to select target by http status code in response
    method : str, default is Undefined, optional
        HTTP method
    path : str, default is Undefined, optional
        Match path of Uri with wildcard matches
    port : int, default is Undefined, optional
        The TCP port that the target service listens on
    proxy_ports : [int], default is Undefined, required
        Composed with one of the port of HTTP connection, we will only attack HTTP connection with port inside proxy_ports
    target : str, default is Undefined, required
        HTTP target: Request or Response
    """


    code?: str

    method?: str

    path?: str

    port?: int

    proxy_ports: [int]

    target: str


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecHTTPConfig:
    r"""
    chaos mesh org v1alpha1 physical machine chaos spec HTTP config

    Attributes
    ----------
    file_path : str, default is Undefined, optional
        The config file path
    """


    file_path?: str


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecHTTPDelay:
    r"""
    chaos mesh org v1alpha1 physical machine chaos spec HTTP delay

    Attributes
    ----------
    code : str, default is Undefined, optional
        Code is a rule to select target by http status code in response
    delay : str, default is Undefined, required
        Delay represents the delay of the target request/response
    method : str, default is Undefined, optional
        HTTP method
    path : str, default is Undefined, optional
        Match path of Uri with wildcard matches
    port : int, default is Undefined, optional
        The TCP port that the target service listens on
    proxy_ports : [int], default is Undefined, required
        Composed with one of the port of HTTP connection, we will only attack HTTP connection with port inside proxy_ports
    target : str, default is Undefined, required
        HTTP target: Request or Response
    """


    code?: str

    delay: str

    method?: str

    path?: str

    port?: int

    proxy_ports: [int]

    target: str


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecHTTPRequest:
    r"""
    used for HTTP request, now only support GET

    Attributes
    ----------
    count : int, default is Undefined, optional
        The number of requests to send
    "enable-conn-pool" : bool, default is Undefined, optional
        Enable connection pool
    url : str, default is Undefined, optional
        Request to send"
    """


    count?: int

    "enable-conn-pool"?: bool

    url?: str


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecJvmException:
    r"""
    chaos mesh org v1alpha1 physical machine chaos spec jvm exception

    Attributes
    ----------
    class : str, default is Undefined, optional
        Java class
    exception : str, default is Undefined, optional
        the exception which needs to throw for action `exception`
    method : str, default is Undefined, optional
        the method in Java class
    pid : int, default is Undefined, optional
        the pid of Java process which needs to attach
    port : int, default is Undefined, optional
        the port of agent server, default 9277
    """


    class?: str

    exception?: str

    method?: str

    pid?: int

    port?: int


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecJvmGc:
    r"""
    chaos mesh org v1alpha1 physical machine chaos spec jvm gc

    Attributes
    ----------
    pid : int, default is Undefined, optional
        the pid of Java process which needs to attach
    port : int, default is Undefined, optional
        the port of agent server, default 9277
    """


    pid?: int

    port?: int


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecJvmLatency:
    r"""
    chaos mesh org v1alpha1 physical machine chaos spec jvm latency

    Attributes
    ----------
    class : str, default is Undefined, optional
        Java class
    latency : int, default is Undefined, optional
        the latency duration for action 'latency', unit ms
    method : str, default is Undefined, optional
        the method in Java class
    pid : int, default is Undefined, optional
        the pid of Java process which needs to attach
    port : int, default is Undefined, optional
        the port of agent server, default 9277
    """


    class?: str

    latency?: int

    method?: str

    pid?: int

    port?: int


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecJvmMysql:
    r"""
    chaos mesh org v1alpha1 physical machine chaos spec jvm mysql

    Attributes
    ----------
    database : str, default is Undefined, optional
        the match database
        default value is "", means match all database
    exception : str, default is Undefined, optional
        The exception which needs to throw for action `exception`
        or the exception message needs to throw in action `mysql`
    latency : int, default is Undefined, optional
        The latency duration for action 'latency'
        or the latency duration in action `mysql`
    mysqlConnectorVersion : str, default is Undefined, optional
        the version of mysql-connector-java, only support 5.X.X(set to "5") and 8.X.X(set to "8") now
    pid : int, default is Undefined, optional
        the pid of Java process which needs to attach
    port : int, default is Undefined, optional
        the port of agent server, default 9277
    sqlType : str, default is Undefined, optional
        the match sql type
        default value is "", means match all SQL type.
        The value can be 'select', 'insert', 'update', 'delete', 'replace'.
    table : str, default is Undefined, optional
        the match table
        default value is "", means match all table
    """


    database?: str

    exception?: str

    latency?: int

    mysqlConnectorVersion?: str

    pid?: int

    port?: int

    sqlType?: str

    table?: str


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecJvmReturn:
    r"""
    chaos mesh org v1alpha1 physical machine chaos spec jvm return

    Attributes
    ----------
    class : str, default is Undefined, optional
        Java class
    method : str, default is Undefined, optional
        the method in Java class
    pid : int, default is Undefined, optional
        the pid of Java process which needs to attach
    port : int, default is Undefined, optional
        the port of agent server, default 9277
    value : str, default is Undefined, optional
        the return value for action 'return'
    """


    class?: str

    method?: str

    pid?: int

    port?: int

    value?: str


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecJvmRuleData:
    r"""
    chaos mesh org v1alpha1 physical machine chaos spec jvm rule data

    Attributes
    ----------
    pid : int, default is Undefined, optional
        the pid of Java process which needs to attach
    port : int, default is Undefined, optional
        the port of agent server, default 9277
    "rule-data" : str, default is Undefined, optional
        RuleData used to save the rule file's data, will use it when recover
    """


    pid?: int

    port?: int

    "rule-data"?: str


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecJvmStress:
    r"""
    chaos mesh org v1alpha1 physical machine chaos spec jvm stress

    Attributes
    ----------
    "cpu-count" : int, default is Undefined, optional
        the CPU core number need to use, only set it when action is stress
    "mem-type" : str, default is Undefined, optional
        the memory type need to locate, only set it when action is stress, the value can be 'stack' or 'heap'
    pid : int, default is Undefined, optional
        the pid of Java process which needs to attach
    port : int, default is Undefined, optional
        the port of agent server, default 9277
    """


    "cpu-count"?: int

    "mem-type"?: str

    pid?: int

    port?: int


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecKafkaFill:
    r"""
    chaos mesh org v1alpha1 physical machine chaos spec kafka fill

    Attributes
    ----------
    host : str, default is Undefined, optional
        The host of kafka server
    maxBytes : int, default is Undefined, optional
        The max bytes to fill
    messageSize : int, default is Undefined, optional
        The size of each message
    password : str, default is Undefined, optional
        The password of kafka client
    port : int, default is Undefined, optional
        The port of kafka server
    reloadCommand : str, default is Undefined, optional
        The command to reload kafka config
    topic : str, default is Undefined, optional
        The topic to attack
    username : str, default is Undefined, optional
        The username of kafka client
    """


    host?: str

    maxBytes?: int

    messageSize?: int

    password?: str

    port?: int

    reloadCommand?: str

    topic?: str

    username?: str


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecKafkaFlood:
    r"""
    chaos mesh org v1alpha1 physical machine chaos spec kafka flood

    Attributes
    ----------
    host : str, default is Undefined, optional
        The host of kafka server
    messageSize : int, default is Undefined, optional
        The size of each message
    password : str, default is Undefined, optional
        The password of kafka client
    port : int, default is Undefined, optional
        The port of kafka server
    threads : int, default is Undefined, optional
        The number of worker threads
    topic : str, default is Undefined, optional
        The topic to attack
    username : str, default is Undefined, optional
        The username of kafka client
    """


    host?: str

    messageSize?: int

    password?: str

    port?: int

    threads?: int

    topic?: str

    username?: str


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecKafkaIo:
    r"""
    chaos mesh org v1alpha1 physical machine chaos spec kafka io

    Attributes
    ----------
    configFile : str, default is Undefined, optional
        The path of server config
    nonReadable : bool, default is Undefined, optional
        Make kafka cluster non-readable
    nonWritable : bool, default is Undefined, optional
        Make kafka cluster non-writable
    topic : str, default is Undefined, optional
        The topic to attack
    """


    configFile?: str

    nonReadable?: bool

    nonWritable?: bool

    topic?: str


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecNetworkBandwidth:
    r"""
    chaos mesh org v1alpha1 physical machine chaos spec network bandwidth

    Attributes
    ----------
    buffer : int, default is Undefined, required
        buffer
    device : str, default is Undefined, optional
        device
    hostname : str, default is Undefined, optional
        hostname
    "ip-address" : str, default is Undefined, optional
        ip address
    limit : int, default is Undefined, required
        limit
    minburst : int, default is Undefined, optional
        minburst
    peakrate : int, default is Undefined, optional
        peakrate
    rate : str, default is Undefined, required
        rate
    """


    buffer: int

    device?: str

    hostname?: str

    "ip-address"?: str

    limit: int

    minburst?: int

    peakrate?: int

    rate: str


    check:
        buffer >= 1
        limit >= 1


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecNetworkCorrupt:
    r"""
    chaos mesh org v1alpha1 physical machine chaos spec network corrupt

    Attributes
    ----------
    correlation : str, default is Undefined, optional
        correlation is percentage (10 is 10%)
    device : str, default is Undefined, optional
        the network interface to impact
    "egress-port" : str, default is Undefined, optional
        only impact egress traffic to these destination ports, use a ',' to separate or to indicate the range, such as 80, 8001:8010.
        it can only be used in conjunction with -p tcp or -p udp
    hostname : str, default is Undefined, optional
        only impact traffic to these hostnames
    "ip-address" : str, default is Undefined, optional
        only impact egress traffic to these IP addresses
    "ip-protocol" : str, default is Undefined, optional
        only impact traffic using this IP protocol, supported: tcp, udp, icmp, all
    percent : str, default is Undefined, optional
        percentage of packets to corrupt (10 is 10%)
    "source-port" : str, default is Undefined, optional
        only impact egress traffic from these source ports, use a ',' to separate or to indicate the range, such as 80, 8001:8010.
        it can only be used in conjunction with -p tcp or -p udp
    """


    correlation?: str

    device?: str

    "egress-port"?: str

    hostname?: str

    "ip-address"?: str

    "ip-protocol"?: str

    percent?: str

    "source-port"?: str


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecNetworkDNS:
    r"""
    chaos mesh org v1alpha1 physical machine chaos spec network DNS

    Attributes
    ----------
    "dns-domain-name" : str, default is Undefined, optional
        map this host to specified IP
    "dns-ip" : str, default is Undefined, optional
        map specified host to this IP address
    "dns-server" : str, default is Undefined, optional
        update the DNS server in /etc/resolv.conf with this value
    """


    "dns-domain-name"?: str

    "dns-ip"?: str

    "dns-server"?: str


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecNetworkDelay:
    r"""
    chaos mesh org v1alpha1 physical machine chaos spec network delay

    Attributes
    ----------
    "accept-tcp-flags" : str, default is Undefined, optional
        only the packet which match the tcp flag can be accepted, others will be dropped.
        only set when the IPProtocol is tcp, used for partition.
    correlation : str, default is Undefined, optional
        correlation is percentage (10 is 10%)
    device : str, default is Undefined, optional
        the network interface to impact
    "egress-port" : str, default is Undefined, optional
        only impact egress traffic to these destination ports, use a ',' to separate or to indicate the range, such as 80, 8001:8010.
        it can only be used in conjunction with -p tcp or -p udp
    hostname : str, default is Undefined, optional
        only impact traffic to these hostnames
    "ip-address" : str, default is Undefined, optional
        only impact egress traffic to these IP addresses
    "ip-protocol" : str, default is Undefined, optional
        only impact traffic using this IP protocol, supported: tcp, udp, icmp, all
    jitter : str, default is Undefined, optional
        jitter time, time units: ns, us (or µs), ms, s, m, h.
    latency : str, default is Undefined, optional
        delay egress time, time units: ns, us (or µs), ms, s, m, h.
    "source-port" : str, default is Undefined, optional
        only impact egress traffic from these source ports, use a ',' to separate or to indicate the range, such as 80, 8001:8010.
        it can only be used in conjunction with -p tcp or -p udp
    """


    "accept-tcp-flags"?: str

    correlation?: str

    device?: str

    "egress-port"?: str

    hostname?: str

    "ip-address"?: str

    "ip-protocol"?: str

    jitter?: str

    latency?: str

    "source-port"?: str


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecNetworkDown:
    r"""
    chaos mesh org v1alpha1 physical machine chaos spec network down

    Attributes
    ----------
    device : str, default is Undefined, optional
        The network interface to impact
    duration : str, default is Undefined, optional
        NIC down time, time units: ns, us (or µs), ms, s, m, h.
    """


    device?: str

    duration?: str


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecNetworkDuplicate:
    r"""
    chaos mesh org v1alpha1 physical machine chaos spec network duplicate

    Attributes
    ----------
    correlation : str, default is Undefined, optional
        correlation is percentage (10 is 10%)
    device : str, default is Undefined, optional
        the network interface to impact
    "egress-port" : str, default is Undefined, optional
        only impact egress traffic to these destination ports, use a ',' to separate or to indicate the range, such as 80, 8001:8010.
        it can only be used in conjunction with -p tcp or -p udp
    hostname : str, default is Undefined, optional
        only impact traffic to these hostnames
    "ip-address" : str, default is Undefined, optional
        only impact egress traffic to these IP addresses
    "ip-protocol" : str, default is Undefined, optional
        only impact traffic using this IP protocol, supported: tcp, udp, icmp, all
    percent : str, default is Undefined, optional
        percentage of packets to duplicate (10 is 10%)
    "source-port" : str, default is Undefined, optional
        only impact egress traffic from these source ports, use a ',' to separate or to indicate the range, such as 80, 8001:8010.
        it can only be used in conjunction with -p tcp or -p udp
    """


    correlation?: str

    device?: str

    "egress-port"?: str

    hostname?: str

    "ip-address"?: str

    "ip-protocol"?: str

    percent?: str

    "source-port"?: str


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecNetworkFlood:
    r"""
    chaos mesh org v1alpha1 physical machine chaos spec network flood

    Attributes
    ----------
    duration : str, default is Undefined, required
        The number of seconds to run the iperf test
    "ip-address" : str, default is Undefined, optional
        Generate traffic to this IP address
    parallel : int, default is Undefined, optional
        The number of iperf parallel client threads to run
    port : str, default is Undefined, optional
        Generate traffic to this port on the IP address
    rate : str, default is Undefined, required
        The speed of network traffic, allows bps, kbps, mbps, gbps, tbps unit. bps means bytes per second
    """


    duration: str

    "ip-address"?: str

    parallel?: int

    port?: str

    rate: str


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecNetworkLoss:
    r"""
    chaos mesh org v1alpha1 physical machine chaos spec network loss

    Attributes
    ----------
    correlation : str, default is Undefined, optional
        correlation is percentage (10 is 10%)
    device : str, default is Undefined, optional
        the network interface to impact
    "egress-port" : str, default is Undefined, optional
        only impact egress traffic to these destination ports, use a ',' to separate or to indicate the range, such as 80, 8001:8010.
        it can only be used in conjunction with -p tcp or -p udp
    hostname : str, default is Undefined, optional
        only impact traffic to these hostnames
    "ip-address" : str, default is Undefined, optional
        only impact egress traffic to these IP addresses
    "ip-protocol" : str, default is Undefined, optional
        only impact traffic using this IP protocol, supported: tcp, udp, icmp, all
    percent : str, default is Undefined, optional
        percentage of packets to loss (10 is 10%)
    "source-port" : str, default is Undefined, optional
        only impact egress traffic from these source ports, use a ',' to separate or to indicate the range, such as 80, 8001:8010.
        it can only be used in conjunction with -p tcp or -p udp
    """


    correlation?: str

    device?: str

    "egress-port"?: str

    hostname?: str

    "ip-address"?: str

    "ip-protocol"?: str

    percent?: str

    "source-port"?: str


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecNetworkPartition:
    r"""
    chaos mesh org v1alpha1 physical machine chaos spec network partition

    Attributes
    ----------
    "accept-tcp-flags" : str, default is Undefined, optional
        only the packet which match the tcp flag can be accepted, others will be dropped.
        only set when the IPProtocol is tcp, used for partition.
    device : str, default is Undefined, optional
        the network interface to impact
    direction : str, default is Undefined, optional
        specifies the partition direction, values can be 'from', 'to'.
        'from' means packets coming from the 'IPAddress' or 'Hostname' and going to your server,
        'to' means packets originating from your server and going to the 'IPAddress' or 'Hostname'.
    hostname : str, default is Undefined, optional
        only impact traffic to these hostnames
    "ip-address" : str, default is Undefined, optional
        only impact egress traffic to these IP addresses
    "ip-protocol" : str, default is Undefined, optional
        only impact egress traffic to these IP addresses
    """


    "accept-tcp-flags"?: str

    device?: str

    direction?: str

    hostname?: str

    "ip-address"?: str

    "ip-protocol"?: str


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecProcess:
    r"""
    chaos mesh org v1alpha1 physical machine chaos spec process

    Attributes
    ----------
    process : str, default is Undefined, optional
        the process name or the process ID
    recoverCmd : str, default is Undefined, optional
        the command to be run when recovering experiment
    signal : int, default is Undefined, optional
        the signal number to send
    """


    process?: str

    recoverCmd?: str

    signal?: int


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecRedisCacheLimit:
    r"""
    chaos mesh org v1alpha1 physical machine chaos spec redis cache limit

    Attributes
    ----------
    addr : str, default is Undefined, optional
        The adress of Redis server
    cacheSize : str, default is Undefined, optional
        The size of `maxmemory`
    password : str, default is Undefined, optional
        The password of Redis server
    percent : str, default is Undefined, optional
        Specifies maxmemory as a percentage of the original value
    """


    addr?: str

    cacheSize?: str

    password?: str

    percent?: str


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecRedisExpiration:
    r"""
    chaos mesh org v1alpha1 physical machine chaos spec redis expiration

    Attributes
    ----------
    addr : str, default is Undefined, optional
        The adress of Redis server
    expiration : str, default is Undefined, optional
        The expiration of the keys
    key : str, default is Undefined, optional
        The keys to be expired
    option : str, default is Undefined, optional
        Additional options for `expiration`
    password : str, default is Undefined, optional
        The password of Redis server
    """


    addr?: str

    expiration?: str

    key?: str

    option?: str

    password?: str


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecRedisPenetration:
    r"""
    chaos mesh org v1alpha1 physical machine chaos spec redis penetration

    Attributes
    ----------
    addr : str, default is Undefined, optional
        The adress of Redis server
    password : str, default is Undefined, optional
        The password of Redis server
    requestNum : int, default is Undefined, optional
        The number of requests to be sent
    """


    addr?: str

    password?: str

    requestNum?: int


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecRedisRestart:
    r"""
    chaos mesh org v1alpha1 physical machine chaos spec redis restart

    Attributes
    ----------
    addr : str, default is Undefined, optional
        The adress of Redis server
    conf : str, default is Undefined, optional
        The path of Sentinel conf
    flushConfig : bool, default is Undefined, optional
        The control flag determines whether to flush config
    password : str, default is Undefined, optional
        The password of Redis server
    redisPath : bool, default is Undefined, optional
        The path of `redis-server` command-line tool
    """


    addr?: str

    conf?: str

    flushConfig?: bool

    password?: str

    redisPath?: bool


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecRedisStop:
    r"""
    chaos mesh org v1alpha1 physical machine chaos spec redis stop

    Attributes
    ----------
    addr : str, default is Undefined, optional
        The adress of Redis server
    conf : str, default is Undefined, optional
        The path of Sentinel conf
    flushConfig : bool, default is Undefined, optional
        The control flag determines whether to flush config
    password : str, default is Undefined, optional
        The password of Redis server
    redisPath : bool, default is Undefined, optional
        The path of `redis-server` command-line tool
    """


    addr?: str

    conf?: str

    flushConfig?: bool

    password?: str

    redisPath?: bool


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecSelector:
    r"""
    Selector is used to select physical machines that are used to inject chaos action.

    Attributes
    ----------
    annotationSelectors : {str:str}, default is Undefined, optional
        Map of string keys and values that can be used to select objects.
        A selector based on annotations.
    expressionSelectors : [ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecSelectorExpressionSelectorsItems0], default is Undefined, optional
        a slice of label selector expressions that can be used to select objects.
        A list of selectors based on set-based label expressions.
    fieldSelectors : {str:str}, default is Undefined, optional
        Map of string keys and values that can be used to select objects.
        A selector based on fields.
    labelSelectors : {str:str}, default is Undefined, optional
        Map of string keys and values that can be used to select objects.
        A selector based on labels.
    namespaces : [str], default is Undefined, optional
        Namespaces is a set of namespace to which objects belong.
    physicalMachines : {str:[str]}, default is Undefined, optional
        PhysicalMachines is a map of string keys and a set values that used to select physical machines.
        The key defines the namespace which physical machine belong,
        and each value is a set of physical machine names.
    """


    annotationSelectors?: {str:str}

    expressionSelectors?: [ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecSelectorExpressionSelectorsItems0]

    fieldSelectors?: {str:str}

    labelSelectors?: {str:str}

    namespaces?: [str]

    physicalMachines?: {str:[str]}


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecSelectorExpressionSelectorsItems0:
    r"""
    A label selector requirement is a selector that contains values, a key, and an operator that
    relates the key and values.

    Attributes
    ----------
    key : str, default is Undefined, required
        key is the label key that the selector applies to.
    operator : str, default is Undefined, required
        operator represents a key's relationship to a set of values.
        Valid operators are In, NotIn, Exists and DoesNotExist.
    values : [str], default is Undefined, optional
        values is an array of string values. If the operator is In or NotIn,
        the values array must be non-empty. If the operator is Exists or DoesNotExist,
        the values array must be empty. This array is replaced during a strategic
        merge patch.
    """


    key: str

    operator: str

    values?: [str]


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecStressCPU:
    r"""
    chaos mesh org v1alpha1 physical machine chaos spec stress CPU

    Attributes
    ----------
    load : int, default is Undefined, optional
        specifies P percent loading per CPU worker. 0 is effectively a sleep (no load) and 100 is full loading.
    options : [str], default is Undefined, optional
        extend stress-ng options
    workers : int, default is Undefined, optional
        specifies N workers to apply the stressor.
    """


    load?: int

    options?: [str]

    workers?: int


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecStressMem:
    r"""
    chaos mesh org v1alpha1 physical machine chaos spec stress mem

    Attributes
    ----------
    options : [str], default is Undefined, optional
        extend stress-ng options
    size : str, default is Undefined, optional
        specifies N bytes consumed per vm worker, default is the total available memory.
        One can specify the size as % of total available memory or in units of B, KB/KiB, MB/MiB, GB/GiB, TB/TiB..
    """


    options?: [str]

    size?: str


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecUserDefined:
    r"""
    chaos mesh org v1alpha1 physical machine chaos spec user defined

    Attributes
    ----------
    attackCmd : str, default is Undefined, optional
        The command to be executed when attack
    recoverCmd : str, default is Undefined, optional
        The command to be executed when recover
    """


    attackCmd?: str

    recoverCmd?: str


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosSpecVM:
    r"""
    chaos mesh org v1alpha1 physical machine chaos spec VM

    Attributes
    ----------
    "vm-name" : str, default is Undefined, optional
        The name of the VM to be injected
    """


    "vm-name"?: str


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosStatus:
    r"""
    Most recently observed status of the chaos experiment

    Attributes
    ----------
    conditions : [ChaosMeshOrgV1alpha1PhysicalMachineChaosStatusConditionsItems0], default is Undefined, optional
        Conditions represents the current global condition of the chaos
    experiment : ChaosMeshOrgV1alpha1PhysicalMachineChaosStatusExperiment, default is Undefined, required
        experiment
    """


    conditions?: [ChaosMeshOrgV1alpha1PhysicalMachineChaosStatusConditionsItems0]

    experiment: ChaosMeshOrgV1alpha1PhysicalMachineChaosStatusExperiment


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosStatusConditionsItems0:
    r"""
    chaos mesh org v1alpha1 physical machine chaos status conditions items0

    Attributes
    ----------
    reason : str, default is Undefined, optional
        reason
    status : str, default is Undefined, required
        status
    $type : str, default is Undefined, required
        type
    """


    reason?: str

    status: str

    $type: str


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosStatusExperiment:
    r"""
    Experiment records the last experiment state.

    Attributes
    ----------
    containerRecords : [ChaosMeshOrgV1alpha1PhysicalMachineChaosStatusExperimentContainerRecordsItems0], default is Undefined, optional
        Records are used to track the running status
    desiredPhase : str, default is Undefined, optional
        desired phase
    """


    containerRecords?: [ChaosMeshOrgV1alpha1PhysicalMachineChaosStatusExperimentContainerRecordsItems0]

    desiredPhase?: "Run" | "Stop"


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosStatusExperimentContainerRecordsItems0:
    r"""
    chaos mesh org v1alpha1 physical machine chaos status experiment container records items0

    Attributes
    ----------
    events : [ChaosMeshOrgV1alpha1PhysicalMachineChaosStatusExperimentContainerRecordsItems0EventsItems0], default is Undefined, optional
        Events are the essential details about the injections and recoveries
    id : str, default is Undefined, required
        id
    injectedCount : int, default is Undefined, required
        InjectedCount is a counter to record the sum of successful injections
    phase : str, default is Undefined, required
        phase
    recoveredCount : int, default is Undefined, required
        RecoveredCount is a counter to record the sum of successful recoveries
    selectorKey : str, default is Undefined, required
        selector key
    """


    events?: [ChaosMeshOrgV1alpha1PhysicalMachineChaosStatusExperimentContainerRecordsItems0EventsItems0]

    id: str

    injectedCount: int

    phase: str

    recoveredCount: int

    selectorKey: str


schema ChaosMeshOrgV1alpha1PhysicalMachineChaosStatusExperimentContainerRecordsItems0EventsItems0:
    r"""
    chaos mesh org v1alpha1 physical machine chaos status experiment container records items0 events items0

    Attributes
    ----------
    message : str, default is Undefined, optional
        Message is the detail message, e.g. the reason why we failed to inject the chaos
    operation : str, default is Undefined, required
        Operation represents the operation we are doing, when we crate this event
    timestamp : str, default is Undefined, required
        Timestamp is time when we create this event
    $type : str, default is Undefined, required
        Type means the stage of this event
    """


    message?: str

    operation: str

    timestamp: str

    $type: str


