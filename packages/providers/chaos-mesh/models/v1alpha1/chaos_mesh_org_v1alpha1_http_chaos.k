"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""
import k8s.apimachinery.pkg.apis.meta.v1


schema HTTPChaos:
    r"""
    HTTPChaos is the Schema for the HTTPchaos API

    Attributes
    ----------
    apiVersion : str, default is "chaos-mesh.org/v1alpha1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "HTTPChaos", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : ChaosMeshOrgV1alpha1HTTPChaosSpec, default is Undefined, optional
        spec
    status : ChaosMeshOrgV1alpha1HTTPChaosStatus, default is Undefined, optional
        status
    """


    apiVersion: "chaos-mesh.org/v1alpha1" = "chaos-mesh.org/v1alpha1"

    kind: "HTTPChaos" = "HTTPChaos"

    metadata?: v1.ObjectMeta

    spec?: ChaosMeshOrgV1alpha1HTTPChaosSpec

    status?: ChaosMeshOrgV1alpha1HTTPChaosStatus


schema ChaosMeshOrgV1alpha1HTTPChaosSpec:
    r"""
    chaos mesh org v1alpha1 HTTP chaos spec

    Attributes
    ----------
    abort : bool, default is Undefined, optional
        Abort is a rule to abort a http session.
    code : int, default is Undefined, optional
        Code is a rule to select target by http status code in response.
    delay : str, default is Undefined, optional
        Delay represents the delay of the target request/response.
        A duration string is a possibly unsigned sequence of
        decimal numbers, each with optional fraction and a unit suffix,
        such as "300ms", "2h45m".
        Valid time units are "ns", "us" (or "µs"), "ms", "s", "m", "h".
    duration : str, default is Undefined, optional
        Duration represents the duration of the chaos action.
    method : str, default is Undefined, optional
        Method is a rule to select target by http method in request.
    mode : str, default is Undefined, required
        Mode defines the mode to run chaos action.
        Supported mode: one / all / fixed / fixed-percent / random-max-percent
    patch : ChaosMeshOrgV1alpha1HTTPChaosSpecPatch, default is Undefined, optional
        patch
    path : str, default is Undefined, optional
        Path is a rule to select target by uri path in http request.
    port : int, default is Undefined, optional
        Port represents the target port to be proxy of.
    remoteCluster : str, default is Undefined, optional
        RemoteCluster represents the remote cluster where the chaos will be deployed
    replace : ChaosMeshOrgV1alpha1HTTPChaosSpecReplace, default is Undefined, optional
        replace
    request_headers : {str:str}, default is Undefined, optional
        RequestHeaders is a rule to select target by http headers in request.
        The key-value pairs represent header name and header value pairs.
    response_headers : {str:str}, default is Undefined, optional
        ResponseHeaders is a rule to select target by http headers in response.
        The key-value pairs represent header name and header value pairs.
    selector : ChaosMeshOrgV1alpha1HTTPChaosSpecSelector, default is Undefined, required
        selector
    target : str, default is Undefined, required
        Target is the object to be selected and injected.
    tls : ChaosMeshOrgV1alpha1HTTPChaosSpecTLS, default is Undefined, optional
        tls
    value : str, default is Undefined, optional
        Value is required when the mode is set to `FixedMode` / `FixedPercentMode` / `RandomMaxPercentMode`.
        If `FixedMode`, provide an integer of pods to do chaos action.
        If `FixedPercentMode`, provide a number from 0-100 to specify the percent of pods the server can do chaos action.
        IF `RandomMaxPercentMode`,  provide a number from 0-100 to specify the max percent of pods to do chaos action
    """


    abort?: bool

    code?: int

    delay?: str

    duration?: str

    method?: str

    mode: "one" | "all" | "fixed" | "fixed-percent" | "random-max-percent"

    patch?: ChaosMeshOrgV1alpha1HTTPChaosSpecPatch

    path?: str

    port?: int

    remoteCluster?: str

    replace?: ChaosMeshOrgV1alpha1HTTPChaosSpecReplace

    request_headers?: {str:str}

    response_headers?: {str:str}

    selector: ChaosMeshOrgV1alpha1HTTPChaosSpecSelector

    target: "Request" | "Response"

    tls?: ChaosMeshOrgV1alpha1HTTPChaosSpecTLS

    value?: str


schema ChaosMeshOrgV1alpha1HTTPChaosSpecPatch:
    r"""
    Patch is a rule to patch some contents in target.

    Attributes
    ----------
    body : ChaosMeshOrgV1alpha1HTTPChaosSpecPatchBody, default is Undefined, optional
        body
    headers : [[str]], default is Undefined, optional
        Headers is a rule to append http headers of target.
        For example: `[["Set-Cookie", "<one cookie>"], ["Set-Cookie", "<another cookie>"]]`.
    queries : [[str]], default is Undefined, optional
        Queries is a rule to append uri queries of target(Request only).
        For example: `[["foo", "bar"], ["foo", "unknown"]]`.
    """


    body?: ChaosMeshOrgV1alpha1HTTPChaosSpecPatchBody

    headers?: [[str]]

    queries?: [[str]]


schema ChaosMeshOrgV1alpha1HTTPChaosSpecPatchBody:
    r"""
    Body is a rule to patch message body of target.

    Attributes
    ----------
    $type : str, default is Undefined, required
        Type represents the patch type, only support `JSON` as [merge patch json](https://tools.ietf.org/html/rfc7396) currently.
    value : str, default is Undefined, required
        Value is the patch contents.
    """


    $type: str

    value: str


schema ChaosMeshOrgV1alpha1HTTPChaosSpecReplace:
    r"""
    Replace is a rule to replace some contents in target.

    Attributes
    ----------
    body : str, default is Undefined, optional
        Body is a rule to replace http message body in target.
    code : int, default is Undefined, optional
        Code is a rule to replace http status code in response.
    headers : {str:str}, default is Undefined, optional
        Headers is a rule to replace http headers of target.
        The key-value pairs represent header name and header value pairs.
    method : str, default is Undefined, optional
        Method is a rule to replace http method in request.
    path : str, default is Undefined, optional
        Path is rule to to replace uri path in http request.
    queries : {str:str}, default is Undefined, optional
        Queries is a rule to replace uri queries in http request.
        For example, with value `{ "foo": "unknown" }`, the `/?foo=bar` will be altered to `/?foo=unknown`,
    """


    body?: str

    code?: int

    headers?: {str:str}

    method?: str

    path?: str

    queries?: {str:str}


schema ChaosMeshOrgV1alpha1HTTPChaosSpecSelector:
    r"""
    Selector is used to select pods that are used to inject chaos action.

    Attributes
    ----------
    annotationSelectors : {str:str}, default is Undefined, optional
        Map of string keys and values that can be used to select objects.
        A selector based on annotations.
    expressionSelectors : [ChaosMeshOrgV1alpha1HTTPChaosSpecSelectorExpressionSelectorsItems0], default is Undefined, optional
        a slice of label selector expressions that can be used to select objects.
        A list of selectors based on set-based label expressions.
    fieldSelectors : {str:str}, default is Undefined, optional
        Map of string keys and values that can be used to select objects.
        A selector based on fields.
    labelSelectors : {str:str}, default is Undefined, optional
        Map of string keys and values that can be used to select objects.
        A selector based on labels.
    namespaces : [str], default is Undefined, optional
        Namespaces is a set of namespace to which objects belong.
    nodeSelectors : {str:str}, default is Undefined, optional
        Map of string keys and values that can be used to select nodes.
        Selector which must match a node's labels,
        and objects must belong to these selected nodes.
    nodes : [str], default is Undefined, optional
        Nodes is a set of node name and objects must belong to these nodes.
    podPhaseSelectors : [str], default is Undefined, optional
        PodPhaseSelectors is a set of condition of a pod at the current time.
        supported value: Pending / Running / Succeeded / Failed / Unknown
    pods : {str:[str]}, default is Undefined, optional
        Pods is a map of string keys and a set values that used to select pods.
        The key defines the namespace which pods belong,
        and the each values is a set of pod names.
    """


    annotationSelectors?: {str:str}

    expressionSelectors?: [ChaosMeshOrgV1alpha1HTTPChaosSpecSelectorExpressionSelectorsItems0]

    fieldSelectors?: {str:str}

    labelSelectors?: {str:str}

    namespaces?: [str]

    nodeSelectors?: {str:str}

    nodes?: [str]

    podPhaseSelectors?: [str]

    pods?: {str:[str]}


schema ChaosMeshOrgV1alpha1HTTPChaosSpecSelectorExpressionSelectorsItems0:
    r"""
    A label selector requirement is a selector that contains values, a key, and an operator that
    relates the key and values.

    Attributes
    ----------
    key : str, default is Undefined, required
        key is the label key that the selector applies to.
    operator : str, default is Undefined, required
        operator represents a key's relationship to a set of values.
        Valid operators are In, NotIn, Exists and DoesNotExist.
    values : [str], default is Undefined, optional
        values is an array of string values. If the operator is In or NotIn,
        the values array must be non-empty. If the operator is Exists or DoesNotExist,
        the values array must be empty. This array is replaced during a strategic
        merge patch.
    """


    key: str

    operator: str

    values?: [str]


schema ChaosMeshOrgV1alpha1HTTPChaosSpecTLS:
    r"""
    TLS is the tls config,
    will override PodHttpChaos if there are multiple HTTPChaos experiments are applied

    Attributes
    ----------
    caName : str, default is Undefined, optional
        CAName represents the data name of ca file in secret, `ca.crt` for example
    certName : str, default is Undefined, required
        CertName represents the data name of cert file in secret, `tls.crt` for example
    keyName : str, default is Undefined, required
        KeyName represents the data name of key file in secret, `tls.key` for example
    secretName : str, default is Undefined, required
        SecretName represents the name of required secret resource
    secretNamespace : str, default is Undefined, required
        SecretNamespace represents the namespace of required secret resource
    """


    caName?: str

    certName: str

    keyName: str

    secretName: str

    secretNamespace: str


schema ChaosMeshOrgV1alpha1HTTPChaosStatus:
    r"""
    chaos mesh org v1alpha1 HTTP chaos status

    Attributes
    ----------
    conditions : [ChaosMeshOrgV1alpha1HTTPChaosStatusConditionsItems0], default is Undefined, optional
        Conditions represents the current global condition of the chaos
    experiment : ChaosMeshOrgV1alpha1HTTPChaosStatusExperiment, default is Undefined, required
        experiment
    instances : {str:int}, default is Undefined, optional
        Instances always specifies podhttpchaos generation or empty
    """


    conditions?: [ChaosMeshOrgV1alpha1HTTPChaosStatusConditionsItems0]

    experiment: ChaosMeshOrgV1alpha1HTTPChaosStatusExperiment

    instances?: {str:int}


schema ChaosMeshOrgV1alpha1HTTPChaosStatusConditionsItems0:
    r"""
    chaos mesh org v1alpha1 HTTP chaos status conditions items0

    Attributes
    ----------
    reason : str, default is Undefined, optional
        reason
    status : str, default is Undefined, required
        status
    $type : str, default is Undefined, required
        type
    """


    reason?: str

    status: str

    $type: str


schema ChaosMeshOrgV1alpha1HTTPChaosStatusExperiment:
    r"""
    Experiment records the last experiment state.

    Attributes
    ----------
    containerRecords : [ChaosMeshOrgV1alpha1HTTPChaosStatusExperimentContainerRecordsItems0], default is Undefined, optional
        Records are used to track the running status
    desiredPhase : str, default is Undefined, optional
        desired phase
    """


    containerRecords?: [ChaosMeshOrgV1alpha1HTTPChaosStatusExperimentContainerRecordsItems0]

    desiredPhase?: "Run" | "Stop"


schema ChaosMeshOrgV1alpha1HTTPChaosStatusExperimentContainerRecordsItems0:
    r"""
    chaos mesh org v1alpha1 HTTP chaos status experiment container records items0

    Attributes
    ----------
    events : [ChaosMeshOrgV1alpha1HTTPChaosStatusExperimentContainerRecordsItems0EventsItems0], default is Undefined, optional
        Events are the essential details about the injections and recoveries
    id : str, default is Undefined, required
        id
    injectedCount : int, default is Undefined, required
        InjectedCount is a counter to record the sum of successful injections
    phase : str, default is Undefined, required
        phase
    recoveredCount : int, default is Undefined, required
        RecoveredCount is a counter to record the sum of successful recoveries
    selectorKey : str, default is Undefined, required
        selector key
    """


    events?: [ChaosMeshOrgV1alpha1HTTPChaosStatusExperimentContainerRecordsItems0EventsItems0]

    id: str

    injectedCount: int

    phase: str

    recoveredCount: int

    selectorKey: str


schema ChaosMeshOrgV1alpha1HTTPChaosStatusExperimentContainerRecordsItems0EventsItems0:
    r"""
    chaos mesh org v1alpha1 HTTP chaos status experiment container records items0 events items0

    Attributes
    ----------
    message : str, default is Undefined, optional
        Message is the detail message, e.g. the reason why we failed to inject the chaos
    operation : str, default is Undefined, required
        Operation represents the operation we are doing, when we crate this event
    timestamp : str, default is Undefined, required
        Timestamp is time when we create this event
    $type : str, default is Undefined, required
        Type means the stage of this event
    """


    message?: str

    operation: str

    timestamp: str

    $type: str


