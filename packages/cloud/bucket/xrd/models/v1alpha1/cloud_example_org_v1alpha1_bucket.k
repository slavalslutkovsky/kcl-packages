"""
This file was generated by the KCL auto-gen tool. DO NOT EDIT.
Editing this file might prove futile when you re-run the KCL auto-gen generate command.
"""

import k8s.apimachinery.pkg.apis.meta.v1

schema Bucket:
    r"""
    cloud example org v1alpha1 bucket

    Attributes
    ----------
    apiVersion : str, default is "cloud.example.org/v1alpha1", required
        APIVersion defines the versioned schema of this representation of an object. Servers should convert recognized schemas to the latest internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
    kind : str, default is "Bucket", required
        Kind is a string value representing the REST resource this object represents. Servers may infer this from the endpoint the client submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
    metadata : v1.ObjectMeta, default is Undefined, optional
        metadata
    spec : CloudExampleOrgV1alpha1BucketSpec, default is Undefined, optional
        spec
    status : CloudExampleOrgV1alpha1BucketStatus, default is Undefined, optional
        status
    """
    apiVersion: "cloud.example.org/v1alpha1" = "cloud.example.org/v1alpha1"

    kind: "Bucket" = "Bucket"

    metadata?: v1.ObjectMeta
    spec?: CloudExampleOrgV1alpha1BucketSpec
    status?: CloudExampleOrgV1alpha1BucketStatus

schema CloudExampleOrgV1alpha1BucketSpec:
    r"""
    Portable object-storage Bucket. Every field below is a COMMON
    parameter mapped to each cloud's native resources (GCP Cloud
    Storage Bucket, AWS S3 Bucket + companion configuration MRs).


    Attributes
    ----------
    blockPublicAccess : bool, default is True, optional
        Block all public access. GCP publicAccessPrevention=enforced / AWS BucketPublicAccessBlock (all four flags).
    cors : [CloudExampleOrgV1alpha1BucketSpecCorsItems0], default is Undefined, optional
        Cross-Origin Resource Sharing rules.
    crossplane : CloudExampleOrgV1alpha1BucketSpecCrossplane, default is Undefined, optional
        crossplane
    deletionPolicy : str, default is "Delete", optional
        What happens to the cloud bucket when this resource is deleted; maps to Crossplane managementPolicies (Orphan keeps the external bucket).
    encryptionKmsKeyId : str, default is Undefined, optional
        CMEK/SSE-KMS key. When set, GCP encryption.defaultKmsKeyName / AWS SSE aws:kms with this key; otherwise provider-default encryption.
    forceDestroy : bool, default is Undefined, optional
        Delete all contained objects when the bucket is destroyed. GCP/AWS forceDestroy.
    lifecycleRules : [CloudExampleOrgV1alpha1BucketSpecLifecycleRulesItems0], default is Undefined, optional
        Object lifecycle management rules (in addition to the storageClass tier mapping).
    logging : CloudExampleOrgV1alpha1BucketSpecLogging, default is Undefined, optional
        logging
    region : str, default is Undefined, required
        Cloud region/location for the Bucket (GCS location / S3 region).
    requesterPays : bool, default is Undefined, optional
        Charge requesters for access. GCP requesterPays / AWS BucketRequestPaymentConfiguration.
    retention : CloudExampleOrgV1alpha1BucketSpecRetention, default is Undefined, optional
        retention
    storageClass : str, default is "standard", optional
        Abstract storage tier; mapped to each cloud's native value (GCP native storage class; AWS lifecycle transition).
    tags : {str:str}, default is Undefined, optional
        Key/value labels applied to the bucket. GCP labels / AWS tags.
    uniformAccess : bool, default is True, optional
        Disable per-object ACLs, enforce bucket-level IAM. GCP uniformBucketLevelAccess / AWS BucketOwnershipControls=BucketOwnerEnforced.
    versioning : bool, default is True, optional
        Keep noncurrent object versions. GCP Bucket.versioning / AWS BucketVersioning.
    website : CloudExampleOrgV1alpha1BucketSpecWebsite, default is Undefined, optional
        website
    """
    blockPublicAccess?: bool = True
    cors?: [CloudExampleOrgV1alpha1BucketSpecCorsItems0]
    crossplane?: CloudExampleOrgV1alpha1BucketSpecCrossplane
    deletionPolicy?: "Delete" | "Orphan" = "Delete"
    encryptionKmsKeyId?: str
    forceDestroy?: bool = False
    lifecycleRules?: [CloudExampleOrgV1alpha1BucketSpecLifecycleRulesItems0]
    logging?: CloudExampleOrgV1alpha1BucketSpecLogging
    region: str

    requesterPays?: bool = False
    retention?: CloudExampleOrgV1alpha1BucketSpecRetention
    storageClass?: "standard" | "nearline" | "cold" | "archive" = "standard"
    tags?: {str:str}
    uniformAccess?: bool = True
    versioning?: bool = True
    website?: CloudExampleOrgV1alpha1BucketSpecWebsite

schema CloudExampleOrgV1alpha1BucketSpecCorsItems0:
    r"""
    cloud example org v1alpha1 bucket spec cors items0

    Attributes
    ----------
    headers : [str], default is Undefined, optional
        Allowed request headers.
    maxAgeSeconds : int, default is 3600, optional
        Preflight cache duration.
    methods : [str], default is Undefined, required
        Allowed HTTP methods.
    origins : [str], default is Undefined, required
        Allowed origins ("*" for any).
    """
    headers?: [str]
    maxAgeSeconds?: int = 3600
    methods: [str]

    origins: [str]

schema CloudExampleOrgV1alpha1BucketSpecCrossplane:
    r"""
    Crossplane composition selection, injected into every v2 XR by Crossplane itself; a composer sets compositionSelector or compositionRef to pick the backend.

    Attributes
    ----------
    compositionRef : CloudExampleOrgV1alpha1BucketSpecCrossplaneCompositionRef, default is Undefined, optional
        composition ref
    compositionRevisionRef : CloudExampleOrgV1alpha1BucketSpecCrossplaneCompositionRevisionRef, default is Undefined, optional
        composition revision ref
    compositionRevisionSelector : CloudExampleOrgV1alpha1BucketSpecCrossplaneCompositionRevisionSelector, default is Undefined, optional
        composition revision selector
    compositionSelector : CloudExampleOrgV1alpha1BucketSpecCrossplaneCompositionSelector, default is Undefined, optional
        composition selector
    compositionUpdatePolicy : str, default is Undefined, optional
        composition update policy
    """
    compositionRef?: CloudExampleOrgV1alpha1BucketSpecCrossplaneCompositionRef
    compositionRevisionRef?: CloudExampleOrgV1alpha1BucketSpecCrossplaneCompositionRevisionRef
    compositionRevisionSelector?: CloudExampleOrgV1alpha1BucketSpecCrossplaneCompositionRevisionSelector
    compositionSelector?: CloudExampleOrgV1alpha1BucketSpecCrossplaneCompositionSelector
    compositionUpdatePolicy?: "Automatic" | "Manual"

schema CloudExampleOrgV1alpha1BucketSpecCrossplaneCompositionRef:
    r"""
    cloud example org v1alpha1 bucket spec crossplane composition ref

    Attributes
    ----------
    name : str, default is Undefined, required
        name
    """
    name: str

schema CloudExampleOrgV1alpha1BucketSpecCrossplaneCompositionRevisionRef:
    r"""
    cloud example org v1alpha1 bucket spec crossplane composition revision ref

    Attributes
    ----------
    name : str, default is Undefined, required
        name
    """
    name: str

schema CloudExampleOrgV1alpha1BucketSpecCrossplaneCompositionRevisionSelector:
    r"""
    cloud example org v1alpha1 bucket spec crossplane composition revision selector

    Attributes
    ----------
    matchLabels : {str:str}, default is Undefined, required
        match labels
    """
    matchLabels: {str:str}

schema CloudExampleOrgV1alpha1BucketSpecCrossplaneCompositionSelector:
    r"""
    cloud example org v1alpha1 bucket spec crossplane composition selector

    Attributes
    ----------
    matchLabels : {str:str}, default is Undefined, required
        match labels
    """
    matchLabels: {str:str}

schema CloudExampleOrgV1alpha1BucketSpecLifecycleRulesItems0:
    r"""
    cloud example org v1alpha1 bucket spec lifecycle rules items0

    Attributes
    ----------
    expirationDays : int, default is Undefined, optional
        Delete objects this many days after creation.
    id : str, default is Undefined, required
        Rule identifier.
    prefix : str, default is Undefined, optional
        Object key prefix the rule matches.
    transitionDays : int, default is Undefined, optional
        Transition objects after this many days.
    transitionStorageClass : str, default is Undefined, optional
        Abstract tier to transition to; mapped per-provider.
    """
    expirationDays?: int
    id: str

    prefix?: str = ""
    transitionDays?: int
    transitionStorageClass?: "standard" | "nearline" | "cold" | "archive"

schema CloudExampleOrgV1alpha1BucketSpecLogging:
    r"""
    Server access logging to another bucket.

    Attributes
    ----------
    targetBucket : str, default is Undefined, required
        Destination bucket name for access logs.
    targetPrefix : str, default is Undefined, optional
        Object key prefix for log objects.
    """
    targetBucket: str

    targetPrefix?: str

schema CloudExampleOrgV1alpha1BucketSpecRetention:
    r"""
    Write-once-read-many object retention. GCP retentionPolicy / AWS Object Lock.

    Attributes
    ----------
    days : int, default is Undefined, required
        Minimum retention in days.
    mode : str, default is "governance", optional
        AWS Object Lock mode. compliance is irreversible and locks the GCP retention policy.
    """
    days: int

    mode?: "governance" | "compliance" = "governance"

    check:
        days >= 1

schema CloudExampleOrgV1alpha1BucketSpecWebsite:
    r"""
    Static website hosting.

    Attributes
    ----------
    errorDocument : str, default is Undefined, optional
        Object served for 404s (GCP notFoundPage / AWS error document).
    indexDocument : str, default is "index.html", optional
        Suffix served for directory requests (GCP mainPageSuffix / AWS index document).
    """
    errorDocument?: str
    indexDocument?: str = "index.html"

schema CloudExampleOrgV1alpha1BucketStatus:
    r"""
    cloud example org v1alpha1 bucket status

    Attributes
    ----------
    arn : str, default is Undefined, optional
        AWS ARN (AWS only).
    bucketName : str, default is Undefined, optional
        Name of the provisioned bucket.
    "cloud-url" : str, default is Undefined, optional
        Cloud-console URL for the bucket.
    endpoint : str, default is Undefined, optional
        HTTP(S) host/domain for the bucket.
    id : str, default is Undefined, optional
        Provider-assigned resource id.
    provider : str, default is Undefined, optional
        Cloud backend that fulfilled the claim (gcp | aws).
    ready : bool, default is Undefined, optional
        True once the backing cloud bucket exists.
    region : str, default is Undefined, optional
        Region/location the bucket resides in.
    selfLink : str, default is Undefined, optional
        GCP self link (GCP only).
    storageClass : str, default is Undefined, optional
        Native storage class reported by the provider.
    url : str, default is Undefined, optional
        Native storage URI (gs://… or s3://…).
    """
    arn?: str
    bucketName?: str
    "cloud-url"?: str
    endpoint?: str
    id?: str
    provider?: str
    ready?: bool
    region?: str
    selfLink?: str
    storageClass?: str
    url?: str
