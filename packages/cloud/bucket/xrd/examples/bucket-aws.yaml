apiVersion: cloud.example.org/v1alpha1
kind: Bucket
metadata:
  namespace: default
  name: my-bucket
spec:
  region: us-central1
  storageClass: archive   # standard | nearline | cold | archive (optional, default standard)
  crossplane:
    compositionSelector:
      matchLabels:
        provider: aws   # the flag: picks the backend
