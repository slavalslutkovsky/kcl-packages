# queue-xrd — KCL schemas of the queue XRD, generated from ./xrd.yaml
# via `just xrd-schema queue` (kcl import -m crd). Import the composite type:
#   import queue_xrd.models.v1alpha1.<group>_v1alpha1_<kind> as <alias>
# Regenerate after every change to xrd.yaml; models/ is not hand-edited.
_generated = "see ./models for schemas"
