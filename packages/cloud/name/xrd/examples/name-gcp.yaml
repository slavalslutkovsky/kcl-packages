apiVersion: cloud.example.org/v1alpha1
kind: Name
metadata:
  namespace: default
  name: my-name
spec:
  region: us-central1
  storageClass: archive   # standard | nearline | cold | archive (optional, default standard)
  crossplane:
    compositionSelector:
      matchLabels:
        provider: gcp   # the flag: picks the backend
